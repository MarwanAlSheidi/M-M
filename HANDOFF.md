# وثيقة التسليم — تطبيق الخطة المالية الخماسية

**لمن:** المطوّر الذي يستلم المشروع ولم يره من قبل.
**تحلّ محلّ:** وثيقة التسليم الأصلية المكتوبة أيام كان التطبيق داخل بيئة Artifacts
— القسمان 0 و8 منها لم يعودا صحيحين، وكل ما وردا فيه أُنجز.

الفرع: `claude/khutta-maliya-app-u0t1v3`

---

## 0. اقرأ هذا أولاً

ثلاث حقائق تختصر عليك ساعات:

1. **المحرّك الحسابي مُتحقَّق منه آلياً.** `npm run verify` يشغّل 50 فحصاً في
   متصفّح حقيقي مقابل حالة اختبار مرجعية. **شغّله قبل أي تعديل وبعده.** إن كسرت
   رقماً ستعرف خلال دقيقتين بدل أن يكتشفه عميل.

2. **أخطر الأخطاء هنا لا تُرى في الكود ولا في نصّ الصفحة** — تُنتج مخرجاً يبدو
   سليماً وهو مقلوب. القسم 11 يسرد ما وقعنا فيه فعلاً؛ اقرأه قبل أن تلمس طبقة
   العرض.

3. **`migrate()` هي الحدّ الأمني الوحيد للبيانات.** كل ما يدخل التطبيق يمرّ بها.
   انهيار فيها **دائم لا عابر** — انظر 4.3.

---

## 1. تشغيل سريع

```bash
npm install
npm run dev            # خادم التطوير على 5173
npm run build          # بناء للإنتاج
npm run verify         # المحرّك (50 فحصاً) + الجوال (8 شاشات)
npm run verify:mobile  # فحص الجوال وحده
```

الفحوصات تحتاج متصفّح Playwright (يُنزَّل تلقائياً أول مرة). في بيئة فيها متصفّح
جاهز مرّر مساره: `CHROMIUM_PATH=/path/to/chrome npm run verify`.

**حجم البناء:** 77ك مضغوطة لأول تحميل، و103ك إضافية تُحمَّل كسولاً عند شاشة
«القراءة» وحدها (recharts).

---

## 2. ما هو التطبيق

محوَّل من ملف Excel من 11 ورقة إلى تطبيق تخطيط مالي عربي (RTL) لعملاء كوتش مالي
في عُمان والخليج. الجمهور: **عميل يملأ خطته على جواله**، وكوتش يقرأ التقرير قبل
الجلسة. مقاس 390px ليس حالة طرفية بل الحالة الأساسية.

| # | المفتاح | الوظيفة |
|---|---|---|
| 01 | `facts` | العمر، الدخل، العملة + 6 أسئلة وقائع |
| 02 | `identity` | 16 بطاقة قرار + تحليل الشخصية |
| 03 | `goals` | أهداف تُضاف (لا صفوف فارغة) + معدّل التضخّم |
| 04 | `wealth` | أصول والتزامات + جدول سداد الديون + حاسبة الزكاة |
| 05 | `budget` | 14 بند مصروف + 4 مصادر دخل + 50/30/20 |
| 06 | `safety` | صندوق الطوارئ |
| 07 | `summary` | محاكاة التدفق النقدي + الاستقلال المالي |
| 08 | `report` | تقرير الكوتش (طباعة + تصدير HTML مستقل) |

---

## 3. البنية

```
index.html                 الخطوط + جذر RTL
src/main.jsx               نقطة الدخول
src/khutta-maliya.jsx      التطبيق كاملاً (~1940 سطراً، ملف واحد بقصد)
src/CashflowChart.jsx      الرسم — مفصول ليُحمَّل كسولاً
src/lib/storage.js         غلاف التخزين
scripts/verify-engine.mjs  اختبار المحرّك
scripts/check-mobile.mjs   اختبار الجوال
```

الملفان المفصولان مفصولان **لسبب تقني لا أسلوبي**: `storage.js` ليسهل استبدال
طبقة التخزين بلمس ملف واحد، و`CashflowChart.jsx` لأن التحميل الكسول لا يعمل داخل
الملف نفسه.

`khutta-maliya.jsx` مقسوم داخلياً إلى ثلاث طبقات:

1. **ثوابت وبيانات** — `T` (الهوية البصرية)، `CARDS`، `TYPES`، `Q`، جداول البنود.
2. **دوال حساب صرفة بلا React** — `computeIdentity`, `computeCalc`,
   `computeDebtPlan`, `buildReport`, `buildReportHtml`. مفصولة عن المكوّن كي تعمل
   على بيانات أي عميل أثناء التصدير الجماعي، لا على الحالة المفتوحة فقط.
3. **المكوّن** — يغلّف كلاً منها في `useMemo` ولا يحسب شيئاً بنفسه.

---

## 4. البيانات والتخزين

### 4.1 المفاتيح

| المفتاح | المحتوى |
|---|---|
| `khutta-maliya:clients-v1` | فهرس العملاء `[{id,name,updatedAt}]` |
| `khutta-maliya:active-v1` | معرّف العميل المفتوح |
| `khutta-maliya:plan:<id>` | خطة عميل واحد |
| `khutta-maliya:v2` | **قديم** — خطة واحدة، تُرحَّل تلقائياً إلى أول عميل |

الحفظ مؤجّل 700ms.

### 4.2 شكل الخطة

```js
{
  cur:"OMR", age:32, inflation:2.5,
  stage, income, dependents, horizon, priority, debtMethod,   // الوقائع الست
  ans: { c1:0, t1:2, ... },                    // معرّف البطاقة → فهرس الخيار 0..3
  goals: [{ id, m, type, tier, cost, fund }],  // m = 0..59
  debts: [{ id, label, balance, apr, min }], debtExtra:0,
  assets: [0,0,0,0,0,0,0],   // ترتيب ملزم — انظر أدناه
  liabs:  [0,0,0,0,0],
  exp: [{ key, label, type, cost }],           // 14 بنداً، type = احتياج | رغبة
  inc: [{ label, amount }],                    // 4 مصادر
  efNow:0, efOverride:0,                       // 0 يعني «استخدم الموصى به»
  zakat: { flags:{...}, nisab:0, hawl:true },
}
```

**ترتيب المصفوفات ملزم:** `assets[2]` الاستثمارات و`assets[3]` التقاعد، وهما
وحدهما ما يدخل حساب الاستقلال المالي. `exp` تُقرأ **بالمفتاح لا بالفهرس**
(`housing`, `debt`) لأن التسميات قابلة للتحرير من المستخدم.

### 4.3 `migrate()` — الحدّ الأمني

كل ما يدخل التطبيق يمرّ بها: التحميل من التخزين، والاستيراد من ملف، وترحيل النسخة
القديمة ذات `months[60]`. لذلك **التحقّق من الأنواع فيها لا في مواضع الاستخدام**.

هي تعيد بناء `assets`/`liabs`/`exp`/`inc` من الجداول المرجعية بدل الوثوق بالوارد،
وتحدّ الأرقام، وتقصر الحقول المعدودة على قيمها المعروفة، وتُسقط أي إجابة بطاقة
فهرسها خارج مدى خيارات تلك البطاقة.

**لماذا هذا التشدّد:** الانهيار في هذه الطبقة **دائم لا عابر**. الخطة تُحفَظ في
`localStorage`، فتُقرأ عند كل إقلاع وتنهار من جديد — يبقى التطبيق معطوباً بلا مخرج
إلا مسح تخزين المتصفّح يدوياً. ملفٌ واحد فيه `exp:null` أو `ans:{c1:99}` كان يكفي
لذلك قبل التشديد.

**لا تكسر ترحيل `months[60]` — فيه بيانات مستخدمين.**

---

## 5. المحرّك الحسابي

كله في `computeCalc(d, horizon, identity)`.

### 5.1 الأساسيات
```
net         = Σassets − Σliabs
needs       = Σ exp[type=احتياج].cost
wants       = Σ exp[type=رغبة].cost
income      = Σ inc.amount
surplus     = income − totalExp
savingsRate = surplus / income
```

### 5.2 قاعدة 50/30/20 — إليزابيث وارن، «كل ما تملك»
`needs/income ≤ .50` · `wants/income ≤ .30` · `surplus/income ≥ .20`

### 5.3 قاعدة 28/36 — الاكتتاب العقاري
`housing/income ≤ .28` · `(housing + debt)/income ≤ .36`

### 5.4 صندوق الطوارئ — الادخار التحوّطي (كارول)
```
base   = { fixed:3, mixed:6, var:9, season:12 }[d.income]
behav  = (تناقض في البطاقات أو P.now ≥ 25) ? 1 : 0
efRec  = min(12, base + شريحة المعالين + behav)
efMonths = d.efOverride || efRec
efTarget = needs × efMonths
efGap    = max(efTarget − efNow, 0)
efCover  = efNow / needs
```

### 5.5 صافي الثروة المتوقع — ستانلي ودانكو
```
expectedNet = age × (income × 12) / 10
netRatio    = net / expectedNet
```
`≥2` متميّز · `≥0.5` متوسط · دون ذلك أقل من المتوقع.
**التنبيه لا يُطلق إلا عند `age ≥ 35`** — المعادلة قاسية على بداية المسار المهني.

### 5.6 الاستقلال المالي — قاعدة السحب 4٪ (ترينيتي)
```
fiTarget = totalExp × 12 × 25
invested = assets[2] + assets[3]
إن كان invested ≥ fiTarget           →  0  (بلغه فعلاً)
إن لم يكن وكان الفائض ≤ 0            →  null
وإلا: b = b×1.04 + surplus×12 حتى b ≥ fiTarget (سقف 60 سنة)
fiPlus = نفس الحساب بفائض + 5٪ من الدخل → سطر الحساسية
```
**`0` قيمة صحيحة تعني «بلغه»، وهي زائفة في JS** — كل مواضع الاستخدام تختبر
`!== null` لا الصدق. تجاهُل هذا يجعل الإصلاح بلا أثر.

### 5.7 نطاق الأصول النامية
```
BANDS = ["0–20٪","20–40٪","40–60٪","60–75٪","75–90٪"]
hs   = { s:0, m:1, l:2, xl:3 }[horizon]
tilt = P.grow ≥ P.safe+20 ? 3 : P.grow ≥ P.safe ? 2 : P.safe ≥ P.grow+20 ? 0 : 1
band = BANDS[ clamp(round((hs + tilt) × 0.72), 0, 4) ]
```
الميل من **سلوك البطاقات**، لا من تصريح المستخدم عن تحمّله للمخاطرة.

### 5.8 التضخّم
تكلفة الهدف تُدخَل بأسعار اليوم وتُصرف مستقبلاً، فتُضخَّم قبل دخول المحاكاة:
```
nominalOf(cost, m) = cost × (1 + inflation/100)^(m/12)
```

### 5.9 محاكاة التدفق النقدي — أهمّ جزء في التطبيق

تُصلح خللاً منطقياً: المقارنة الساذجة `تكلفة السنة ≤ الفائض×12` كانت تصرف الفائض
مرتين، مرة على الطوارئ ومرة على الأهداف.

```js
for (i = 0; i < 60; i++) {
  y = floor(i/12); s = surplus;
  if (ef > 0 && s > 0) { put = min(ef, s); ef -= put; s -= put;
                         if (ef === 0 && efDone === null) efDone = i; }
  pot += s;  capYear[y] += s;
  due = dueBy[i] || 0;                       // dueBy مضخَّمة بالتضخّم
  if (due) { costYear[y] += due; pot -= due;
             if (pot < 0 && firstShort === null) firstShort = i; }
  if (pot < worst) worst = pot;
}
```
المخرجات: `efDone` · `firstShort` · `worstShort` · `endPot` · `capYear[5]`
(«المتاح فعلياً») · `costYear[5]`.

الرسم يقارن `costYear` بـ `capYear`، **لا** بالفائض الخام.

### 5.10 جدول سداد الديون
حد أدنى على كل دين شهرياً، ثم الدفعة الإضافية تُوجَّه حسب الترتيب:
الانهيار الجليدي = الأعلى فائدة أولاً، كرة الثلج = الأصغر رصيداً أولاً.
سقف 600 شهر؛ تجاوزه يعني `neverPaidOff` (الحد الأدنى لا يغطي الفائدة).

### 5.11 الزكاة
`2.5٪` على الأصول المؤشّرة زكوية، إن بلغت النصاب ومضى الحول. النصاب **إدخال يدوي**
ولا يُحسب من سعر ذهب حيّ — لا مصدر أسعار متاح، وتثبيت رقم قديم في عبادة أسوأ من
تركه للمستخدم.

---

## 6. بطاقات القرار

16 بطاقة × 4 خيارات، كل خيار موسوم بفئة من سبع:
`plan` تخطيط · `safe` أمان · `grow` نمو · `now` اندفاع · `face` مكانة ·
`give` عطاء · `hide` تجنّب.

```
P[cat] = round(count[cat] / answeredCount × 100)
ready  = answeredCount ≥ ceil(16 × 0.6) = 10
top    = الفئة الأعلى تكراراً
```

### كشف التناقض
```
present = ans.t1 > ans.t2      // الخصم المفرط — لايبسون
frame   = ans.t3 !== ans.t4    // المحاسبة الذهنية — ثالر
incons  = present || frame
profile = incons ? SPLIT : TYPES[top]
```

`t1`/`t2` تسألان عن ثمن انتظار شهر واحد، مرة من اليوم ومرة بعد سنة — **الفارق
الزمني نفسه**. `t3`/`t4` تعطيان 300 من مصدرين (مكافأة مقابل وفر). تبدوان بطاقتين
عاديتين للمستخدم **وهذا مقصود** — لا تكشف الغرض في الواجهة.

`TYPES` فيه 7 أنماط و`SPLIT` هو الثامن. كل نمط: `name, sub, d, good, trap, say,
acts[3]`. و`acts[0..1]` تُحقن تلقائياً في «الخطوات التالية» في التقرير.

---

## 7. التقرير والتصدير

- `rep.flags` — تنبيهات بثلاث درجات (`high`/`med`/`low`)، **مرتّبة بترتيب الإضافة
  في الكود لا بالفرز**. أي عتبة جديدة تُضاف بـ `add(lvl, title, detail)`.
- `rep.actions` — أربع أو خمس خطوات، تخلط حالة الأرقام بـ `acts` النمط السلوكي.
- `buildReportHtml()` يبني مستنداً مستقلاً بتنسيقه الخاص. **لا تحذفه** — هو المسار
  الاحتياطي للطباعة، وهو الملف الذي يصل العميل فعلاً.
- `exportJson` / `importJson` زوج لا يُفصل. الاستيراد يُنشئ عميلاً **جديداً** ولا
  يكتب فوق الحالي، ويشترط مفتاحاً واحداً على الأقل من بنية الخطة لأن `migrate()`
  تملأ الفراغات فأيّ JSON عشوائي يصير خطة فارغة تبدو سليمة.
- التصدير الجماعي يعيد تشغيل خط الحساب كاملاً لكل عميل بـ `horizon = 60` دائماً.

---

## 8. الهوية البصرية

نظام «Horizon Financial» من مشروع Stitch. الثابت `T` أعلى `khutta-maliya.jsx` هو
**نقطة التبديل الوحيدة** — كل طبقة العرض inline styles تقرأ منه.

| الرمز | القيمة | الاستعمال |
|---|---|---|
| `ink` | `#1A2B48` | كحلي — الترويسة والعناوين |
| `accent` | `#3F51B5` | نيلي — التفاعل وحلقة التركيز |
| `good` | `#10B981` | زمردي — النمو والفائض |
| `bad` | `#EF4444` | أحمر — العجز والتنبيهات |
| `fillLine` | `#FBBF24` | كهرماني — الحقول القابلة للتحرير |
| خطوط | IBM Plex Sans Arabic (نص) · IBM Plex Sans (أرقام) | |
| أنصاف الأقطار | بطاقات 8px · حقول 4px | |

---

## 9. قواعد ثابتة لا تُكسر

- **RTL:** `dir="rtl"` على الجذر. استخدم `marginInlineStart` و`borderInlineStart`
  لا `marginLeft`/`borderLeft`.

- **الأرقام:** `direction:"ltr"` و`fontFamily:T.mono` على كل خانة فيها رقم.
  **هذه ليست قاعدة تجميلية** — انظر 11.1.

- **`whiteSpace:"nowrap"` ليس جزءاً من القاعدة السابقة.** إعادته تمدّ القيم
  الطويلة خارج شاشة الجوال، والالتفاف لا يكسر رقماً أصلاً (لا فراغ داخل `53,760`).

- **العملة:** `money(n, cur)` — العماني والكويتي والبحريني بثلاث خانات عشرية،
  والباقي باثنتين.

- **`NumberField`** يحتفظ بنص داخلي أثناء التركيز حتى لا يُفسد الكتابة (`"12."`
  تكسر التحويل المباشر). **لا تبسّطه إلى `value={n}`.**

- **لا نموذج `<form>`** — معالجات `onClick` فقط.

- **`recharts` تُستورد كسولاً فقط**، ولا تُسمَّى في `manualChunks`: التسمية
  تُدخلها في رسم التحميل المسبق فيضيف Vite لها `modulepreload` وتُحمَّل عند أول
  فتح رغم كسل الاستيراد.

- **أرقام التقرير من `byTierAll` لا `byTier`** — الثاني مقيَّد بمرشّح الأفق في
  شاشة الأهداف، وهو مرشّح عرض لا يجوز أن يغيّر تقريراً مطبوعاً.

- **أي رقم يدخل جملة توصية يحتاج فرعاً لحالة الصفر وحالة عدم الكفاية** — انظر 11.2.

---

## 10. الاختبارات

```bash
npm run verify
```

يبني، يشغّل خادماً، يقود متصفّحاً حقيقياً، ويقارن **ما يُعرض فعلاً** بالمتوقّع.
50 فحصاً + 8 شاشات جوال.

يغطّي: حالة الاختبار المرجعية · التضخّم · الزكاة (فوق/دون النصاب، الحول) · مقارنة
الانهيار الجليدي بكرة الثلج · اتجاه الخانات الرقمية في الشاشة **وفي الملف
المصدَّر** · التحميل الكسول للرسم · الاستقلال المالي لمن بلغه · دورة
تصدير→مسح→استيراد · سبع حمولات مشوّهة · التمدّد الأفقي وبتر الحقول على 390px.

**الفخّ المرجعي:** «المتاح فعلياً» للسنة الأولى يجب أن يكون **صفراً**. لو أظهرت
السنة الأولى «ضمن الفائض ✓» فقد رجع أحدهم إلى المقارنة الساذجة.

**تحفّظ على البيانات المرجعية:** الوثيقة الأصلية تعطي إجمالي تكلفة الأهداف
(24,400) دون توزيعها على الأشهر. التوزيع في `baseCase()` اجتهاد يحافظ على الإجمالي
وعلى أول شهر عجز. لذلك قيمتان مرتبطتان بالتوزيع — أقصى عجز والرصيد الختامي —
تختلفان عن رقمي الوثيقة (17,105 و−15,390) وتُقارَنان بما ينتجه هذا التوزيع
(15,865 و−14,640). **بقية القيم تطابق الوثيقة حرفياً.** لو ظهر التوزيع الأصلي،
حدّث `baseCase()` وحدها.

---

## 11. مصائد وقعنا فيها فعلاً

أنفع قسم في الوثيقة. كلها أخطاء حقيقية شُحنت أو كادت، وكلها **تنتج مخرجاً يبدو
سليماً**.

### 11.1 انقلاب ترتيب الأرقام (bidi) — الأخطر

خانة فيها عدّة مقاطع رقمية مفصولة بمحايدات — `65.7% / 16.8% / 17.5%` — **تنقلب
بصرياً** بلا `direction:ltr` تحت أساس RTL، فتُقرأ `17.5% / 16.8% / 65.7%`. أي يرى
الكوتش الاحتياجات 17.5٪ والادخار 65.7٪ — عكس واقع العميل تماماً. وأصابت أيضاً
`الدخل / المصروف` فتبادلا.

**لماذا لم يكشفها شيء:** `textContent` يعيد الترتيب المنطقي مهما كان العرض. كل
فحص نصّي ينجح والمخرج مقلوب. الفحص الآن **يقيس مواضع البكسل** للمقاطع الرقمية،
في الشاشة وفي الملف المصدَّر منفصلَين.

**كيف دخلت:** أثناء إصلاح تمدّد الجوال حُذف `nowrap` و`ltr` معاً، والمشكلة كانت
في `nowrap` وحده.

### 11.2 توصيات تُصاغ بلا شرط

- «تأجيل أهداف الطموح (**0 ر.ع.**)» لخطة لا طموح فيها.
- «خفض الرغبات بمقدار **0**» لعميل عجزه كلّه في الاحتياجات — وهي حالة تحتاج
  توصية معاكسة تماماً.
- «خفض الرغبات بمقدار X» حين تكون الرغبات أقل من العجز، فتوهم أنها تغلقه.
- «نمطك الثاني … بنسبة **0٪**» لنمط لم يظهر منه شيء.

### 11.3 الاستقلال المالي لمن بلغه

الحلقة تبدأ من `y=1` فلا تختبر الرصيد الابتدائي، وشرط «الفائض موجب» كان يسبق فحص
البلوغ. فمتقاعد بمحفظة تفوق هدفه وفائض سالب — **وهي بالضبط حال مرحلة «الإنفاق»
التي يعرضها التطبيق** — يُقال له «لا فائض حالياً» عن استقلال بلغه.

### 11.4 ملف واحد يعطب التطبيق للأبد

`migrate()` كانت تمرّر الأنواع بلا فحص. `exp:null` أو `ans:{c1:99}` ينهار عند
الرسم، **ويظلّ منهاراً بعد إعادة التحميل** لأن الخطة حُفظت. لا مخرج إلا مسح تخزين
المتصفّح يدوياً. الفحص الآن يشترط بقاء التطبيق حيّاً **بعد إعادة التحميل**، لا
عند الاستيراد فقط.

### 11.5 بتر الحقول على الجوال

شبكة الديون ذات الأعمدة الخمسة الثابتة كانت تبتر الحقول عند 390px: رصيد **5000
يُعرض «50»**، وحد أدنى 150 يُعرض «15». القيمة في الحالة صحيحة والحساب صحيح —
**والمعروض خطأ**. البتر لا يسبّب تمدّداً أفقياً فلا يكشفه فحص التمدّد؛ يُفحص
صراحةً بمقارنة `scrollWidth` بـ `clientWidth` لكل حقل.

### 11.6 «تحميل كسول» لا يؤجّل شيئاً

تسمية `recharts` في `manualChunks` أدخلتها في رسم التحميل المسبق، فأضاف Vite
`<link rel="modulepreload">` وحمّل 500ك عند أول فتح رغم `lazy()`. الفحص الآن يعدّ
حزم الجافاسكربت المطلوبة على الشبكة، **لا** يبحث عن اسم ملف (الاسم تغيّر مع
الإصلاح فمرّ الفحص فارغاً).

---

## 12. حدود التطبيق — اقرأ قبل أي وعد للعميل

1. **لا خادم.** التخزين كلّه `localStorage`: عملاء الكوتش على **متصفّح واحد على
   جهاز واحد**. العميل يملأ على جواله والكوتش لا يرى شيئاً إلا بتصدير ملف يدوياً.
   «وضع الكوتش» يدير عملاء متعددين على جهاز واحد، لا فريقاً ولا مزامنة.

2. **لا حماية للبيانات.** أعمار ورواتب وديون وأصول عملاء حقيقيين، غير مشفّرة، بلا
   تسجيل دخول. أي شخص يفتح الجهاز يراها. لاستخدام مهني هذه مسألة قانونية قبل أن
   تكون تقنية.

3. **لا نشر.** مستودع لا موقع.

4. **بطاقات القرار غير مقنَّنة.** مستلهمة من أدوات منشورة (كلونتز، ثالر، لايبسون،
   يامَوتشي وتمبلر) لكنها **لم تُختبر على عيّنة خليجية**. التنبيه موجود في الواجهة
   وفي التقرير المصدَّر، لكن عرضها كأداة تشخيص **يحتاج مراجعة مختصّ قبل أي استخدام
   تجاري**. ليست مهمة برمجية.

5. **الزكاة اجتهاد عام لا فتوى.** تأشير الأصول الزكوية افتراض شائع قابل للتعديل،
   والنصاب إدخال يدوي.

6. تنسيق البطاقات الورقية الأصلي علامة تخص صباح المقبالية؛ السيناريوهات هنا أصلية
   والمنطق مستوحى منها.

---

## 13. أول ثلاث خطوات مقترحة

1. **النشر** (Netlify / Vercel / GitHub Pages) — عمل قصير يحوّله إلى رابط يُفتح
   من أي جوال. لا يحلّ تشتّت البيانات.

2. **خادم بيانات** (Supabase أقرب خيار) — يحلّ المزامنة والحماية وتعدّد الأجهزة
   معاً. استبدل `src/lib/storage.js` وحده؛ الواجهة لا تعرف مصدر البيانات.
   انتبه: `computeCalc` وأخواتها دوال صرفة، فالانتقال لا يمسّ المحرّك.

3. **مراجعة البطاقات** مع مختصّ قبل أي عرض تجاري.
