# مراجعة الكود الأصلي — ما أُصلح ولماذا

المواصفات الأصلية في `PROJECT_SPEC.md` سليمة معمارياً، لكن كود `main.js` فيها
كان يحتوي أخطاء تمنع تشغيله في الإنتاج. هذه قائمة بها مرتّبة بالخطورة.

---

## 🔴 حرج — ثغرات مالية

### 1. التبرع بدون دفع
```js
// الأصلي
Parse.Cloud.define("fundRequest", async (request) => {
    const { requestId, amount } = request.params;
    mosque.set("walletBalance", currentBalance + amount);   // ← من أين جاء المال؟
```
الدالة تزيد رصيد المسجد بمبلغ يرسله العميل، دون أي تفاعل مع بوابة دفع.
أي شخص يستدعي `fundRequest({amount: 1000000})` من Postman يصبح متبرعاً بمليون ريال.

**الإصلاح:** فُصلت إلى `initiateDonation` (تُنشئ معاملة `pending` + جلسة دفع)
و`confirmDonation` (تتحقق من البوابة ثم تُقيّد). المصدر الوحيد للحقيقة هو
استعلام البوابة، لا مدخلات العميل.

### 2. الرصيد يصبح NaN
```js
const mosque = serviceReq.get("mosqueId");        // Pointer غير مُحمّل
const currentBalance = mosque.get("walletBalance") || 0;   // undefined → 0
mosque.set("walletBalance", 0 + amount);          // ← مسح الرصيد السابق!
```
الـ Pointer المُعاد من `get()` لا يحمل بياناته. النتيجة أن كل تبرع كان
**يستبدل** الرصيد بدل أن يضيف إليه.

**الإصلاح:** `fetchPointer()` قبل القراءة، و`increment()` الذرّية بدل
قراءة‑ثم‑كتابة (التي تفقد تبرعات متزامنة أصلاً).

### 3. تمويل جزئي يُعتبر كاملاً
طلب تكلفته 500 ريال يصبح `funded` بتبرع قدره ريال واحد.

**الإصلاح:** حقل `fundedAmount` تراكمي؛ الحالة تتغيّر عند بلوغ `estimatedCost`،
ويُرفض أي مبلغ يتجاوز المتبقي.

---

## 🟠 عالٍ — إشعارات لا تصل أبداً

```js
await Parse.Push.send({ where: { "role": "imam" }, ... });
await Parse.Push.send({ where: { objectId: { $in: userIds } }, ... });
```
`Parse.Push.send` يستعلم على فئة **`_Installation`** (الأجهزة) لا `_User`.
فئة `_Installation` لا تحتوي حقل `role`، و`objectId` فيها معرّف جهاز لا معرّف
مستخدم. الاستعلامان لا يطابقان شيئاً — الإشعارات كانت ستفشل صامتة.

**الإصلاح:** `lib/push.js` يستعلم على `Parse.Installation` بشرط
`containedIn('user', users)`، ما يستلزم ربط الـ Installation بالمستخدم عند
تسجيل الدخول في التطبيق.

كذلك: الإشعار عند التمويل كان يذهب إلى **كل** الأئمة في المنصة، لا إلى إمام
المسجد المعني.

---

## 🟠 عالٍ — ثغرة صلاحيات

```js
Parse.Cloud.define("completeService", async (request) => {
    const query = new Parse.Query("ServiceRequests");
    query.equalTo("mosqueId", mosque);
    const serviceReq = await query.get(requestId, ...);   // ← القيد يُتجاهل
```
`Parse.Query.get()` يتجاهل قيود `equalTo` المضافة قبله. أي إمام كان يستطيع
إقفال طلب يخص مسجد إمام آخر.

**الإصلاح:** جلب الطلب أولاً ثم التحقق اليدوي من الملكية عبر `mosqueForImam()`.

---

## 🟡 متوسط

| المشكلة | الإصلاح |
|---|---|
| `throw new Error()` يفقد كود الخطأ عند العميل | `Parse.Error` بأكواد قياسية — `lib/errors.js` |
| `getNearbyMosques` بلا حد أقصى للنتائج ولا للنطاق | سقف 100 نتيجة و50 كم، مع `select()` للحقول العامة |
| `getMosqueForImam` يفترض مسجداً واحداً لكل إمام | دعم تعدد المساجد مع `mosqueId` صريح |
| لا يوجد تحقق أن `imamId` يحمل الدور `imam` | `beforeSave` على `_User` + مسار `claimMosque` بمراجعة |
| المنفّذ يُقفل الطلب بنفسه | فصل `markWorkDone` (المنفّذ) عن `completeService` (الإمام) |
| `estimatedCost` غير محقّق — يقبل السالب واللانهائي | حدود 0–5000 ريال |
| لا يوجد صرف للشركات ولا سجل مالي | `payoutContractor` + `getMosqueLedger` |
| المستخدم يستطيع تعيين دوره `admin` بنفسه | حظر في `beforeSave` إلا بـ Master Key |
| لا يوجد حد للطلبات المفتوحة | 10 طلبات مفتوحة كحد أقصى للمسجد |

---

## 🔴 جولة ثانية — ثغرات في الكود المُصلَح نفسه

الإصلاحات أعلاه صحيحة في جوهرها، لكن ثلاثاً منها بقيت مفتوحة عند حوافّها.

### 1. `confirmDonation` بلا تحقق من الملكية — يُضيّع مال المتبرع

```js
Parse.Cloud.define('confirmDonation', async (request) => {
    requireUser(request);                       // ← أي مستخدم مصادَق، لا صاحب المعاملة
    ...
    if (!verification.paid) {
        transaction.set('status', 'failed');    // ← والجلسة قد تكون ما تزال مفتوحة
```

الشرطان معاً يفتحان مساراً لإتلاف تبرّع: يستدعي أي مستخدم مصادَق الدالة على
معاملة غيره **قبل** أن يدفع صاحبها، فالبوابة تردّ `unpaid` فتُعلَّم `failed`.
ثم يدفع المتبرع فعلاً، ويعود للتأكيد فيصطدم بشرط `status === 'pending'` —
المال مقبوض لدى البوابة ولا سبيل لقيده، ولا مسار استرداد في النظام.

**الإصلاح:** التأكيد لصاحب المعاملة وحده أو بـ Master Key (webhook البوابة).
و`verifySession` صارت تميّز الحالة النهائية (`cancelled`/`expired`) من الجلسة
التي ما تزال مفتوحة؛ الثانية تُترك `pending` ليصحّ التأكيد بعد الدفع.

### 2. سباق التمويل الزائد في `initiateDonation`

```js
const remaining = serviceRequest.get('estimatedCost') - (serviceRequest.get('fundedAmount') || 0);
```

`fundedAmount` لا يعدّ إلا المُقيَّد، فكل متبرّع يرى المتبقي كاملاً متاحاً ما دام
لم يدفع أحد بعد. خمسة متبرّعين يبدأون معاً بـ500 ريال لطلب تكلفته 500 ويدفعون
جميعاً ⇐ تُقبض 2500 ريال بلا استرداد.

**الإصلاح:** نيّات التبرّع المعلّقة تُحجز ضمن المتبقي، بمهلة 30 دقيقة يُفرَج
بعدها عن الحجز حتى لا يُعطّل متبرّعٌ لم يُكمل الدفع تمويلَ الطلب إلى الأبد.
هذا يُضيّق النافذة إلى الطلبات المتزامنة في اللحظة نفسها ولا يُلغيها كلياً —
الإلغاء التام يحتاج قيداً على مستوى قاعدة البيانات.

### 3. `_User` بلا صلاحيات في المخطط

`schema.json` عرّف `classLevelPermissions` لكل الفئات إلا `_User`، و
`apply_schema.js` لا يستدعي `setCLP` إلا عند وجود المفتاح. النتيجة أن الفئة
تبقى على إعداد الخادم الافتراضي — وقراءة `_User` العامة تكشف `phone` و
`fullName` و`lastKnownLocation`، أي المواقع الجغرافية للمتطوعين.

**الإصلاح:** صلاحيات صريحة في المخطط (`create` مفتوح لأن التسجيل يمرّ عبره،
والقراءة تتطلب مصادقة، و`protectedFields` تُخفي الحقول الحسّاسة)، ومعها
`afterSave` يقفل الـ ACL على صاحب الحساب عند التسجيل. الـ ACL هو الحماية
الفعلية: لا يُضبط في `beforeSave` لأن `objectId` لم يُسنَد بعد عند الإنشاء.

---

## 🔴 جولة رابعة — ما كشفه أول تشغيل على خادم حقيقي

شُغّل الكود على `parse-server` فعليّ فوق PostgreSQL، بالمخطط الحقيقي وبـ200 مسجد
من بيانات الوزارة. الاختبارات الـ87 كانت خضراء، ومع ذلك ظهر خللان لا يستطيع
البديل في الذاكرة رصدهما — كلاهما كان يمنع الإطلاق.

### 1. لا يستطيع أحد التسجيل إطلاقاً

```js
if (user.dirty('isVerifiedContractor')) {
    throw new Parse.Error(..., 'اعتماد الشركات يتم من الإدارة.');
}
```

`isVerifiedContractor` له `defaultValue` في `schema.json`، وParse يطبّق القيم
الافتراضية عند الإنشاء **فيُعلّم الحقل مُعدَّلاً**. فكان أول تسجيل — لإمام، لا
لشركة — يُردّ برسالة اعتماد الشركات. المنصّة كانت ستُطلق بلا إمكان إنشاء حساب
واحد.

البديل في الذاكرة لا يطبّق قيم المخطط الافتراضية، فالحالة لا تنشأ فيه أصلاً.

**الإصلاح:** الحساب الجديد يبدأ غير معتمد دائماً (`set(false)` بلا رفض)،
والتعديل بعد الإنشاء بـ Master Key وحده. والقاعدة العامة: **`dirty()` وحده لا
يصلح حارساً على حقل له `defaultValue`.**

### 2. فشل الإشعار يُسقط العملية التي يُبلّغ عنها

`createServiceRequest` يحفظ الطلب ثم يُشعر المتطوّعين القريبين. الاستعلام
الجغرافي فشل (لا فهرس مكاني في تلك البيئة)، فارتفع الخطأ وأسقط الدالة كلها —
**والطلب كان قد حُفظ فعلاً**. فيرى الإمام فشلاً ويُعيد المحاولة فيُنشئ نسخة ثانية.

وهذا ليس افتراضاً بعيداً: `CLAUDE.md` نفسه ينبّه أن فهرس `2dsphere` يُضاف يدوياً
من لوحة Back4app، وأن الإشعارات تحتاج ربط `_Installation` بالمستخدم — فكلا
الشرطين قد يغيب في أول يوم تشغيل.

**الإصلاح:** `lib/push.js` لا يرمي أبداً، أسوةً بـ`lib/audit.js`. الإشعار أثر
جانبي، والفشل يُسجَّل ويُبتلع.

---

## 🟠 جولة خامسة — ما كشفه بناء الواجهة

**البحث لا يُطبّع مصطلح المستخدم.** `nameNormalized` وُجد ليطابق «الرحمة»
بـ«الرحمه»، والبيانات تُخزَّن مطبَّعة عبر `normalize_ar` في سكربت التنظيف —
لكن `searchMosques` كان يرسل النص كما كتبه المستخدم. فالحقل بلا فائدة: من يكتب
الاسم بالتاء المربوطة أو بالهمزة لا يجد شيئاً. ظهر فور أول بحث حقيقي من
الواجهة. **الإصلاح:** `normalizeArabic` في `cloud/functions/mosques.js`، نظير
دالة بايثون حرفاً بحرف — الطرفان يمرّان بالتطبيع نفسه.

---

## 🟠 جولة سادسة — القرب الجغرافي بلا فهرس مكاني

`getNearbyMosques` و`pushToNearbyVolunteers` كانا يستعملان `withinKilometers`،
وهي تفرض فهرس `2dsphere` على MongoDB (وPostGIS على PostgreSQL). و`CLAUDE.md`
ينبّه أن الفهرس **يُضاف يدوياً** من لوحة Back4app — فقد يغيب في أول يوم تشغيل،
فيفشل الاستعلام أو يمسح 18 ألف وثيقة. وقد فشل فعلاً على أول خادم اختبار، وأسقط
معه إنشاء الطلب (الجولة الرابعة).

وموقع المسجد هو ما يربط المصلّي بمسجده — فلا يصحّ أن يتعلّق الأمر بخطوة يدوية.

**الإصلاح:** `cloud/lib/geo.js`. حقلان رقميان `lat`/`lng` يخدمهما فهرس مركّب
عادي، صندوق إحاطة يُضيّق المرشّحين إلى العشرات، ثم مسافة هافرساين الدقيقة في
الكود تُسقط زوايا الصندوق وترتّب بالأقرب. النتيجة تحمل `distanceKm` — وهو ما
تعرضه الواجهة أصلاً. حقل `location` (GeoPoint) يبقى للاستعمالات المستقبلية،
والفهرس المكاني صار تحسيناً لا شرطاً.

---

## 🟡 جولة سابعة — ما كشفه بناء بقية الواجهة

- **الإمام كان يعتمد عملاً لم يره.** `completionPhotos` معرّف ويُكتب، ولا مسار
  في الواجهة لرفع صورة — فدورة «المنفّذ يبلّغ والإمام يقفل» تقوم على الثقة
  وحدها. صار الرفع عبر `Parse.File` ثم الإبلاغ، والصور تُعرض قبل زرّ الاعتماد.
  والخادم لا يقبل إلا روابط تخزينه: رابط خارجي في سجلّ المسجد يتتبّع الإمام حين
  يفتح الطلب، أو يتغيّر محتواه بعد الاعتماد فيصير الدليل غير ما اعتُمد.
- **شاشة «مساجدي» كانت تشتقّ المساجد من `MosqueClaims`** بينما الخادم يتحقّق من
  `Mosques.imamId` في `mosqueForImam`. مسجدٌ أُسند بغير مسار الطلب — ترحيل
  بيانات أو تدخّل إداري — لا يراه إمامه رغم أن كل إجراء عليه يمرّ. `getMyMosques`
  تقرأ من مصدر الحقيقة نفسه.
- **`markWorkDone` كان يعدّل قبل أن يتحقّق:** الحالة تصير `pending_approval` ثم
  تُرفض الصور. لا يُحفظ شيء لأن `save` لا تُنفَّذ، لكن أي قراءة من الكائن نفسه
  بعدها تصدّق حالةً لم تقع. التحقّق أولاً.

---

## 🟠 جولة ثامنة — البحث بكلمة من وسط الاسم

**العَرَض:** المستخدم يكتب «النور» لا «مسجد النور». البادئة المثبّتة لا تلتقطه،
فيسقط البحث إلى `contains` الذي يولّد `$regex` غير مثبّت — مسحٌ كامل على 18 ألف
سجل في كل ضغطة زر.

**العلاج:** حقل `nameTokens` (مصفوفة) يُحسب عند الاستيراد من الاسم المطبَّع
والقرية، مع فهرس `name_tokens`. البحث صار ثلاث مراحل: الكلمات أولاً، ثم البادئة،
ثم `contains` كخطة أخيرة لجزء من داخل كلمة.

**ما كشفه الخادم الحقيقي:** الصياغة الأولى كانت `equalTo('nameTokens', word)`
مكرّرة لكل كلمة — وهي الصياغة الصحيحة على MongoDB (مطابقة multikey أصيلة). على
محوّل PostgreSQL ترمي `invalid input syntax for type json` فتسقط الدالة كلها
بخطأ 500. لم يظهر ذلك في الاختبار لأن بديل Parse كان يطابق المصفوفات في
`equalTo` — أي أن البديل كان **يخفي** عدم المحمولية لا يكشفها.

الصياغة المعتمدة `containsAll('nameTokens', words)`: تعبّر عن الشرط نفسه (AND على
الكلمات) في استعلام واحد، تُترجم إلى `$all` على MongoDB فتخدمها الفهرسة ذاتها،
وتعمل على المحوّلين. وبديل Parse صار **يرمي** على `equalTo` فوق حقل مصفوفة، حتى
لا يمرّ نمطٌ غير محمول في الاختبار ثم يسقط على خادم حقيقي.

**الدرس:** البديل الذي يقبل أكثر من الخادم أسوأ من البديل الذي يقبل أقل. حين
يختلف محوّلان في دعم صيغة، فليرفضها البديل لا فليقبلها.

---

## 🟠 جولة تاسعة — التكليف طريقٌ بلا رجعة

كان البند الأخير في «للأسئلة المفتوحة»: لا منطق لمنع سوء الاستخدام المتكرر.
ثلاث فجوات متّصلة:

**١. لا رجعة من `assigned`.** منفّذٌ لا يحضر يترك الطلب معلّقاً بلا أجل. ليس
أمام الإمام إلا `cancelServiceRequest` — فيسقط سجلّ الطلب والحاجة قائمة، ثم
يُنشئ طلباً جديداً يبدأ من الصفر بلا أثر لما جرى. والمنفّذ لا يُقيَّد عليه شيء
فيكرّرها على مسجد آخر.

`releaseAssignment` تُعيد الطلب إلى ما كان (`funded` إن كان بتكلفة،
`open_for_volunteers` إن كان تطوّعاً عينياً)، وتُفرغ التكليف، وتُقيّد الغياب في
`abandonedJobs`. القيد يُعرض للإمام في قائمة المهتمّين ولا يمنع الاختيار: الغياب
مرّةً له أسبابه، والمنع الآلي يُقصي متطوّعاً بلا مراجعة.

**تُفتح للطرفين قصداً.** المنفّذ يسحب عن نفسه بلا أن يُقيَّد عليه. إتاحة
الانسحاب المُعلن وجعله بلا كلفة هو خير ما يُقلّل التغيّب — إغلاقه لا يجعل
المتخلّف يحضر، بل يجعله يصمت. وبعد `startWork` لا سحب: للمنفّذ حينها جهدٌ مبذول
وحقٌّ في المعاينة، وهو قيد `cancelServiceRequest` نفسه.

والمسحوب منه لا يُعيد التسجيل في الطلب نفسه (اهتمامه يُوسم `released`)، وإلا
دارت الحلقة: تكليف ثم غياب ثم تسجيل من جديد والإمام يرى اسمه كأن شيئاً لم يكن.
اهتمامات الآخرين تبقى `closed` — الطلب عاد مفتوحاً فليسجّلوا إن شاؤوا، ولا
يُحيا اهتمامٌ قد يكون صاحبه انصرف عنه.

**٢. لا حدّ للاهتمامات.** الاهتمام لا يحجز الطلب، فمتطوّع واحد كان يسجّله في كل
طلب مفتوح فيتصدّر قوائم الأئمة جميعاً وهو لا ينوي تنفيذ إلا واحد. الحدّ عشرة
اهتمامات مفتوحة، والسحب يُفرّغ مكاناً.

**٣. لا حدّ للتكليفات المتزامنة.** وهذا أخطر لأن التكليف يحجز فعلاً: عشرة مساجد
تظنّ أن لها منفّذاً والمنفّذ واحد لا يسع إلا مسجداً. الحدّ ثلاثة أعمال في
`assigned` أو `in_progress`؛ المنجَز والملغى لا يُحسبان.

الأرقام (10 و3) تقديرية لا محسوبة — تُراجَع بعد أول موسم تشغيل حقيقي.

**ما كشفه الخادم الحقيقي:** لا شيء في الكود — لكن سكربت التحقّق كان يمرّر
حالتين زائفتين. `user.fetch({ useMasterKey: true })` على كائن الجلسة **يمسح
رمزها منه**، فصارت الاستدعاءات التالية تفشل بـ«يجب تسجيل الدخول أولاً». ومساعد
`fails()` كان يقبل أي خطأ، فعدّ انقطاع الجلسة نجاحاً للقيد المقصود. القاعدة:
اختبارُ رفضٍ لا يتحقّق من سبب الرفض ليس اختباراً.

---

## 🔴 جولة عاشرة — ستة عشر فهرساً معلَناً وصفرٌ مطبَّق

**العَرَض:** لا شيء. كل شيء يعمل — ببطء لا يظهر على 400 سجل ويظهر على 18 ألفاً.

`schema.json` يعلن ستة عشر فهرساً، و`apply_schema.js` **يتجاهل كتلة `indexes`
كلّها**: يطبّق الحقول والصلاحيات وحدها، ثم يطبع اعتذاراً صريحاً بأن «فهارس
2dsphere والفهارس المركّبة تُضاف من لوحة Back4app — Parse SDK لا يديرها».

الاعتذار غير صحيح. `Parse.Schema.addIndex` موجودة في SDK المثبَّت (5.3.0)،
وتعمل على محوّلي MongoDB وPostgreSQL معاً. فكان كل استعلامٍ ضُبط في المراجعات
السابقة «ليستفيد من الفهرس» يمسح المجموعة في الواقع: `name_tokens`
و`name_search` و`geo_box` و`open_feed` و`ledger` و`trail` — كلّها حبرٌ على ورق.

هذا أخطر من أي خطأ منطقي سبقه لأنه **لا يُنتج سلوكاً خاطئاً**: الاختبارات تمرّ،
والواجهة تعمل، والنتائج صحيحة. لا يظهر إلا حين تكبر البيانات، أي بعد الإطلاق.

**العلاج:** `apply_schema.js` يطبّق الفهارس فعلاً، والتخطيط انتُزع إلى
`scripts/lib/index-plan.js` ليكون تحت الاختبار — الفهرس هو الفرق بين استعلامٍ
يقرأ سطراً وآخر يمسح ثمانية عشر ألفاً، فلا يجوز أن يبقى تطبيقه بلا تغطية.

- **المكانيّ يُفرز ويُطبَّق وحده.** `2dsphere` يفشل حيث لا امتداد مكاني، وكان
  فشله سيُسقط بقية فهارس الفئة معه لأن `schema.update()` تُرسل الكلّ دفعةً.
- **ويُميَّز بقيمته النصّية لا باسمه:** `geo_box` اسمه جغرافيّ وهو فهرس عاديّ
  على `lat`/`lng`، وعليه يقوم القرب حيث لا PostGIS. عدُّه مكانياً يُسقط ما يعمل.
- **عند الفشل تُعاد المحاولة واحداً واحداً** ليُعرف الفاسد ويمرّ السليم.

**التحقّق على خادم حقيقي:** 14 من 16 طُبّقت، والاثنان الباقيان مكانيّان يفشلان
على PostgreSQL كما هو متوقَّع. وتأكّدت من وجودها في `pg_indexes` لا من رسالة
نجاح واجهة Parse — الفرق بين «قال إنه فعل» و«فُعِل».

## 🟠 التكرار الصامت في المعرّفات الخارجية

`existingIds()` كانت تبني خريطة `externalId → objectId` بـ`found.set`، فإن
تكرّر معرّفٌ احتفظت بآخر نسخة وأسقطت ما قبلها بلا أثر. النسخة المهجورة لا
تُحدَّث أبداً، وتبقى تظهر في البحث وتُطلَب ملكيتها بينما الطلبات تُنشأ على
أختها — مسجدٌ واحد بسجلّين وإمامين محتملين.

وفهرس `externalId` فهرس بحث لا قيد تفرّد (Parse لا يعبّر عن التفرّد في مخططه)،
فلا شيء في القاعدة يمنع ذلك. صار الرصد في `existingIds` نفسها — نمرّ على كل سجل
أصلاً فلا يكلّف شيئاً — والاستيراد يقف بدل أن يكتب فوق قاعدة مكرّرة فيُرسّخها.
و`--verify` يفحص بلا كتابة.

وأُعيدت تسمية الفهرس إلى `externalId_lookup`: الاسم القديم كان يَعِد بتفرّدٍ لا
يفرضه، ويُطمئن قارئه إلى حماية غير موجودة. اختبارٌ يمنع عودة أي اسم يحوي
«unique» إلى المخطط.

---

## 🔴 جولة حادية عشرة — كل إشعار كان يذهب إلى لا أحد

**العَرَض:** لا شيء، مرّةً أخرى. الدوال تُرجع `{ sent: 3 }` والسجلّ نظيف.

`Parse.Push.send` يستعلم على `_Installation` لا على `_User`، فلا يصل إلا لمن
سُجّل لجهازه Installation ورُبط بحسابه. **تطبيق الويب لا يسجّله**، والقيد مكتوب
في `CLAUDE.md` منذ البداية بوصفه شرط تشغيل — لكن الشرط لم يتحقّق قطّ ولم يمنع
أحداً من كتابة عشرة مواضع تعتمد عليه.

فكان المتطوّع يُكلَّف ولا يعلم، والإمام يُسجَّل اهتمامٌ بطلبه ولا يعلم، ويُسحب
التكليف ولا يعلم أحد. والأسوأ أن `pushToUsers` كانت تُرجع `{ sent: list.length }`
— **تُبلّغ بنجاحٍ لم يقع**، فلا تظهر المشكلة حتى في سجلّ الخادم.

**العلاج:** فئة `Notifications` — صندوق وارد دائم. لكل إشعار موجَّه سطرٌ يقرأه
صاحبه حين يفتح التطبيق، والدفع تحسينٌ فوقه لا شرطٌ له. وشاشة «التنبيهات» في
كل دور بشارة غير المقروء.

- **الموجَّه يُحفظ والبثّ لا.** خمسمائة سطر عند كل طلب جديد تُنهك باقة الطلبات،
  والفرصة القريبة لها قناتها أصلاً: `getNearbyOpportunities` يراها المتطوّع متى
  فتح التطبيق. الحفظ لما لا بديل له — التكليف والسحب والاهتمام والإنجاز.
- **`stored` هو الضمان لا `pushed`.** الأخيرة عدد من استُهدف لا من وصله، ولا
  سبيل لمعرفة الثاني من الخادم. الاسم يقول ذلك الآن بدل `sent` المُوهِمة.
- **الحفظ أثر جانبي كالتدقيق** — لا يرمي. لا يفشل تكليفُ منفّذ لأن سطر إشعار
  لم يُكتب.
- **الصندوق مقفل على Master Key** كسجل التدقيق: القراءة عبر `getMyNotifications`
  وحدها فلا يصل أحدٌ إلى وارد غيره ولو خمّن معرّفه. و`markNotificationsRead`
  تقيّد الاستعلام بالمستخدم **حتى مع تمرير `ids`** — لولا ذلك لأخفى أحدهم عن
  غيره إشعاراً لم يره.
- **التقليم:** `pruneNotifications` بتسعين يوماً. الصندوق ينمو أسرع من سجل
  التدقيق (سطرٌ لكل مستهدَف لا لكل حدث)، والإشعار خبرٌ عاجل — والأثر الدائم في
  `AuditLog` لا هنا.

**التحقّق:** تسع خطوات على خادم حقيقي **بصفر Installation مسجَّل** — وهو بيت
القصيد: كل ما وصل، وصل بلا دفع. ومنها محاولة قراءة `Notifications` مباشرةً
(رُفضت) ومحاولة تعليم وارد الغير مقروءاً (عُلّم صفر). والرحلة كاملة في متصفّح.

**الدرس:** قيدٌ مكتوب في التوثيق ليس حمايةً. «الإشعارات تحتاج تسجيل Installation
وإلا لن يصل أي إشعار» كانت مكتوبة بالحرف، وبُني فوقها عشرة مواضع.

---

## 🟠 جولة ثانية عشرة — التحقّق الذي لا يبقى ليس تحقّقاً

كل ما بُني في الجولات الأخيرة تُحقّق منه على خادم حقيقي — لكن بسكربتات في مجلّد
مؤقّت خارج المستودع. سبع وثلاثون خطوة تختفي مع الحاوية، بينما التغطية المحفوظة
في المستودع سبع حالات على مسارٍ واحد.

هذا ليس نقصاً في التغطية بل **وهمٌ بها**: من يقرأ `npm run test:integration`
يظنّ أن البحث والحدود والوارد محميّة، وهي ليست كذلك في أي جهازٍ غير هذا.

نُقلت إلى `tests/integration/`: `search.test.js` و`limits.test.js`
و`inbox.test.js` — فصارت 34 حالة بدل 7، تعمل في 7 ثوانٍ.

**واختبار البحث يستحقّ وجوده هناك:** أعدتُ صياغة `equalTo` المتكرّرة فسقطت خمس
حالات منه. هذه بالضبط ما لا يستطيع البديل في الذاكرة رصده مهما كثرت حالاته —
الفرق في المحوّل لا في المنطق.

**وانحرافٌ كُشف في أثناء النقل:** `harness.applySchema` كانت نسخةً مستقلّة من
`apply_schema.js`، فلمّا أُصلح السكربت ليطبّق الفهارس بقيت هي على حالها. أي أن
اختبار التكامل كان — بعد الإصلاح مباشرةً — يشهد لبيئةٍ بلا فهارس ليست هي التي
تُنشر. صارت تستدعي `index-plan` نفسه، وتُعيد عدد ما طبّقت، و`flow.test.js`
يتحقّق منه. وكذلك `tokenize` انتُزعت إلى `scripts/lib/` وتشترك فيها الاثنتان:
لولا ذلك لتحقّق اختبار البحث من كلماتٍ ليست هي التي تُستورَد فعلاً.

**والبيانات حقيقية لا مخترَعة.** `seedMosques` يستورد من `data/mosques.json`:
أسماء المساجد العُمانية لها بنيتها («مصلى التدريب بعيدم»)، واختبارُ بحثٍ على
«مسجد ١» و«مسجد ٢» يشهد لنفسه ولا يشهد للبحث.

---

## 🟠 جولة ثالثة عشرة — ما كشفه أوّل متصفّح آليّ

أُضيف `npm run test:e2e`: يُشغّل الخادم الحقيقي نفسه (بمِرقاة التكامل)، يبني
الواجهة موجَّهةً إلى منفذه، يقدّمها من خادمٍ ساكن، ثم يقود Chromium خلال
الرحلة كاملة — التسجيل، الطلب، الاهتمام، التكليف، السحب، إعادة التكليف،
الإبلاغ، التنبيهات، الاعتماد، وانقطاع الاتصال.

**وكشف في أوّل تشغيلٍ ما لا يراه خادمٌ ولا اختبارُ وحدة:**

الشاشات تجلب بياناتها عند الظهور مرّة واحدة، **وضغط التبويب النشط لا يُعيد
الجلب**. فمن فتح التطبيق قبل أن يُنشر طلبٌ يبقى يرى «لا توجد فرص مفتوحة الآن»
إلى أن يُغلق التطبيق ويفتحه — ولا زرّ تحديث في أي شاشة.

لم يظهر هذا في تحقّقي اليدوي السابق لأن ترتيب الخطوات هناك كان يُسجّل المتطوّع
**بعد** نشر الطلب، فيجد القائمة محمّلةً من أوّل مرّة. الاختبار الآليّ سجّل
الثلاثة أوّلاً — كما يقع في الواقع — فبان العطل.

العلاج في `App.jsx`: مفتاحٌ متغيّر على الشاشة يزيد مع كل ضغطة تبويب، فتُعاد
تركيبها وتجلب من جديد. وهو سلوك التطبيقات المعتاد: ضغطُ التبويب الذي أنت فيه
يُحدّثه.

**وما لا يُعدّ عطلاً:** أوّل تقرير أظهر «جارٍ تحديد موقعك…» عالقةً، فظننتها
انتظاراً بلا مهلة. `currentPosition` لها مهلة عشر ثوانٍ أصلاً، والمصادفة أن
مهلة المتصفّح في الاختبار كانت عشراً كذلك فالتُقطت الشاشة في اللحظة نفسها.
رُفعت مهلة الاختبار فوقها حتى لا تصف اللقطةُ انتظاراً لم ينتهِ بعد.

**التشخيص نفسه احتاج علاجاً:** «Timeout waiting for button» لا تقول شيئاً —
أهي شاشة خطأ، أم قائمة فارغة، أم زرٌّ باسمٍ آخر؟ صار كل عطلٍ يُلحق بنصّه ما
تعرضه الشاشة فعلاً، فبان السبب من أوّل قراءة.

---

## 🟠 جولة خامسة عشرة — ما كشفته البيانات كاملةً

شُغّلت البيانات كاملةً لأوّل مرّة على خادمٍ حقيقي: **18,214 مسجداً** بدل 400.

**الأداء لم يكن المشكلة.** القياس على القاعدة الكاملة:

| العملية | الزمن |
|---|---|
| بحث بالكلمات («النور»، «السلطان قابوس») | 69–169 مللي |
| بحث مصفّى بالمحافظة | 24 مللي |
| أسوأ حالة: جزء من داخل كلمة (`contains` يمسح) | 180 مللي |
| القرب الجغرافي (1–50 كم) | 4–34 مللي |

والقاعدة 22 ميغابايت (14 للمساجد، 6 منها فهارس) — أي أقلّ من عُشر حدّ الباقة
المجانية. الفهارس التي أُضيفت في «الجولة العاشرة» تفعل عملها.

**المشكلة كانت في مكانٍ آخر تماماً: تشابه الأسماء.**

اختبرتُ ما يهمّ فعلاً — هل يجد الإمام مسجده؟ — بعيّنة من 110 مساجد موزّعة على
المحافظات الإحدى عشرة. **103 وُجدت، و7 لم تُوجد**، وكلّها بأسماء من نمط واحد:
«مصلى العيدين»، «العيدين»، «مصلى العيد».

والسبب ليس خللاً في البحث: **«مصلى العيدين» اسمٌ لـ369 مسجداً**، و«العيدين»
لـ161. فالبحث يُرجع المئة الأولى ومسجدُ الإمام ليس بينها. وحتى لو كان، فالبطاقات
متطابقة الظاهر ولا يعرف أيّها مسجده.

وهذا **لا يظهر في تجربة محافظةٍ واحدة** — بها تصير النتائج عشرين لا مئة.

**والأدوات كانت موجودة كلّها ولا يصلها المستخدم:**

- `searchMosques` تقبل `governorate` و`wilayat` — والواجهة لا تمرّر أيّهما.
- القرية ضمن `nameTokens` منذ «الجولة الثامنة»، فـ«العيدين المنجرد» يُرجع نتيجةً
  **واحدة** — ولا شيء في الشاشة يدلّ على ذلك.
- القرية في `PUBLIC_FIELDS` وتصل إلى العميل — والبطاقة تعرض المحافظة والولاية
  وتُسقطها، وهي أدقّ ما يميّز مسجدين في الولاية نفسها.

**العلاج ثلاثةٌ في الواجهة لا في الخادم:** تصفيةٌ بالمحافظة، وعرض القرية في
البطاقة، وإرشادٌ صريح إلى إضافة اسم القرية — مع تنبيهٍ حين تبلغ النتائج السقف
بأنها أكثر من مئة فلتُضيَّق.

**الدرس:** البحث كان «يعمل» بكل مقياس تقني — سريعاً، مفهرساً، صحيح النتائج —
وكان يفشل في وظيفته. الفرق بين الاثنين لا يظهر إلا على البيانات كاملةً.

### وتشابهُ الأسماء لا يخصّ شاشة البحث وحدها

عالجتُ أوّلاً شاشة تسجيل المسجد، ثم تبيّن أن العلّة نفسها في كل موضعٍ يُعرض
فيه اسم مسجد — والأسوأ فيها **قائمة مهامّ المنفّذ: اسمُ المسجد وحده بلا موضع**.
والمنفّذ يقصد المسجد بجسده لا بعينه، فاسمٌ يشترك فيه 369 مسجداً يعني رحلةً إلى
مسجدٍ آخر.

فُحصت المواضع كلّها: الفرص المفتوحة (ولاية بلا قرية)، ومساجد الإمام (محافظة بلا
قرية)، وطلبات الملكية عنده وعند المشرف (ولاية وحدها) — والمشرف يعتمد ملكية
مسجدٍ بعينه وثلاثمئة غيره تحمل الاسم نفسه.

وصار الموضع يُعرض من مكوّنٍ واحد (`Where`) لا من خمس صياغات تنحرف عن بعضها،
ومعه **رابط الطريق في قائمة المهامّ**: الإحداثيات كانت في القاعدة ولا تصل إلى
المنفّذ. أُضيفت القرية إلى `getNearbyOpportunities` و`getMyClaims`
و`listPendingClaims` وإلى قراءة الطلبات في العميل.

---

## 🟢 بيئة الاختبار تُمثّل مستخدماً يشارك موقعه

المنصّة صارت تقوم على الموقع: القرب يرتّب البحث والفرص، وتسجيل المسجد يشترط
تأكيد الموقع عنده. ومتصفّح الاختبار كان بلا إذن موقعٍ إطلاقاً — أي أنه يُمثّل
**مستخدماً رافضاً** في كل حالة، وهي الحالة الأقلّ لا الغالبة.

فصار السياق يُمنح الإذن وموقعاً ثابتاً افتراضياً، ويُترك الرفض حالةً تُطلب
صراحةً (`newUserPage(null)`). وبه صار يمكن اختبار ما لم يكن يُختبر: أن البحث
يُرتّب بالأقرب فعلاً في الشاشة، وأن التسجيل يُقبل بتأكيد الموقع ويُحتسب «من عند
المسجد».

**الدرس:** بيئة الاختبار افتراضٌ عن المستخدم كأي افتراضٍ آخر. وحين تغيّر المنتجُ
ليقوم على قدرةٍ في الجهاز، بقيت البيئة على حالها تُمثّل من لا يملكها.

---

## 🟢 المسجد يتعلّم موقعه من طلب ملكيته

يتبع من تأكيد الموقع: حين يسجّل الإمام مسجداً **بلا إحداثيات** ونحن نحفظ موقعه
لحظة التقديم — **فقد عرفنا أين المسجد**.

فصار `reviewMosqueClaim` يتبنّى إحداثيات الطلب عند الاعتماد، ويضبط `location`
و`hasLocation` معها، ويقيّد `location_learned` في سجلّ المسجد. فيخرج المسجد من
عزلته: يظهر في البحث بالقرب، وتخرج فرصه من ذيل القائمة، ويعرف المتطوّع طريقه
إليه. **البيانات تُصلَح بالاستعمال** لا بحملة تصحيحٍ منفصلة.

**وثلاثة قيود:**

- **لا تُمسّ إحداثياتٌ موجودة أبداً.** بيانات الوزارة مرجع، وموقع الجهاز تقديرٌ
  بدقّته: يملأ فراغاً ولا ينسخ فوق مرجع.
- **المصدر يُقال** (`locationSource: 'claim'`) — من يقرأ الحقل لاحقاً يعرف أنه
  تقديرٌ لا بيانات وزارة.
- **الرفض لا يمنح شيئاً.** الاعتماد وحده يُثبت، فطلبٌ رفضه المشرف لا يغيّر بيانات
  مسجد.

### وحارسٌ بدل درسٍ مكتوب

وقعتُ **للمرّة الثالثة** في وضع خادمَين في ملفٍ واحد — و`directAccess` يربط
Parse بأوّلهما، فيخاطب الثاني قاعدة الأوّل بعد إغلاقها، وتظهر
`relation "_User" does not exist` وهي رسالةٌ لا تدلّ على سببها إطلاقاً.

وكنتُ كتبتُ درس ذلك **مرّتين** في رؤوس الملفات. لم ينفع: الدرس المكتوب لا يُقرأ
إن لم يُبحث عنه. فصار `startStack` يرفض النداء الثاني برسالةٍ تسمّي السبب وتقول
العلاج. **ما يتكرّر ثلاثاً لا يُعالَج بتعليقٍ رابع.**

---

## 🟢 تأكيد الموقع عند التسجيل — وصفة مقدّم الطلب

**السؤال المفتوح منذ أوّل يوم:** كيف يُثبت الإمام أنه إمام هذا المسجد؟ لا جواب
تامّ دون تكامل مع الوزارة — **لكن من يدّعي مسجداً يُتوقّع أن يكون فيه**.

فصار `claimMosque` يطلب موقع مقدّم الطلب، ويحسب مسافته من المسجد، ويحفظها،
ويعرضها للمشرف بوسمٍ صريح: «قُدّم من عند المسجد» أو «قُدّم من ٨٠٠ كم». ليس
دليلاً قاطعاً، لكنه **أقوى ما بيد المشرف** قبل التكامل الرسمي — وقد كان لا
يملك شيئاً سوى اسمٍ ورقم هاتف.

**ثلاثة قرارات في التشدّد:**

- **الموقع شرطٌ حين للمسجد إحداثيات**، لا اختياريّ. «إعادة تأكيد الموقع» تعني
  أن يكون عنده، وطلبٌ بلا موقع يُرفض برسالةٍ تقول ما يفعله.
- **والبعيد لا يُرفض آلياً.** قد يسجّل الإمام مساءً من بيته. الرفض الآلي يُقصي
  محقّاً بلا مراجعة — فالقرار للمشرف، والمسافة تصله ليقرّر بها.
- **ومسجدٌ بلا إحداثيات لا يُطلب له موقع** — ستة عشر منها في البيانات، ولا
  مسافة تُقاس إليها. حرمانُ أهلها من التسجيل عقوبةٌ على خطأٍ في بيانات الوزارة.

### وكيل المسجد

الوكيل يتولّى شؤون المسجد كالإمام في عرف كثير من المساجد. وحرمانه من التسجيل
يُعطّل مساجد، **وإجبارُه أن يسمّي نفسه إماماً كذبٌ يُدخل على المشرف** — وهو
الذي يُفترض به أن يتحقّق.

فصارت **الصفة** حقلاً في الطلب (`imam` / `agent`) تُذكر ويراها المشرف، والصلاحيات
واحدة. ولم يُضَف دورٌ ثالث: ثمانية مواضع تشترط دور الإمام، وإضافة دورٍ موازٍ
تُضاعف سطح الصلاحيات لأجل تسميةٍ — والتسمية تُقال في الطلب بلا ذلك.

**وخطأٌ في طريقة عملي كشفه الفحص:** أضفتُ `capacity` إلى قارئَي الطلبات
باستبدالٍ نصّي، فمرّ أحدهما بلا تغيير — `getMyClaims` يُرتّب حقوله ترتيباً آخر،
فلم يُطابق النمط و**لم يُبلّغ الاستبدال بشيء**. صار كل استبدالٍ في هذه الجلسة
مسبوقاً بتأكيدٍ أن موضعه موجود.

---

## 🟢 الموقع هو ما يميّز — لا رقمٌ في سجلّ

عالجتُ تشابه الأسماء برقم الوزارة، وهو صحيح لكنه ليس أوّل ما يُسأل عنه الإمام:
لن يخرج أوراق مسجده ليجده في تطبيق. **وهو واقفٌ في مسجده أو قريبٌ منه.**

فصار `searchMosques` يقبل موقع الباحث — اختيارياً — ويُرتّب بالأقرب. فمن بين
واحدٍ وعشرين «مسجد الغبي» في عبري، الذي على بُعد خمسين متراً هو مسجده بلا شكّ،
ورقم الوزارة يبقى للتثبّت لا للتمييز.

وهذا هو المبدأ الذي بُنيت عليه المنصّة من أوّلها: **موقع المسجد هو ما يربط
المصلّين بمسجدهم**. كان مطبَّقاً في «حولي» وفي فرص التطوّع، وغائباً عن الشاشة
التي يختار فيها الإمام مسجده — وهي أوّل ما يفعله، وأخطر خطوة في المسار كلّه:
اختيارٌ خاطئ يُقيّد طلباته على سجلٍّ ليس سجلّ مسجده.

**والقاعدة نفسها المتّبعة في الفرص:** الأقرب أوّلاً، **ومجهول الموقع آخراً
موسوماً لا محذوفاً** — فالمسجد بلا إحداثيات يبقى في النتيجة ويبقى قابلاً
للتسجيل. والموقع اختياري: من رفض مشاركته يرى الترتيب الطبيعي كما كان، ويبقى
له اسم القرية والتصفية بالمحافظة ورقم الوزارة.

**وحالتان تُختبران صراحةً لأنهما تُنتجان أخطاءً صامتة:** إحداثياتٌ فاسدة (نصّ
مكان الرقم) تُعامَل كغيابها لا كصفرٍ عند خط الاستواء يجعل كل المساجد على بُعد
آلاف الكيلومترات؛ ومسجدٌ بلا إحداثيات لا يُحذف من النتيجة.

---

## 🟠 ألفٌ وأربعة وثمانون مسجداً لا يميّزها شيءٌ ممّا نعرضه

اختبرتُ كفاية إصلاحي السابق — عرضُ القرية لتمييز متشابهي الاسم — فتبيّن أنه
**غير كافٍ لستّة في المئة من البيانات**: 417 مجموعة تتطابق في الاسم والولاية
والقرية معاً، تشمل 1,084 مسجداً. «مسجد الغبي» في عبري بقرية الغبي: **واحدٌ
وعشرون مسجداً** بالاسم نفسه والولاية نفسها والقرية نفسها.

فكيف يختار الإمام مسجده من إحدى وعشرين بطاقة متطابقة؟ لا يستطيع. يختار واحدةً
عشوائياً، فتُقيَّد طلباته على سجلٍّ ليس سجلّ مسجده، ويأتي إمامٌ آخر فيختار
أخرى عشوائياً كذلك. والمشرف الذي يعتمد الملكية لا يملك ما يتحقّق به أصلاً.

**والوزارة حلّت هذا قبلنا.** لكل مسجد رقمٌ في سجلّها (`27/1109`)، وهو موجود في
البيانات، **ويصل إلى العميل ضمن `PUBLIC_FIELDS` منذ البداية** — ولا يُعرض في
شاشة واحدة. وفحصتُ: **الرقم يميّز المجموعات الـ417 كلّها بلا استثناء**، ولا
مسجد فيها بلا رقم.

فصار يُعرض حيث يجب أن يُختار مسجدٌ بعينه: شاشة تسجيل المسجد، ومراجعة المشرف،
ومساجد الإمام وطلباته. وهو ما بيد الإمام في أوراق مسجده فيُطابقه.

**الدرس:** إصلاحٌ يُحسَب كافياً حتى يُقاس. عرضُ القرية عالج 94% وبدا حلّاً
تامّاً — ولولا أنني سألت «وكم بقي؟» لبقيت 1,084 حالةً بلا حلّ خلف إصلاحٍ يبدو
مكتملاً.

### وقوائم المحافظات صارت محروسة

كتبتُ قائمة المحافظات بيدي في موضعين — التحقّق في السحابة والاختيار في الواجهة —
والبيانات مصدرها الوزارة. فحصتُها فطابقت، لكن لا شيء كان يُبقيها مطابقة. وحرفٌ
واحد يعني محافظةً يرفضها الخادم أو لا يجدها المستخدم، **ويعني خطةَ الإشعار
البديلة تُطابق صفراً** لأنها تقارن نصّاً بنصّ. صار اختبارٌ يقارن الموضعين
بالبيانات: جرّبته بمسافةٍ زائدة واحدة فسقط.

---

## 🟠 ستة عشر مسجداً خارج المنصّة بلا أن يعلم أحد

`data/cleaning_report.json` يقول: ثلاثة عشر صفّاً بإحداثيات خارج حدود السلطنة
(أحدها في الهند)، وثلاثة بلا إحداثيات. ستة عشر مسجداً بلا موقع صالح — سُجّلت
في `DATA.md` بوصفها مشكلة بيانات، ولم يُسأل: **وماذا يقع لأهلها في التطبيق؟**

الجواب: صندوق الإحاطة لا يبلغها أبداً، فطلباتها **لا تصل متطوّعاً شارك موقعه**.
وتصل من رفض المشاركة وحده — لأن الواجهة تسقط حينها إلى `openOpportunities` التي
تُدرج الجميع. أي أن الفرصة تظهر لمن يبحث أقلّ ويختفي عمّن يبحث أكثر، وهي مفارقةٌ
صامتة لا يُبلَّغ بها أحد.

وأشدّ منها: `if (near.length === 0) return []` — متطوّعٌ لا مسجد **مُحدَّد
الموقع** حوله كان يُرجع له فراغٌ تامّ، وفيه فرصةٌ متاحة.

**العلاج — بقاعدةٍ لا باستثناء:** القائمة تُرتَّب بالقرب، **وما لا يُعرف قربه
يأتي آخراً موسوماً لا محذوفاً**. تُلحق مساجد `hasLocation: false` ذات الطلبات
المفتوحة (وهي ستة عشر بحدّها الأعلى) بمسافةٍ `null`، وتُفرز آخر القائمة،
وتُوسَم في الواجهة «موقعه غير مسجّل» — لا وسمُ حالةٍ عامّ يُفهَم منه أنها قريبة.
وموضعها (الولاية والقرية) يصل ولو غاب موقعها، وإلا لم يعرف المتطوّع أين يذهب.

**الدرس:** مشكلةُ بياناتٍ موثّقة ليست مُعالَجة. أن تُحصى وتُكتب في تقرير شيء،
وأن يُسأل «ماذا يقع لمن خلفها؟» شيءٌ آخر.

### واختبارات التكامل تُشغَّل على التوالي

الملفّ السادس أسقط اختبارَي بحثٍ صحيحين. كلٌّ منهما يمرّ وحده، ويسقط في
المجموعة: `node --test` يُشغّل الملفات متوازيةً، **وكل ملفٍ يُقلع عنقود
PostgreSQL كاملاً وخادماً**. ستة معاً تُنهك الجهاز.

`--test-concurrency=1`. أربعٌ وعشرون ثانية للملفّات كلّها، وهو ثمنٌ أرخص من
تشخيص فشلٍ بيئيٍّ يبدو خطأً في الكود.

**وخطأٌ كرّرته:** وضعتُ الاختبار الجديد أوّلاً في ملفٍّ فيه خادمٌ آخر، فسقط بـ
`relation "_User" does not exist` — وهو عين ما وقعت فيه مع `contractor.test.js`
**وكتبتُ درسه في رأس ذلك الملف**. الدرس المكتوب لا يُقرأ إن لم يُبحث عنه؛
الحارس وحده يمنع التكرار.

---

## 🔴 مهامٌ دوريّة بُنيت ولا يجدولها أحد

فحصٌ منهجي رابع: المواصفات الأصلية مقابل ما بُني. خمس دوال زادت عليها
(`releaseAssignment`، `getMyInterests`، `updateMyProfile`، `getMyNotifications`،
`markNotificationsRead`) — وهذا انحرافُ توثيقٍ يُصحَّح.

**لكن الأخطر مهمّةٌ دورية:** `pruneNotifications` بُنيت مع صندوق الوارد ولم
تدخل جدول المهام في المواصفات. **و`docs/DEPLOY.md` لم يكن يذكر جدولة المهام
إطلاقاً** — لا هذه ولا `pruneAuditLog` التي سبقتها.

فالنتيجة على خادمٍ حقيقي: المُشغّل ينشر، ولا يجدول شيئاً لأن لا شيء يطلب منه
ذلك، فينمو سجل التدقيق بلا حدّ **وينمو صندوق الوارد أسرع منه** (سطرٌ لكل مستهدَف
لا لكل حدث) حتى تمتلئ قاعدةٌ حدّها 250 ميغابايت — ولا يظهر السبب إلا بالبحث.

المهامّ كُتبت واختُبرت وقُلّمت بحدٍّ أدنى للاحتفاظ… ولا تعمل أبداً لأن أحداً لم
يُخبر المُشغّل بتشغيلها. **كودٌ صحيح لا يُستدعى ليس حمايةً.**

**العلاج:** قسمٌ مرقّم في `DEPLOY.md` يجدولها ويقول ماذا يقع إن أُهملت، وجدول
المواصفات يُحدَّث — **واختبارٌ يقارن جدولَي المواصفات (الدوال والمهام) بالمُسجَّل
فعلاً**، فينحرف الاثنان معاً أو لا ينحرف أيّهما. جرّبته بحذف السطر: يسقط ويسمّي
السبب.

**الدرس:** التوثيق التشغيلي جزءٌ من المنتج لا وصفٌ له. وما يعتمد على قراءة
إنسانٍ له يحتاج حارساً كأي كود.

---

## 🟢 دعوى «يعمل بلا إنترنت» — تُثبت أخيراً

التوثيق يقولها منذ البداية، و`npm run verify:pwa` يفحص وجود الـmanifest وعامل
الخدمة وسلامتهما — **وهو فحص إعدادٍ لا سلوك**. لا يُثبت أن الصفحة تُفتح بلا
شبكة، والفرق بينهما هو الفرق بين تطبيقٍ مثبَّتٍ يعمل وشاشةِ خطأٍ من المتصفّح.

صار في المتصفّح اختبارٌ للدعوى نفسها: يُحمّل التطبيق، **ينتظر سيطرة عامل الخدمة
على الصفحة**، يقطع الشبكة، **ثم يُعيد التحميل** — ويتحقّق أن هيكل التطبيق يُقدَّم
من التخزين المسبق وأن شريط انقطاع الاتصال يظهر. الدعوى صحيحة، ومثبتة الآن.

### واختبارٌ متذبذب استقرّ

حالة التنبيهات كانت تسقط أحياناً وتمرّ أحياناً. السبب أن الشاشة تمرّ بنافذة
«جارٍ التحميل…» بين حالتين، و`waitForSelector('.card')` يُطابق **بقيّة العرض
السابق** فينجح قبل أن تُحدَّث النتيجة، ثم يجد الفحص التالي القائمة خاليةً.

عولج بانتظار شرطٍ **لا يصدق إلا بعد الاستقرار**: بطاقاتٌ حاضرة ولا واحدة منها
غير مقروءة. وهو أفضل من سابقه لأنه يختبر أثر التعليم نفسه لا مجرّد نجاة القائمة.

**الدرس:** انتظارُ عنصرٍ لا يُميّز بين «لم يتغيّر بعد» و«تغيّر وعاد» — فانتظر
شرطاً لا يصدق إلا في الحالة المقصودة.

---

## 🟠 حقلان يُقرآن ولا يُكتبان قطّ

فحصٌ منهجي ثالث: كل حقلٍ في المخطط، هل يُكتب؟ وهل يُقرأ؟ أكثر ما ظهر ضجيجٌ من
تعبيرٍ نمطيّ خشن (حقول تُقرأ عبر `equalTo` أو `select` أو `increment`)، واثنان
كانا حقيقيّين — وكلاهما من الشكل نفسه الذي تكرّر في هذا المستودع: **الخادم
يقرأ ما لا يكتبه أحد.**

**`_User.skills`.** `getRequestInterests` تُعيدها، والواجهة تعرض «مهارات: …»،
والغرض أن يختار الإمام من يناسب العمل — كهرباء لطلبٍ كهربائي. **ولا شيء يجمعها
قطّ**: لا حقل في التسجيل ولا في الحساب. فكل متطوّع يظهر أبداً بـ«مهارات: غير
محدّدة»، والإمام يختار بلا بيّنة.

**`_User.governorate`.** `pushToNearbyVolunteers` يجعلها **خطة بديلة** حين يفشل
الاستعلام الجغرافي أو يخلو: يُطابق المتطوّعين بمحافظة المسجد. ولمّا لم يكن أحد
يكتبها، كانت الخطة البديلة **تُطابق صفراً دائماً** — شبكةُ أمانٍ بلا خيوط،
تبدو في الكود حمايةً وهي لا شيء.

**العلاج:** `updateMyProfile` تكتب الاثنين (ومعهما الهاتف والولاية) لصاحب الحساب
وحده، ولا تقبل `role` ولا حقول الاعتماد. والمهارات **قائمة مغلقة** تُطابق فئات
الأعمال نفسها: نصٌّ حرّ يجعل «كهرباء» و«كهربائي» و«كهربا» ثلاثة أشياء لا يجمعها
بحث. وتُجمع عند التسجيل، **وتُعدَّل بعده** — فمن سجّل مسرعاً لا يبقى بلا مهارات
إلى الأبد، وهو الفخّ نفسه الذي وقعت فيه صفة الشركة.

**وخطآن لي كشفهما اختباراي:**

الأوّل في التحقّق: كنت أقارن طول القائمة قبل التنقية وبعدها لأرصد المهارة
المجهولة — فصار المكرَّر يُرفض برسالة «مهارة غير معروفة». التحقّق قبل طيّ
التكرار، والرسالة تُسمّي المجهولة.

والثاني أن إغلاق القائمة نقل المشكلة ولم يُنهها: صارت المهارة تُخزَّن بمفتاحٍ
إنجليزي، وبطاقة المهتمّين تعرضه كما هو — «مهارات: electrical» لقارئٍ عربي. وهو
عين ما عولج في `worker_assigned` بسجلّ المسجد وفي `volunteer` برسالة الأدوار.
**القاعدة تتكرّر: المفتاح للتخزين، والعربية للعرض — ولا يلتقيان.**

---

## 🟠 الشفافية غاية المنصّة، وسجلّها كان يُكتب ولا يُقرأ

أوّل سطرٍ في وصف المشروع: «منصّة تربط أئمة المساجد بالمتطوعين والشركات
والمتبرعين، لإدارة أعمال صيانة المساجد **بشفافية**».

وسجلّ التدقيق هو أداة تلك الشفافية: سطرٌ لكل تحوّل حالة وكل حركة مال، بفاعله
ووقته. كُتب في «الجولة الثالثة»، وقُلّم في «التاسعة»، وحُرس بالصلاحيات — **ولم
يُعرَض في أي شاشة قطّ**. `getMosqueAuditTrail` مُصدَّرة في `app/src/api.js`
ولا تستدعيها شاشة واحدة.

كُشف بفحصٍ منهجي لا بالتخمين: مقارنة دوال الخادم بما تستدعيه الواجهة، ثم
مقارنة ما تُصدّره طبقة `api` بما تستعمله الشاشات. الأولى لم تُظهر شيئاً (غير
المستدعى كلّه مسار التبرعات المعطّل قصداً)، والثانية أظهرت هذه.

**العلاج:** شاشة «سجلّ المسجد» — من «حولي» لأي مستخدم، ومن «مساجدي» للإمام.
وثلاثة قرارات فيها:

- **مفتوحة لكل مصادَق، لا للإمام وحده.** `getMosqueAuditTrail` كذلك من البداية،
  وهو الصواب: من صلّى في المسجد له أن يعرف ما جرى فيه. ولا امتياز للإمام في
  الشفافية — يرى ما يراه المصلّي.
- **بالعربية لا بأسماء الأفعال البرمجية.** الخادم يُعيد `worker_assigned`، وهي
  لا تعني شيئاً لقارئها.
- **بالصفة لا بالاسم.** الدالة تُعيد `actorRole` ولا تُعيد `actorId` قصداً —
  الشفافية أن يُعرف ما جرى، لا أن يُكشف الأشخاص. والاختبار يحرس ذلك: يرفض ظهور
  أسماء المنفّذين في السجلّ.

**الدرس:** ميزةٌ لا مدخل لها في الواجهة غير موجودة، مهما كانت مُتقنةً في
الخادم ومحروسةً بالاختبارات. والفحص المنهجي لسطح الاستدعاء يكشف ذلك في دقيقة.

---

## 🟠 صفتان تُعرضان عند التسجيل ولا تُوصلان إلى شيء

نموذج التسجيل يعرض أربع صفات، واثنتان منها تُفضيان إلى طريقٍ مسدود بلا كلمة
تفسير:

**الشركة.** تسجّل، فتفتح «مهامّي» فترى «لم يُسنَد إليك عمل بعد» — إلى الأبد.
لأن التكليف يشترط `isVerifiedContractor`، ولا شيء في التطبيق يقول ذلك. **والأدهى
أن الحلقة مقطوعة من طرفيها**: نموذج التسجيل لا يجمع اسم الشركة ولا سجلّها
التجاري أصلاً، فيرى المشرف في قائمة المراجعة «السجل التجاري: — غير مُدخَل»
ولا يملك ما يتحقّق منه. فلا الشركة تعرف لماذا تنتظر، ولا المشرف يستطيع أن
يُنهي انتظارها.

**المتبرّع.** يسجّل ليتبرّع، والتبرّع معطّل بقرارٍ تنظيمي — فيجد شاشةً تعرض
المساجد القريبة ولا شيء غير ذلك، بلا تفسير.

الحقول كانت موجودة كلّها في الخادم: `getMyProfile` يُعيد `companyName`
و`crNumber` و`isVerifiedContractor` منذ البداية، والعميل لا يعرض شيئاً منها.

**العلاج:**
- نموذج التسجيل يجمع اسم الشركة وسجلّها التجاري عند اختيار صفتها، ويقول إن
  الإدارة تراجعهما.
- «مهامّي» تشرح للشركة غير المعتمدة سبب خلوّها، بدل صمتٍ يُفسَّر عطلاً.
- «حسابي» يعرض حالة الاعتماد بوسمٍ صريح: «معتمدة» أو «بانتظار المراجعة».
- المتبرّع يُخبَر عند التسجيل أن التبرّع النقدي يحتاج تصريح الوزارة، ويُدلّ على
  صفة «متطوّع» إن أراد المشاركة الآن.

**الدرس:** لا تعرض للناس باباً لا يفتح. وإن كان لا بدّ من الانتظار، فقل لهم
لماذا ينتظرون وحتى متى.

---

## 🔴 لا مشرف على خادمٍ جديد — والمنصّة معطّلة بلا مشرف

ظهر عند سؤالٍ بسيط: هل البرنامج جاهز؟

`beforeSave` يمنع أي مستخدم من ترقية نفسه إلى `admin` — بحقّ. والنتيجة أن
خادماً جديداً يبدأ **بلا مشرف إطلاقاً**، ولا سكربت ولا خطوة موثّقة لإنشاء
أوّله. فالأئمة يسجّلون ويطلبون ملكية مساجدهم، وطلباتهم تبقى `pending` إلى
الأبد لأن `reviewMosqueClaim` تحتاج `admin`. **الطريق مقطوع عند أوّل خطوة**،
وكل ما بُني بعدها لا يُبلَغ.

لم يظهر في أي اختبار لأن كل اختبار يصنع مشرفه بالمفتاح الرئيسي — وهو ما لا
يملكه المُشغّل من التطبيق.

**العلاج:** `scripts/promote_admin.js` (و`npm run admin`). لا يُنشئ حساباً بل
يرقّي حساباً سجّل صاحبه بنفسه، فتبقى كلمة المرور عنده وحده. ومعه `--list`
ليُعرف من هم المشرفون، و`--demote` للإلغاء — وترجع الصفة إلى `donor`، أضيق
الأدوار، لا إلى ما كانت لأننا لم نحفظه. وصار خطوةً مرقّمة في `CLAUDE.md`.

**الدرس:** الاختبارات تُغطّي ما يفعله المستخدم، لا ما يفعله من يُشغّل النظام
أوّل مرّة. مسار الإقلاع لا اختبار له بطبيعته — فليُكتب في خطوات النشر.

---

## 🟢 سلسلة إثبات الإنجاز تحت التحقّق أخيراً

الإمام يعتمد عملاً لم يره إلا في صورةٍ رفعها المنفّذ، فسلامة هذه السلسلة هي ما
يمنع اعتماداً على غير بيّنة. وكانت `validatePhotos` مُختبَرة على روابط مخترَعة
وحدها: لا رفع حقيقي، ولا تحقّق أن المخزَّن هو المرفوع.

مِرقاة التكامل صارت تُشغّل محوّل ملفات على القرص (`@parse/fs-files-adapter`)
وتفتح الرفع للمصادَقين — المحوّل الافتراضي GridFS ويلزمه MongoDB. وعليه
`photos.test.js`: يرفع `Parse.File` حقيقياً، يمرّره بـ`markWorkDone`، يقرؤه
الإمام من السجل، **ثم يجلبه من رابطه ويقارن بايتاته بالمرفوع** — فالرابط في
السجل بلا محتوى ليس دليلاً، وقد يكون التخزين رفض الملف صامتاً.

ويرفض الرابط الخارجي ولو كان HTTPS، ويتحقّق أن الطلب لم يُترك في حالةٍ لم تقع
بعد الرفض (التحقّق قبل أي تعديل — «جولة سابعة»).

وفي المتصفّح: المنفّذ يختار صورةً فعلاً عبر `setInputFiles`، والإمام لا يكتفي
الاختبار بوجودها في السجل بل يتحقّق أنها **حُمِّلت في الصفحة**
(`naturalWidth > 0`) قبل زرّ الاعتماد — صورةٌ في السجل لا تُعرض تعني اعتماداً
على ثقةٍ لا بيّنة.

---

## 🟠 جولة رابعة عشرة — دورٌ كامل مبنيّ على الخادم ولا يصله العميل

الشركة تُسجَّل، ويعتمدها المشرف، ويكلّفها الإمام — ثم **لا ترى تكليفها أبداً**.

الخادم بُني لها من البداية: `assignWorker` يفرّع على الدور ويكتب في
`assignedContractorId`، و`startWork` و`markWorkDone` تقبلانها،
و`recordWorkerRating` تقرأ حقلها، و`payoutContractor` موجودة. أمّا العميل فذكرها
في موضعٍ واحد: شاشة المشرف التي تعتمدها. ثم:

- `assignedToMe` تستعلم على `assignedVolunteerId` وحده، فالشركة لا ترى شيئاً.
- لا تبويب «مهامّي» لها أصلاً — تبويبها الوحيد كان «حسابي».

فالنتيجة أن العمل يبقى في `assigned` إلى الأبد، ولا مخرج للإمام إلا سحب التكليف.

**العلاج:** الحقل يُشتقّ من الدور في العميل كما يفعل `assignmentField` في دوال
السحابة (الدور ثابت بعد التسجيل فالاشتقاق آمن)، و`MyVolunteering` صارت
`MyTasks` — الاسم القديم يكذب على الشركة — وقسم «اهتماماتي» يظهر للمتطوّعين
وحدهم لأن `expressInterest` مقصورة عليهم: الشركة يختارها الإمام مباشرةً.

**ولم يكن للمسار اختبارٌ قطّ.** أُضيف `contractor.test.js`: شركة غير معتمدة
تُرفض، يعتمدها المشرف فتُكلَّف، تُسجَّل في حقلها لا حقل المتطوّع، تراه باستعلام
الواجهة نفسه، تبدأ وتُبلّغ ويعتمد الإمام، ويتحدّث سجلّها كسجل المتطوّع.

**وفي ملفٍ مستقلّ لسببٍ تقني لا تنظيمي:** `directAccess` يربط نسخة Parse المفردة
بأوّل خادمٍ يُنشأ في العملية، فخادمان في ملفٍ واحد يجعلان الثاني يخاطب قاعدة
الأوّل بعد إغلاقها — وتظهر بـ`relation "_User" does not exist`. لكل ملفٍ عمليته.

**وكشف الاختبار رسالةً تخالف قاعدة المستودع:** `requireRole` كانت تسرد الأسماء
البرمجية — «هذه الخاصية متاحة لـ: volunteer فقط» — إنجليزيةٌ في واجهة عربية،
وتكشف تسميةً داخلية لا تعني قارئها شيئاً. صارت تُسمّي الأدوار بالعربية، مع
إدغام لام الجرّ في «الـ» («للمتطوّعين» لا «لـالمتطوّعين»). و«فقط» لا «وحدهم»:
الأخيرة تلزمها مطابقة العدد والجنس، و«الإدارة» مفرد مؤنّث فتصير «وحدها».

---

## 🟡 `npm run lint` لم يعمل قطّ

السكربت موجود في `package.json` منذ البداية، ولا ملفّ إعدادٍ لـESLint في
المستودع. فكان يفشل بـ«ESLint couldn't find a configuration file» في كل مرّة —
أي أن أحداً لم يشغّله، وأن من يقرأ `package.json` يظنّ الشيفرة مفحوصة.

أُضيف `.eslintrc.json` بقواعد الأخطاء وحدها (`eslint:recommended`) لا الأسلوب:
الغرض رصد الشيفرة الميتة والمتغيّر غير المعرَّف، لا فرض تنسيق. و`Parse` معرَّف
عالمياً لأن الخادم يحقنه عند التحميل، و`main.bundle.js` مستثنى لأنه مولَّد.

كشف أربع حالات شيفرةٍ ميتة، أُصلحت جميعاً: `requireUser` مستورَدة بلا استعمال
في `requests.js`، و`spawn` ووسيط `dataRoot` في مِرقاة التكامل، ووسيطٌ زائد في
اختبار الوارد.

---

## 🔴 نقطةٌ واحدة تحمل 154 مسجداً في 45 ولاية

**العَرَض:** لا شيء. كل شيء كان يعمل، والاختبارات خضراء، والبحث بالقرب يردّ
نتائج معقولة المظهر.

**كيف ظهر:** كنت أقيس شيئاً آخر — أردتُ عتبةً معقولةً للمسافة، فحسبتُ لكل مسجد
بُعدَه عن أقرب مسجدٍ في ولايته نفسها. فإذا الذيل مستحيل: مسجدٌ في «ثمريت» يبعد
741 كم عن أقرب جارٍ له في ولايته. وعُمان كلّها ألف كيلومتر.

**السبب:** بيانات الوزارة تحمل قيمةً افتراضية مكان الإحداثيّ المجهول.

- النقطة `23.59768, 58.42077` عليها **154 مسجداً في 45 ولاية مختلفة** — من
  صلالة إلى دبا إلى نزوى. نقطةٌ واحدة لا تكون في خمسٍ وأربعين ولاية.
- ونقاطٌ أخرى مشتركة أصغر، ومساجدُ مفردة في «منح» بالداخلية إحداثياتها في
  **دبي**، وفي «الحمراء» إحداثيات في عرض الخليج.

المجموع **414 مسجداً — 2.27٪** بإحداثيٍّ كاذب.

**لماذا لم يمسكه التنظيف:** `clean_mosques.py` يفحص كل صفٍّ **وحده**: أمقلوبٌ
هو؟ أخارج حدود عُمان؟ وكل صفٍّ من الأربعمئة يجتاز هذين الفحصين بامتياز — فهو
داخل عُمان وغير مقلوب. العطب ليس في الصفّ بل في **تكراره**، ولا يُرى إلا بالنظر
إلى السجلّات جملةً. فحصٌ بُني على الصفّ لا يمكنه أن يرى ما بين الصفوف.

**الأثر على الناس:** موقع المسجد هو ما يربط المصلّي بمسجده — وهذا هو المبدأ
الذي تقوم عليه المنصّة كلّها. مسجدُ صلالة الموضوع في مسقط يظهر لمتطوّعٍ في
مسقط على بُعد نصف كيلومتر فيقصده فلا يجده، ويختفي عن أهله في صلالة. وأسوأ من
غياب المسجد أن يحضر في المكان الخطأ.

**الإصلاح:** `scripts/lib/coord-trust.js` — لا نخمّن الصواب، بل **نسحب الثقة**
من الخطأ. قاعدتان:

| القاعدة | ما تلتقطه | العدد |
|---|---|---|
| النقطة نفسها في ولايتين فأكثر، أو خمسة مساجد عليها في ولاية واحدة | القيمة الافتراضية | 305 |
| بُعدٌ عن مركز الولاية يتجاوز `max(50 كم، 10× انتشار الولاية)` | المفرد المُزاح | 109 |

والعتبة تُقاس بانتشار الولاية نفسها لا برقمٍ واحد للسلطنة: «ثمريت» صحراءُ
مترامية و«منح» قريةٌ مجتمعة، ورقمٌ واحد يظلم إحداهما أو يُعمي عن الأخرى.
والمركز يُحسب بالوسيط بعد استبعاد القيم الافتراضية — لولا ذلك لجذبت النقطةُ
الافتراضية المركزَ إليها فبرّأت نفسها واتّهمت الصحيح.

والمسجد المسحوبة ثقة إحداثيّه يعود «مجهول الموقع»: يبقى في البحث بالاسم، ويجيء
في **ذيل** قائمة القرب لا في رأسها كذباً. **موقعٌ مجهول صدقٌ، وموقعٌ خاطئ يقود
الناس ضلالاً.**

و`data/mosques.json` لا يُمسّ: هو سجلٌّ أمينٌ لما أصدرته الوزارة، وحكمُنا عليه
طبقةٌ فوقه يطبّقها الاستيراد.

**ونصفُ الإصلاح كان سيكون أسوأ من تركه:** سحبُ الموقع من 414 مسجداً بلا طريقٍ
لردّه يترك أهلها خارج المنصّة إلى الأبد. طريق التعلّم القائم — تبنّي إحداثيّ
طلب الملكية بعد اعتماد المشرف — لا يبلغ مسجداً **سُجّل قبل ذلك**: لا طلبَ له
ينتظر. فأُضيفت `confirmMosqueLocation`: الإمام يقف عند مسجده فيثبّت موقعه، بشرط
ألّا يكون له موقعٌ قائم، ويُقيَّد الفعل في سجلّ المسجد باسمه.

**والدرس:** التحقّق يرث بنيةَ ما بُني عليه. مصفوفةٌ من الفحوص على الصفّ الواحد
لا تكشف عطباً في العلاقة بين الصفوف مهما كثرت، وليس ذلك نقصاً في دقّتها بل في
مستواها. ولم أذهب أبحث عن هذا — عثرتُ عليه وأنا أقيس شيئاً آخر. **القياس على
البيانات الحقيقية يكشف ما لا يكشفه التفتيش المقصود**، وقد وقع هذا في هذا
المستودع مرّتين الآن.

---

## 🟠 أشدّ قرارٍ للمشرف، وأقلّ ما بيده

**العَرَض:** لا شيء ظاهر. الإصلاح السابق ضاعف عدد المساجد مجهولة الموقع من 16
إلى 430، وكشف بذلك ثغرةً كانت قائمةً طوال الوقت وصغيرةً بما يكفي لتُغفَل.

**السبب:** طلب ملكية مسجدٍ **له موقع** يعرض على المشرف مسافةَ مقدّمه عنه
(«قُدّم من عند المسجد» / «من 800 كم»). أمّا مسجدٌ **بلا موقع** فلا مسافة له
تُقاس، فيرى المشرف `null` ويقرّر بلا قرينة.

وهذه بالضبط أشدّ حالةٍ يحتاج فيها إلى قرينة: اعتمادُه هناك لا يمنح الإمام مسجده
فحسب، بل **يمنح المسجد موقعه الدائم على الخريطة** — يقود إليه كل متطوّع بعدها.
فالحالة التي أثرها أبقى هي الحالة الوحيدة التي كان يُترك فيها بلا شيء.

**الإصلاح:** القرينة من بياناتنا نفسها — مساجد الولاية المعلومة. مسجدٌ في نزوى
لا يبعد عن سائر مساجد نزوى بمئات الكيلومترات.

| اللحظة | ما يقع |
|---|---|
| التقديم | يُقاس الموقع المُرسل إلى أقرب مسجدٍ معلومٍ في الولاية، ويُردّ ما جاوز 80 كم — فوراً، لا بعد انتظار مراجعة |
| العرض على المشرف | `wilayatNearestKm` مع تنبيهٍ صريح: «هذا المسجد بلا موقع، واعتمادك يمنحه موقع مقدّم الطلب» |
| الاعتماد | يُعاد القياس — الطلب قد يكون أُنشئ قبل وجود الفحص. والمريب لا يُبطل الطلب: يُعتمد الإمام ويبقى المسجد مجهول الموقع حتى يثبّته بنفسه |

**والثمانون كيلومتراً مقيسة لا مُخمَّنة:** على 17,784 مسجداً موثوقاً، بُعدُ كلٍّ
عن أقرب جارٍ في ولايته: الوسيط 260 متراً، المئين 99 عند 4.8 كم، **وأقصى ما وُجد
73.4 كم**. فالعتبة فوق أقصى الواقع، لا تُقصي قائماً، وتلتقط المستحيل.

**وخطأٌ وقعتُ فيه أثناء الإصلاح نفسه:** أوّل صيغةٍ كتبتُها كانت تردّ كل موقعٍ
لا تجد قربه مسجداً معلوماً — فأسقطت اختبارين قائمين. وأوّل ظنّي أن الاختبارين
فيهما نقص، ثم تبيّن أن **العلّة في قاعدتي**: بيئة الاختبار ولايةٌ لا مسجد معلوم
فيها، وقاعدتي تخلط بين «موقعٌ بعيد» و«ولايةٌ لا نعرف عنها شيئاً». **غيابُ
البيّنة ليس بيّنةَ نفي.** وليست الحالة نظرية: `DEPLOY.md` يوصي بالاستيراد على
مراحل، فولايةٌ لم تُستورد بعد تُقصي كل إمامٍ فيها بلا أن يفهم أحدٌ لماذا.
الفحص الآن يميّز الحالتين بسؤالٍ ثانٍ لا يُطرح إلا حين يخلو الجوار.

**والدرس:** حين يسقط اختبارٌ قائمٌ على تغييرٍ جديد، فالاحتمال الأوّل أن
التغيير أخطأ لا أن الاختبار قصّر. وقد كان الاختبار هنا يمثّل حالةً حقيقيةً
تماماً لم أفكّر فيها.

---

## 🟡 «ما حولك» تعرض ما على بُعد ألف كيلومتر

**السبب:** قائمة الفرص القريبة تُلحق بها المساجدَ مجهولةَ الموقع — وإلا لم تصل
طلباتُها متطوّعاً شارك موقعه أبداً. لكنها كانت تُجلب **من السلطنة كلّها**، بلا
ترتيب، بسقف خمسين.

وستةَ عشرَ مسجداً كانت ضجيجاً محتملاً. أمّا بعد سحب الثقة فصارت 430، فمتطوّعٌ
في مسندم يرى في قائمةٍ عنوانها «ما حولك» فرصاً في ظفار — فيفقد الثقة بالقائمة
كلّها، بما فيها الصحيح. والسقف بلا ترتيب يعني أن المقطوع عشوائيّ: مسجدٌ بعينه
قد لا يظهر أبداً بلا أن يُعرف لماذا.

**الإصلاح:** حصرٌ في محافظة المستخدم، وترتيبٌ صريح بعدد الطلبات المفتوحة.

والمحافظة تُستنبط بلا خدمةٍ خارجية: عندنا 17,784 نقطةً معلومة موزّعة على
السلطنة، فأقربُها إلى المستخدم يقول أين هو. وسلسلة البدائل مقصودة:

1. أقرب مسجدٍ معلوم خلال 25 كم — يكفي كل مأهول.
2. فإن كان في صحراء أو بحر: المحافظة من ملفّه.
3. فإن لم تكن: السلطنة كلّها كما كان. **وإخفاء الفرصة أسوأ من إظهارها بعيدة.**

**والدرس:** إصلاحُ عطبٍ يكشف ما بُني على حجمه. الحصرُ في المحافظة كان صواباً
من أوّل يوم، لكنه لم يكن يُحدث فرقاً وهي ستة عشر — والقرار الذي لا يُحدث فرقاً
لا يُنظر فيه. فحين يتضاعف حجمُ شيءٍ في النظام سبعاً وعشرين مرّةً، يُعاد النظر
فيما اتُّكئ على صغره لا فيما تغيّر وحده.

---

## 🟡 «عبـري» — عُشر السجلّات باسم ولايةٍ مشوّه

**العَرَض:** لا شيء يسقط. البحث يعمل، والتجميع صحيح.

**السبب:** التطويل (`ـ` — U+0640) محرفٌ زخرفيّ يمدّ الحرف بصرياً ولا يحمل
معنى. وفي بيانات الوزارة **1,836 مسجداً — عُشر السجلّات** — تحمل ولايةً
ممدودة: «عبـري»، «ضـنـك»، «السـنينه». وواحدٌ اسمه «زايــد».

**الأثر:** بطاقة كل مسجدٍ منها تعرض اسم ولايته مشوّهاً. وإمامٌ في عبري يرى
«عبـري» فيشكّ أن التطبيق أخطأ في مسجده — والثقة في منصّةٍ كهذه رأسُ مالها.
ودونه أثرٌ كامن: «زايد» لا تجد «زايــد» في بحث الكلمات، والمطابقة التامّة على
ولايةٍ ممدودة لا يبلغها من كتب الاسم كما يُكتب.

**الإصلاح:** التجريد عند الاستيراد، و`externalId` وحده يبقى كما هو — هو هويّة
السجلّ التي يُبنى عليها التحديث، وتغييرها يجعل كل مسجدٍ يبدو جديداً فتُنشأ
نسخةٌ ثانية ويُهجر الأصل بطلباته وملكيّته.

**وحارسٌ لم يكن موجوداً:** `normalizeArabic` في السحابة و`normalize_ar` في
بايثون **يجب أن يتطابقا** — الأوّل يطبّع ما يكتبه المستخدم، والثاني يطبّع ما
يُخزَّن في `nameNormalized`. وانفصالهما يُبطل الحقل كلّه بصمت. ولم يكن بينهما
شيء، فأُضيف اختبارٌ يستخرج دالّة بايثون من مصدرها نصّاً — بلا استيراد الوحدة،
تفادياً لجرّ `pandas` — ويشغّل الاثنين على الكلمات نفسها ويقارن. جُرّب بحذف سطر
التطويل من بايثون فسقط، ثم أُعيد.

**وعطبٌ ثانٍ ظهر أثناء الإصلاح:** `seed_mosques.js` ومِرقاة اختبار التكامل كانا
يكتبان حقول المسجد كلٌّ على حدة. فلمّا طُبّق سحبُ الثقة من الإحداثيات على
الاستيراد وحده، صارت المِرقاة تزرع في القاعدة إحداثياتٍ لا يزرعها الإنتاج
أبداً — **فاختبار التكامل يتحقّق من بياناتٍ لا وجود لها.** وهي أخطر أنواع
الاختبار: خضراءُ عن شيءٍ آخر. الحقول الآن من `scripts/lib/mosque-record.js`
وحدها، ويحرس ذلك سلكُ تعثّر.

**والدرس:** نسخةٌ ثانية من منطقٍ لا تُخطئ يوم كتابتها، بل يوم يتغيّر الأصل
وحده. ولم يكشفها اختبار — كشفها أنني مررتُ على المِرقاة لسببٍ آخر.

---

## 🔴 461 مسجداً لا يجدها أئمّتها مهما وقفوا عندها

**كيف ظهر:** لا بالتفتيش، بل بالقياس. أوّل مخاوف الإطلاق المكتوبة في
`DEPLOY.md` سؤالٌ واحد: **هل يجد الإمام مسجده؟** فأعدتُ قياسه على البيانات
كاملةً بعد أن صار البحث يرتّب بالقرب — مئتا مسجدٍ من كل المحافظات، الإمام واقفٌ
عند مسجده يبحث باسمه.

| | مسجده أوّلَ النتائج | ضمن الثلاثة الأُوَل | غاب تماماً |
|---|---|---|---|
| بلا موقع | 132 / 200 | 168 | 2 |
| بالموقع | 198 / 200 | 198 | **2** |

الفرق في العمود الأوّل هو ما كنت أقيسه، وهو كبير. لكن العمود الأخير هو الذي
أوقفني: **مسجدان غابا تماماً، والموقع لم يُغنِ عنهما شيئاً.**

**السبب:** سقف النتائج ثلاثون، و`limit(30)` تُنفَّذ في **القاعدة**، أي **قبل**
الفرز بالقرب. فتردّ القاعدة ثلاثين صفاً بأيّ ترتيبٍ كان، ثم نفرزها نحن — ومسجد
الإمام قد لا يكون في الثلاثين أصلاً. والفرزُ لا يستدعي ما لم يُجلب.

و«مصلى العيدين» في شمال الباطنة **123 مسجداً**، وفي شمال الشرقية 82.
**461 مسجداً** في بيانات الوزارة تقع في مجموعاتٍ متطابقة الاسم داخل محافظتها
أكبر من ثلاثين — أئمّتها لا يجدون مساجدهم مهما فعلوا.

**الإصلاح:** السقف في الاستعلام صار **سقف مرشّحين** (500) لا سقف نتائج، والقطع
إلى الثلاثين يقع **بعد** الفرز. وهي القاعدة نفسها التي يتبعها `getNearbyMosques`
منذ البداية — أُغفلت في البحث وحده.

**القياس بعد الإصلاح: 200 من 200، كلُّها أوّلَ النتائج، ولا واحدة غابت.**

**والدرس:** «اجلب ثم رتّب ثم اقطع» — وأيّ قطعٍ يسبق الفرز يُلغي الفرز. وهذا
عطبٌ لا يظهر إلا على البيانات الحقيقية بحجمها: في مِرقاةٍ فيها خمسة مساجد
متطابقة لا سقفَ يُتجاوز، وكل اختبارٍ يمرّ. **قِسْ على ما سيُشغَّل عليه.** وهي
المرّة الثالثة في هذا المستودع التي يكشف فيها القياسُ على البيانات كاملةً ما لم
يكشفه التفتيش المقصود.

---

## 🟢 مواقع المساجد لا تنتظر أئمّتها — ولا تُشترى

**التوجيه:** «وكيل المسجد أو الإمام لن يكون عائقاً — سيسجَّل يدوياً بعدين.
مواقع المساجد موجودة في جوجل ماب بالاسم.» ثم: **«لا أريد فواتير.»**

وهو تصحيحٌ لأولوية بنيتُها خطأً: جعلتُ استعادة مواقع الـ430 معلّقةً على أن يقف
عندها أئمّتها. وذلك يؤخّر أهلها إلى أجلٍ غير معلوم بينما المصدر متاح.

**والخطر الظاهر:** أن نأخذ أوّل نتيجةٍ من جوجل — وهو عين ما كلّفنا 414 إحداثياً
كاذباً حين أُخذت بيانات الوزارة على علّاتها. «مصلى العيدين» في الولاية الواحدة
سبعةٌ وثمانون، فأيُّها مسجدُنا؟

**والمصدر ليس جوجل.** البحث النصّي في Places مدفوعٌ بالطلب — 430 طلباً تعني
فاتورة. و**OpenStreetMap** مفتوحة بترخيص ODbL: تنزيلٌ واحد مجاني يأتي بكل مساجد
السلطنة، ثم تجري المطابقة على الجهاز بلا شبكة. لا مفتاح، ولا حساب، ولا عدّاد
طلبات — وإعادة التشغيل مجانية. وقواعد القبول واحدة أياً كان المصدر.

فلا يُقبل المرشّح إلا باجتماع خمسة، وما رُدّ يبقى مجهولاً وسببُ ردّه مكتوب:

| الشرط | ما يمنعه |
|---|---|
| بيتُ صلاة (وسمُ الخريطة، أو كلمةٌ في اسمه) | أن يصير موقع صيدليةٍ موقعَ مسجد |
| كلماتُ اسمنا المميِّزة كلُّها في اسمه | مسجدٌ آخر في الحيّ نفسه |
| قريبٌ من مساجد ولايته المعلومة (≤80 كم) | مسجدٌ يحمل الاسم في محافظةٍ أخرى |
| أقربُ معلومٍ إليه من ولايتنا لا من جارتها | تكرارُ الاسم عبر حدٍّ إداريّ |
| غيرُ مأخوذ بمسجدٍ معلوم **في أي ولاية** | نسخُ إحداثيّ مسجدٍ آخر |
| لا مرشّح آخر يزاحمه | التخمين بين متشابهَين |

**والقاعدتان الرابعة والخامسة لم أكتبهما ابتداءً — كشفتهما تجربةٌ على البيانات
الحقيقية.** زرعتُ عشر نقاطٍ اصطناعية لعشرة مساجد مجهولة، وأضفتُ ألف مسجدٍ معلومٍ
بأسمائها ومواضعها الحقيقية ضجيجاً، ثم شغّلتُ المطابقة على الـ430 كلّها:

| | مقبول | منها صحيح |
|---|---|---|
| بالقواعد الثلاث الأولى | 32 | 8 |
| + «مأخوذ» على الجوار كلِّه | 9 | 8 |
| + «لمن هذا الموضع» | **10** | **10** |

الأربعةُ والعشرون الزائدة في الصفّ الأوّل كانت كلُّها على بُعد **صفرٍ** من مسجدٍ
معلومٍ في الولاية المجاورة — أي نسخَ مساجدَ أخرى لا اكتشافَ مسجدنا؛ لأن فحص
«مأخوذ» كان داخل الولاية وحدها. والحدود الإدارية لا تعني الخرائط شيئاً.

والباقي بعد ذلك واحدٌ: «الغفار جل جلاله» في صلالة أخذ موضع سميِّه في رخيوت،
وبينهما 74 كم — دون الثمانين. **فالثمانون حاجزٌ أمام الكوارث (850 كم) لا فاصلٌ
بين ولايتين متجاورتين**، والأسماء تتكرّر عبر الحدود. فأُضيف أن يكون أقربُ مسجدٍ
معلومٍ إلى الموضع من ولايتنا لا من غيرها.

**وشرطُ بيت الصلاة كذلك لم أكتبه ابتداءً — كشفه اختبارٌ سقط.** كنت أختبر أن المطابق
الوحيد يُقبل ولو رافقه غيرُ مطابق، فوضعتُ «صيدلية النور» شاهداً على غير
المطابق، فإذا هي **تطابق**: الكلمة العامّة «مسجد» تُطرح قبل المقارنة، فلا يبقى
بين «مسجد النور» و«صيدلية النور» إلا «النور». ولو جاءت الصيدلية وحدها في
النتائج لصار موقعُها موقعَ المسجد.

**وترتيب المصادر عند التعارض: الوزارة، ثم من وقف عند المسجد، ثم جوجل.** دبّوس
الخرائط وضعه غريب، والإمام وقف هناك.

**وعطبٌ في ترتيبٍ سابق كشفه هذا العمل:** حارس إعادة الاستيراد كان يحفظ
`locationSource === 'claim'` وحده. ولمّا أُضيفت `confirmMosqueLocation` تكتب
`'imam'`، صارت **كل إعادة استيرادٍ تمحو موقع كل مسجدٍ وقف عنده إمامه** — عطبٌ
أدخلتُه بنفسي قبل ثلاث دفعات ولم يكشفه شيء. والقائمة الآن **معكوسة**: تُعدّد ما
يكتبه الاستيراد (`ministry`, `google`)، وما عداه محفوظٌ افتراضياً.

**والدرس:** قائمة استثناءات تُعدّد ما يُحمى تنسى الداخل الجديد فتمحوه؛ وقائمةٌ
تُعدّد ما يُستبدل تحمي الداخل الجديد افتراضياً. **حين يكون الخطأ في جانبٍ أفدح،
اجعل الافتراض في الجانب الآخر.**

**ودرسٌ ثانٍ:** ثلاثٌ من قواعد القبول الخمس كتبتُها بالتفكير، واثنتان كشفتهما
تجربةٌ على البيانات الحقيقية — وهما اللتان كانتا تُدخلان 24 موقعاً خاطئاً من 32.
**التفكيرُ يضع القواعد، والتجربةُ على الحجم الحقيقي تقول أيُّها ناقص.**

**ثمّ دليلٌ يُرفق بالردّ.** «لا اسم يطابق» رقمٌ لا يقول شيئاً وحده: أقاعدتُنا
أضيق مما ينبغي، أم أن المسجد ليس في الخريطة أصلاً؟ والفرق حاسم — الأوّل يُعالَج
والثاني لا. فصار كلُّ ردٍّ لاسمٍ يحمل معه **أقربَ ثلاثةِ أسماءٍ كادت تطابق**.

وأثمر في التجربة الأولى: ظهر السطر

```
  نزوى — «السيدة حفصة بنت عمر» ← «مسجد حفصة بنت عمر» (3)
```

فإذا **الألقاب** — «الصحابي»، «السيدة»، «الإمام»، «الشيخ» — تتصدّر الاسم في
بيانات الوزارة ولا تكاد تُكتب في الخرائط. و**63 من الـ430** المجهولة موقعُها
تبدأ بلقب (و1,068 في البيانات كلّها). واللقب وصفٌ لا تعريف: ليس ثمّة «خالد بن
الوليد» آخر يُفرَّق عنه باللقب. فصار يُطرح — **بشرط أن يبقى بعده كلمتان**، وإلا
صار «الشيخ سالم» كلمةً واحدة شائعة. **وتخفيفُ القاعدة لا يجوز أن يبلغ حدَّ
إبطالها.**

وظهر معه عطبٌ مقابل: شرطُ الاسم في اتجاهٍ واحد فتزيدُه كلمةٌ ولا تضرّ، لكنّ
الزيادة قد تكون فارقاً — «الفردوس» و«الفردوس الأعلى» مسجدان لا مسجد. فصار
المطابقُ **تماماً** يُقدَّم على من زاد عليه.

وقياسٌ على البيانات كاملةً بمجموعةٍ اصطناعية (لكل مسجدٍ مجهول نقطةٌ باسمٍ ناقصِ
الكلمة الأولى، ومعها 600 مسجدٍ معلومٍ ضجيجاً): **119 مقبولاً، 117 منها صحيح.**
والاثنان الباقيان اسمان متطابقان في ولايةٍ واحدة — لا يفرّق بينهما شيء.

**وهذه أرقامٌ دالّة لا تنبّؤية:** المجموعة من صنعي، والتغطيةُ الحقيقية لـOSM
في عُمان لم تُقَس بعد — الشبكة محجوبة في بيئة التطوير هذه.

**والنسبةُ التزامٌ لا تجميل:** ODbL يشترط ذكر المصدر عند الاستعمال العلنيّ.
فـ«© مساهمو OpenStreetMap» مكتوبةٌ في كل ملفٍّ يُخرجه السكربت، **ومعروضةٌ في
شاشة «حسابي»** — ويحرس ظهورَها اختبار متصفّح، لأن التزاماً قائماً على سطرٍ في
الشيفرة لم يره أحدٌ يُعرَض ليس التزاماً.

**وما بقي خارج التغطية:** التنزيل نفسه — الشبكة محجوبة في بيئة التطوير هذه.
فانفصلت قراءةُ ردّ Overpass عن نقله وصارت دالّةً نقيّةً تحت الاختبار: شكلُ
`node` مقابل `way`/`relation` (الأوّل يحمل الإحداثيّ، والآخران في `center`)،
وتقديمُ `name:ar`، وإسقاطُ ما لا اسم له. **يبقى خارج التغطية النقلُ وحده، لا
فهمُ ما نُقل.**

**ثمّ فحصٌ ثانٍ على الملفّ نفسه.** وثيقةُ النشر توصي بمراجعته وتحريره قبل
إيداعه — أي أنّ مدخلةً بشريّةً تُكتب في `Mosques.location` بلا مارٍّ آخر عليها.
فأُعيد الفحص عند الاستيراد بالقاعدة نفسها، كما يُعاد قياس طلب الملكية لحظةَ
اعتماده لا لحظةَ تقديمه: **آخرُ من يلمس البيانات قبل القاعدة يفحصها.**

وكشف اختبارُ ذلك فخّاً في جافاسكربت: `Number(null)` يساوي **صفراً** لا `NaN`،
و`Number('')` كذلك. فحقلٌ حُذفت قيمتُه في التحرير يصير الإحداثيَّ (0,0) — نقطةً
في الأطلسي قبالة غانا، «صالحةً» في كل فحصٍ للمدى. وقد كانت قاعدةُ البُعد عن
الولاية تردّها بالمصادفة، **والاعتماد على المصادفة ليس فحصاً.**

---

## 🟢 من وقف عند المسجد أعلمُ بموضعه من أيّ مصدر

**التوجيه:** «سيتم تصحيحها لاحقاً، والإمام أو وكيل المسجد سيصوّب الموقع.»

وهو تصحيحٌ لقرارٍ اتّخذتُه أنا: `confirmMosqueLocation` كانت **ترفض** المسجدَ
الذي له موقع — «لهذا المسجد موقعٌ مسجّل، لتصحيحه راسل الإدارة». بنيتُها على أن
بيانات الوزارة مرجعٌ لا يُنسخ فوقه، والإمام يملأ الفراغ لا غير.

**وقد أبطلتُ ذلك المبدأ بنفسي قبل عشر دفعات** حين أثبتُّ أن **414 إحداثياً في
بيانات الوزارة كاذب** — نقطةٌ واحدة تحمل 154 مسجداً في 45 ولاية. فالمرجع ليس
معصوماً، وما يُستخرج من الخرائط تقديرٌ لا يقين، والواقف عند مسجده أصدق منهما.
لكنّي تركتُ القاعدة القديمة قائمةً على مقدّمةٍ سقطت.

**الإصلاح:**

- الإمام أو الوكيل **يصوّب** موقعاً مسجَّلاً، لا يملأ الفارغ فحسب.
- والتصويب يُقاس إلى مساجد الولاية كما يُقاس موضعُ طلب الملكية. **والفحص ليس
  شكّاً في الإمام بل في الجهاز:** إشارةٌ ضعيفة داخل البناء تعطي إحداثياً بعيداً
  بكيلومترات ولا يظهر ذلك لصاحبه، وتصويبٌ يضع المسجد في محافظةٍ أخرى أسوأ من
  الخطأ الذي جاء يصلحه.
- ويُقيَّد بفعلٍ يميّزه (`location_corrected`) **ومعه ما كان قبله**: «سُجّل
  موقع» لا يقول ماذا تغيّر، ومن يملك تغيير البيانات يجب أن يُرى وهو يغيّرها.
  ولذلك أُضيف حقل `note` إلى سجلّ التدقيق ويصل إلى الشاشة.

**والأثر الذي كاد يُفلت:** ترتيب المصادر في الاستيراد كان «الوزارة، ثم الإنسان،
ثم الخرائط» — أي أن إعادة الاستيراد **تنسخ إحداثيّ الوزارة فوق تصويب الإمام**.
فتصويبٌ يمحوه السكربتُ في تشغيلته التالية ليس تصويباً. صار ما مصدرُه إنسانٌ لا
يُنسخ فوقه ولا يُمحى، مهما جاء به الاستيراد.

**وتتمّةٌ ثانية — لمن يقود فعلاً:** التنبيه الأوّل يراه الإمام في «مساجدي»،
وهو لا يقصد المسجد بجسده. أمّا **المنفّذ** فيفتح «الطريق إلى المسجد» ويسوق
إليه. فمن يُساق إلى نقطةٍ تقديرية بلا أن يُقال له يقف عند مكانٍ ليس مسجده،
فيظنّ العمل وهماً ويتّهم المنصّة لا الخريطة. فصار يُقال له «الموقع تقديريّ —
استرشد بالولاية والقرية»، ومسجدٌ بلا موقعٍ أصلاً يُقال له ذلك كذلك بدل أن
يغيب الرابط بلا تفسير.

**وتتمّةٌ أولى:** التصويب متاحٌ للجميع، لكنّ الحاجة إليه ليست واحدة. فصار
`getMyMosques` يقول **من أين جاء الموقع**، وموقعٌ مستخرَجٌ من خريطةٍ مفتوحة
يُنبَّه إمامُه إليه ويُفتح له الزرّ: **من لا يعلم أن موقع مسجده مُخمَّن لا يخطر
له أن يتحقّق منه.** وذاك فرقُ ما بين إتاحة التصويب وطلبه.

وحقل `note` كان يصل من الخادم **ولا يُعرض في الشاشة** — فالسجلّ يقول «صُوّب
الموقع» ولا يقول ماذا كان، وهو ما ادّعيتُ أنه يقوله. عيبٌ من صنف نسبة ODbL
نفسه: ادّعاءٌ يقوم على بياناتٍ لا تبلغ العين.

**والدرس:** حين تسقط مقدّمةٌ، فتّش عن كل ما بُني عليها. أثبتُّ كذبَ بيانات
الوزارة في موضع، وتركتُ في موضعٍ آخر قاعدةً تقول إنها مرجعٌ لا يُمسّ — ولم
يكشف التناقضَ اختبارٌ ولا مراجعة، بل كشفه صاحبُ المنتج بجملةٍ واحدة.

---

## 🟠 صورةٌ كسولة خارج الشاشة — وإصلاحٌ أوّلُ كان خطأً

**العَرَض:** اختبار المتصفّح يسقط مرّةً كل بضع تشغيلات، **دائماً** في «الإمام
يرى الصورة ثم يعتمد» وفي التي تليها. ولم يتكرّر في ثماني عشرة تشغيلة متتالية
حين تتبّعتُه أوّل مرّة، فتركتُه مذكوراً غير مشخَّص.

**السبب — وقد ثبت بتجربةٍ لا باستنتاج:**

| الحلقة | ما تفعله |
|---|---|
| `loading="lazy"` على وسم الصورة | المتصفّح **لا يجلب البايتات** حتى تقارب الصورةُ الشاشة |
| `.gallery img { width:100%; aspect-ratio:1 }` | للصورة صندوقٌ قبل تحميلها، فتراها Playwright «ظاهرة» |
| `waitForSelector` | تعود عند الظهور — أي **قبل أن يبدأ الجلب أصلاً** |
| المعرض أسفل بطاقة الطلب | وقوعُه داخل الشاشة أو خارجها يتغيّر بطول ما قبله |

فآخرُ سطرٍ هو مصدر التقطّع: يسقط حين يطول ما قبله. ويسقط معه ما بعده، لأن
الإمام يبقى على شاشةٍ غير التي تليها.

**وإصلاحي الأوّل كان خطأً.** ظننتُ العلّة سباقَ تحميلٍ عادياً فانتظرتُ
`complete`. وصورةٌ خارج الشاشة **لا تبدأ التحميل**، فلا يصدق `complete` أبداً:
جرّبتُها فبقيت كاذبةً بعد 2364 مللي ثانية. أي أن «الإصلاح» كان يحوّل سقوطاً
سريعاً إلى سقوطٍ بطيء — ولم أكن لأعرف ذلك لو اكتفيتُ بتشغيلاتٍ خضراء، فقد
مرّت اثنتا عشرة تشغيلة نظيفة فوقه.

**والإصلاح الصحيح:** `scrollIntoViewIfNeeded` ثم انتظار `complete` ثم قياس
العرض — **وهو ما يفعله الإمام نفسه قبل أن ينظر إلى الصورة.**

**والدرس الأهمّ:** كتبتُ في هذا الملفّ من قبل أن التشغيلات الخضراء لا تُثبت
زوال خللٍ متقطّع، وأن الإثبات يكون بفهم السبب. ثم ادّعيتُ فهماً لم أتحقّق منه،
واستشهدتُ باثنتي عشرة تشغيلة خضراء على إصلاحٍ لا يُصلح شيئاً. **والفرق بين
التعليل والبرهان أن البرهان يمكن أن يُكذّبك** — وقد كذّبني: أوّل تجربةٍ كتبتُها
لم تُظهر السباق أصلاً، لأنني نسيتُ مقاس الـCSS و`lazy`. فلمّا حاكيتُ الصفحة
كما هي، ظهر السبب وظهر معه خطأ إصلاحي.

---

## 🔴 جلسةٌ أُبطلت، وتطبيقٌ يظنّ نفسه داخلاً

**كيف ظهر:** بسؤالٍ عن طرفٍ لم يُفحص قطّ — ماذا يرى المستخدم حين **ينتهي**
شيء؟ لا حين ينجح ولا حين يُخطئ في مدخلاته، بل حين تسقط جلسته من تحته.

**السبب:** `Parse.User.current()` يقرأ من تخزين المتصفّح لا من الخادم. فرمزُ
الجلسة قد يُرفض — تنتهي صلاحيته، أو تُبطَل من لوحة الإدارة، أو **تُستبدل
القاعدة عند نشرٍ جديد** — والتطبيق لا يعلم: يرسم الهيكل والتبويبات واسم
المستخدم، ثم تفشل كل شاشةٍ يفتحها واحدةً واحدة.

**والرسالة التي يراها إنجليزية:** `Invalid session token`. في واجهةٍ عربية،
ولا مخرج منها — لا زرّ خروجٍ يعمل، ولا سبيل إلا مسح بيانات المتصفّح. ومستخدمٌ
عاديّ لا يعرف ذلك، فيهجر التطبيق ظانّاً أنه معطوب.

**وحالةٌ ثالثة كانت أسوأ من الخطأ نفسه:** شاشة «حسابي» تقرأ `state.rows`
ككائن، و`useList` يُرجع `rows: []` عند الفشل — **ولا تعرض `state.error` أصلاً**.
فكان الفشل يرسم **ملفاً فارغاً كأنه بيانات صحيحة**: «بلا اسم»، وصفةٌ فارغة،
ولا كلمة عمّا جرى. وبياناتٌ كاذبةٌ صامتة أسوأ من رسالة عطل.

**الإصلاح:**

- كل خطأ يمرّ بمعالجٍ واحد في `api.js`. والكود **209** يأتي من طرفين — Parse
  حين لا يعرف الرمز، وحارسُنا `requireUser` حين لا جلسة — وكلاهما يعني الشيء
  نفسه: العميل يظنّ نفسه داخلاً والخادم لا يعرفه. فيُمحى المحلّي ويُطلق حدث.
- `App` يلتقطه فيعيد شاشة الدخول ومعها **سببٌ بالعربية**، ويختفي التنبيه بعد
  الدخول.
- ورسائل Parse تُترجَم. **والفصل بينها وبين رسائلنا بالحرف لا بالكود:** دوالُّ
  السحابة عندنا تتكلّم العربية دائماً، وParse يتكلّم الإنجليزية دائماً؛ وكودٌ
  واحد قد يأتي من الطرفين (209، و101 من تسجيل دخولٍ خاطئ ومن `notFound`).
  فما فيه حرفٌ عربيّ يُعرض كما هو، وما سواه يُترجَم بكوده.
- و«حسابي» تعرض خطأها.

**ولا يراها اختبار التكامل** — هو يحمل رمزاً صالحاً دائماً. فالاختبار في
المتصفّح: تُحذف الجلسة من `_Session` بالمفتاح الرئيسي، ثم يُضغط تبويب، فيجب أن
تعود شاشة الدخول ويعمل الدخول من جديد بلا مسح بيانات المتصفّح.

**والدرس:** فتّشتُ طويلاً في مسارات النجاح وفي مدخلات المستخدم الخاطئة. وهذا
عطبٌ في **زوال شرطٍ كان قائماً** — لا خطأ فعلٍ ولا خطأ مُدخَل. وهي فئةٌ كاملة:
ماذا يقع حين ينتهي ما كان صحيحاً؟

**وعطبٌ في أداة التشخيص نفسها ظهر أثناء ذلك:** مِرقاةُ الاختبار تطبع نصّ
`main` عند العطل، و`main` لا تُركَّب أصلاً على شاشة الدخول — فكانت تطبع
«تعذّرت القراءة» في اللحظة التي يلزم فيها النصّ. صارت ترجع إلى `body`.

---

## 🔴 الرايةُ زينة: إيقافُ الحساب لا يوقف شيئاً

**كيف ظهر:** بمتابعة الفئة التي فتحها عطبُ الجلسة — **زوالُ شرطٍ كان قائماً**.
والحالة الأولى في القائمة: مستخدمٌ يُوقَف حسابه.

**السبب:** `isActive` يُضبط `true` عند التسجيل في `beforeSave`، ثم **لا يُقرأ
في مكانٍ واحد** إلا تصفية من يصله بثُّ الإشعارات القريبة. لا `requireUser`
يقرؤه، ولا `requireRole`، ولا دالّةٌ من الثلاثين.

فالموقوف — وهو **الأداة الوحيدة** بيد الإدارة لكفّ مسيء — كان يدخل ويُنشئ
الطلبات ويسجّل الاهتمام ويتسلّم التكليف ويُبلّغ بالإنجاز، ولا يُمنع من شيء إلا
أن إشعارَ الفرص القريبة لا يصله. **رايةٌ تُرفع ولا تحجب.**

**الإصلاح في موضعين، وكلاهما لازم:**

- **`beforeLogin`** يردّه عند الباب فلا تُفتح له جلسة أصلاً — أصدقُ وأرحم من
  أن يدخل ويصطدم بالمنع في كل ضغطة.
- **`requireUser`** يكفّه عن كل فعل. فالإيقاف يقع **والجلسة مفتوحة**، ولو
  انتُظر خروجُه ليُكفّ لبقي يعمل ما شاء حتى ينتهي رمزُه.

**والشرط `=== false` لا `!isActive`.** حساباتٌ سجّلت قبل وجود الحقل لا تحمله،
و`!undefined` صادقة — فكانت الصيغة المختصرة ستُقصيها كلَّها. **غيابُ البيانات
لا يُقرأ إدانةً.**

**ولا يكشفه البديل في الذاكرة:** `beforeLogin` لا يُشغّله إلا خادمٌ حقيقي عند
تسجيل دخولٍ حقيقي، وجلسةٌ قائمة لموقوفٍ لا تُحاكى بكائن. فالاختبار على خادم.

**ولأن الأداة لا تنفع إن لم يكن لها مقبض:** `promote_admin.js` صار يحمل
`--suspend` و`--restore` و`--suspended`. وتركُ ذلك لتحرير حقلٍ يدوياً في لوحة
Back4app يجعل كفَّ المسيء عرضةً لخطأ ضغطة.

**والدرس:** حقلٌ يُكتب ولا يُقرأ ليس سياسةً بل نيّة. وقد رصدتُ من قبل في هذا
المستودع حقولاً تُقرأ ولا تُكتب (`skills`، `governorate`)، وهذه نقيضها — ولم
أكن أفتّش عنها. **فليكن السؤال قائماً في الاتجاهين: ما يُكتب هل يُقرأ؟ وما
يُقرأ هل يُكتب؟**

---

## 🟠 شرطٌ صدق طرفاه في لحظتين لم تجتمعا

**العَرَض:** اختبار المتصفّح «التنبيهات تصل الإمام» يسقط مرّةً كل ثلاث تشغيلات،
ورسالتُه «التعليم حذف القائمة» بينما لقطةُ الشاشة تعرض القائمة كاملة.

**السبب:** الشرط كان يُقرأ باستعلامين متتاليين:

```js
await page.locator('.card').count() > 0
&& await page.locator('.card.unread').count() === 0
```

وبين الاستعلامين تمرّ الشاشة بإطار «جارٍ التحميل…» بلا بطاقات. فيقرأ الأوّل
بطاقاتِ ما قبل التحديث فيصدق، ويقرأ الثاني صفراً غير مقروء — **لأنه صفرُ بطاقات
أصلاً** — فيصدق. ويمرّ الشرط على حالةٍ لم تقع قطّ.

**الإصلاح:** العدّان من لقطةٍ واحدة داخل `page.evaluate`، فلا يفصل بينهما إطار.

**والدرس:** الانتظار على شرطٍ مركّب يحتاج أن يكون **الشرط كلّه** من لحظةٍ واحدة.
وهذا الاختبار كان قد «أُصلح» من قبل حين استُبدل `waitForSelector` بشرطٍ مستقرّ —
فالإصلاح السابق عالج سبباً حقيقياً وترك هذا. **العلّةُ الثانية لا يفضحها إلا
تكرار التشغيل**: تشغيلةٌ خضراء واحدة لا تُثبت شيئاً عن اختبارٍ متقطّع.

---

## 🟠 حُكمٌ بالغياب يُطلب بلا المدّة التي يقوم عليها

**كيف وُجد:** لم يُوجد بالتخمين. الدرس المكتوب في القسم السابق — «فليكن السؤال
قائماً في الاتجاهين: ما يُكتب هل يُقرأ؟ وما يُقرأ هل يُكتب؟» — أُجري على المخطط
كلّه بمسحٍ آليّ: كلّ حقلٍ في `cloud/schema.json` يُبحث عن كتابته وقراءته في
`cloud/` و`app/src/` و`scripts/`. فخرجت ثمانية عشر حقلاً تُكتب ولا تُقرأ.

**والمسح دلّ ولم يحكم.** قُرئ كلُّ حقلٍ منها في موضعه، فإذا أكثرُها ليس عطباً:

| الحقل | الحكم بعد القراءة |
|---|---|
| `lastLat` / `lastLng` | **خطأ في المسح** — تُقرآن في `push.js` عبر `geo.withinBox(near, box, 'lastLat', 'lastLng')`، ولا يلتقطها نمطٌ يبحث عن `equalTo('lastLat'` |
| `AuditLog.actorId` | **مقصود** — `getMosqueAuditTrail` تُعيد الدور دون الهوية: «الغرض تتبّع المسار لا كشف الأشخاص». يبقى للمساءلة بالمفتاح الرئيس |
| `ServiceRequests.imamRating` | **مقروءٌ بأثره** — يُجمَّع في `avgRating` للمنفّذ، وهو معروضٌ في «حسابي» وفي بطاقة المهتمّ |
| حقول `Transactions` | **معطّلةٌ بقرار** — مسار التبرّع موقوفٌ حتى تصريح الوزارة |
| `_User.lastKnownLocation` | **محجوزٌ بلا مستعمل** — نسخةُ `GeoPoint` من إحداثيَّين يُقرآن فعلاً. تُرك، وهو محميٌّ بـ`protectedFields` |
| `Mosques.address` | **ميّتٌ** — لا يُكتب ولا يُقرأ، ولا عنوانَ في بيانات الوزارة أصلاً. حُذف من المخطط |

**والعطب الحقيقي كان في `assignedAt`.** يُكتب عند كل تكليف، ويُمحى عند كل سحب،
ولا يُقرأ في موضعٍ واحد. وشاشة الإمام تقول:

> كُلِّف المنفّذ ولمّا يبدأ بعد. إن لم يحضر فاسحب التكليف…

**الجملة نفسها في اليوم الأول وفي الشهر الثالث**، وتحتها زرّ «سحب التكليف — لم
يحضر» الذي يزيد `abandonedJobs` على المنفّذ — ويراه كلُّ إمامٍ بعده في بطاقة
اهتمامه. فالإمام مدعوٌّ إلى الحكم بالغياب وهو لا يعرف كم غاب: إمّا سحبٌ من قادمٍ
غداً يُوسَم به بلا حقّ، وإمّا انتظارُ من انصرف ولا يعود، والمسجد ينتظر معه.

وقد كُتب `releaseAssignment` أصلاً لعلاج «منفّذٌ لا يحضر يترك الطلب معلّقاً في
`assigned` **بلا أجل**». **فتُح البابُ ولم تُعطَ البيّنة التي يُدخَل بها.**

**الإصلاح:**
- `listRequests` تُعيد `assignedAt` و`startedAt` — الحقلان قابلان للقراءة من
  العميل أصلاً، فلم يلزم تغييرٌ على الخادم. **كان العطب في الطريق لا في المصدر.**
- `app/src/time.js`: `daysSince` و`sinceLabel`. والصياغة عربيةٌ مضبوطة — «منذ
  يومين» لا «منذ 2 أيام»، و«منذ 11 يوماً» لا «منذ 11 أيام». وقد هربتُ من هذا
  الإعراب من قبل في رسائل `coord-trust` إلى صيغة «التسمية: العدد» المحايدة؛
  ولا مهرب هنا لأنها تُقرأ داخل جملة.
- شاشة الإمام تعرض المدّة، وبعد سبعة أيام تتبدّل الصيغة إلى تنبيه. **والرقم
  تقديريّ** يُراجَع بعد أول موسم — كحدّي الاهتمامات والتكليفات — **ولا يسحب
  تكليفاً من تلقائه**: الغياب مرّةً له أسبابه، والقرار للإمام كما هو في سائر
  المنصّة.
- **والمنفّذ يرى المدّة نفسها في «مهامّي».** من يُقيَّد عليه الغياب أولى الناس
  بأن يعلم كم مضى قبل أن يُقيَّد عليه.
- وسقطت جملةٌ كانت تُعرض على `assigned` أيضاً: «العمل جارٍ — يُبلّغك المنفّذ عند
  الإنجاز» — وهي تناقض ما فوقها، إذ الشكوى في `assigned` أن العمل **لم** يبدأ.

**والاختبار في متصفّح حقيقي**، لأن العطب لم يكن في الخادم: يُكلَّف منفّذ، ويُرجَع
`assignedAt` اثني عشر يوماً بالمفتاح الرئيس، فتُقرأ شاشة الإمام وشاشة المنفّذ.
وحارسٌ ماسحٌ للمصدر يمنع رجوع الوصلة، **وقد جُرِّب على نسخة `HEAD` قبل الإصلاح
فسقط ثلاث مرات** — حارسٌ لم يُرَ ساقطاً ليس حارساً.

**والدرس:** المسح الآليّ يدلّ ولا يحكم — ستةٌ من سبعة كانت قصداً أو خطأ قراءة،
والسابع عطبٌ حقيقي. **ولو صُدّق المسح لأُفسد ستّة، ولو أُهمل لبقي السابع.**
وأعمّ منه: **حين يُطلب من إنسانٍ حكمٌ على إنسان، فالبيّنة جزءٌ من الطلب لا
تحسينٌ عليه** — وقد كانت مكتوبةً في القاعدة منذ أول يوم، وما نقص إلا أن تصل
العين.

---

## 🔴 شركةٌ يُسحب اعتمادها وهي تعمل في المسجد — ولا أحد يعلم

**السياق:** المسح في الاتجاهين انتهى بي إلى قيدين في سجلّ التدقيق يُكتبان بلا
`mosqueId`. و`getMosqueAuditTrail` — القارئ الوحيد — تستعلم بالمسجد. **فقيدٌ بلا
مسجد لا يبلغ عيناً أبداً، ثم يحذفه التقليم بعد 180 يوماً.** وتتبُّع أحدهما
(`contractor_reviewed`) قاد إلى ما هو أكبر منه.

**العَرَض:** `isVerifiedContractor` يُفحص في `assignWorker` وحدها. فإن سحب
المشرف اعتماد شركةٍ — لسجلٍّ تجاريٍّ انتهى أو شكوى — وهي مكلَّفةٌ بأعمال:

- **إمام المسجد لا يُخبَر.** والعمل يجري في مسجده، وهو من يتحمّل نتيجته.
- **الشركة تبدأ عملها الجديد كما كانت** — `startWork` لا يسأل عن الاعتماد.
- **المشرف لا يعرف ماذا فعل**: لا يُعاد إليه أنّ سحبه مسّ ثلاثة أعمالٍ قائمة.
- **وشاشة الشركة تناقض نفسها**: «حسابكم بانتظار اعتماد الإدارة» — وتحتها
  الأعمالُ المكلَّفة معروضة. **وهو انتظارٌ لا يأتي: قرارُه قد صدر.**
- **وتعود إلى طابور المنتظرين** في شاشة المشرف كأنها لم تُراجَع قطّ، فيعتمد
  اليوم من سحب اعتماده أمسِ وهو لا يدري.

والأصل واحد: `isVerifiedContractor` بوليانٌ بحالتين، والواقع ثلاث — معتمدة، ولم
تُراجَع بعد، وسُحب اعتمادها. **وما لا يُمثَّل في البيانات لا يُقال للناس.**

**الإصلاح:**
- `contractorReviewedAt` يميّز من رُوجع فسُحب ممّن لم يُراجَع، ويُشتقّ منه
  `contractorStatus` على الخادم — الشرط واحدٌ فليكن في موضعٍ واحد. والحقل
  **يُقرأ في ثلاثة مواضع** كُتب من أجلها: شاشة الشركة، وبطاقة «حسابي»، وطابور
  المشرف. (وقد كتبتُ في القسم السابق أن حقلاً يُكتب ولا يُقرأ نيّةٌ لا سياسة —
  فلا يُضاف حقلٌ إلا وقارئه معه.)
- `reviewContractor` عند السحب: يجمع الأعمال القائمة (`assigned`،
  `in_progress`، `pending_imam_approval`)، **ويُشعر إمام كلّ مسجد** باسم الشركة
  والعمل، **ويُقيّد على المسجد** `contractor_suspended` فيظهر في سجلّه، ويُعيد
  `affectedRequests` إلى المشرف.
- `startWork` يفحص الاعتماد ثانيةً. **ولا يُمنع `markWorkDone`**: عملٌ بدأ في
  المسجد فعلاً، ومنعُ الإبلاغ عنه يترك الطلب معلّقاً بلا صورةٍ ولا ملاحظة
  ويُضيّع على الإمام معاينة ما أُنجز. **يُمنع الابتداء ولا يُقطع الطريق على
  البيّنة.**
- **ولا يُسحب التكليف تلقائياً.** عملٌ قد يكون نصفَ منجَز، وإسقاطه بلا معاينة
  يُضيّع جهداً بُذل. تُعطى البيّنة ويبقى القرار للإمام — وله `releaseAssignment`
  قبل بدء التنفيذ، وهو الطريق الذي فُتح له من قبل.

**والقيد الثاني — `interest_withdrawn` — عُولج معه.** نظيرُه
`interest_expressed` يُقيَّد بمسجده وهو لا يُقيَّد. **فالسجلّ يُظهر كلّ من سجّل
اهتمامه ويُخفي من انصرف:** يقرأ الإمام خمسة اهتمامات ولا يرى أن أربعةً سُحبت.
عددٌ صحيحٌ في كل سطرٍ منه، وخبرٌ كاذبٌ في مجموعه — **وهو أخطر من سطرٍ ناقص،
لأنه يُقرأ ولا يُشكّ فيه.**

**والاختبار كُتب أوّلاً وشُغّل قبل الإصلاح فسقط ثمانيَ مرات من تسع** — التاسعة
هي «تُبلّغ بما بدأته»، ونجاحُها قبل الإصلاح وبعده هو المقصود: **حدٌّ يُثبت أن
المنع لم يتجاوز موضعه.**

**والدرس:** شرطٌ يُفحص عند الدخول ثم يُنسى ليس شرطاً — وهذه ثالثة ثلاث في هذا
المستودع بعد الحساب الموقوف والجلسة الزائلة. وأزيدُ عليها ما لم يكن في
سابقتيها: **سحبُ صفةٍ يجب أن يبلغ من يتحمّل نتيجتها، لا من سُحبت منه وحده.**

---

## 🔴 المسجد مربوطٌ بأوّل من سجّله إلى الأبد

**العَرَض:** `claimMosque` تردّ كلّ طلبٍ على مسجدٍ مسجَّل — «هذا المسجد مسجّل
باسم غيرك بالفعل» — ولا دالةَ في المستودع كلّه تُغيّر `imamId` بعد ذلك. والأئمّة
يُنقلون ويتقاعدون ويموتون، ومنهم من يُوقَف حسابه لإساءة. وحين يقع ذلك:

- **المسجد يتجمّد:** لا طلب صيانةٍ جديد، ولا اعتماد لعملٍ أُنجز
- **ومنفّذٌ أتمّ عملَه** يبقى بلا اعتمادٍ ولا تقييمٍ ولا عدٍّ في سجلّه
- **وخَلَفُه لا يستطيع تسجيله** — يُردّ عند الباب

**فعقوبةُ إمامٍ مسيء تقع على جماعة المسجد**، ولا مخرج إلا تعديلُ السجلّ يدوياً
من لوحة Back4app: بلا أثرٍ، ولا مراجعة، ولا علمٍ للإمام المنزوع.

وهذا ليس فرضياً: بُني في هذا المستودع إيقافُ الحساب أداةً بيد الإدارة، ثم
جُعل `beforeLogin` يردّ الموقوف — **فصار كلُّ إيقافٍ لإمامٍ تجميداً لمسجده.**
أنشأتُ الأداة ولم أرَ أثرها.

**والمفاجئ أن آلة النقل كانت موجودة:** `reviewMosqueClaim` تكتب `imamId` عند
الاعتماد أصلاً. الناقص أن يُقبل الطلب، وأن يعرف المشرف أنه نقل. **قفلٌ على
بابٍ مفتوح.**

**الإصلاح — بمسار الطلب نفسه لا بمسارٍ جديد:**
- طلبٌ على مسجدٍ مسجَّل يُقبل بوصفه **طلب نقل**، ويُردّ على صاحبه إن كان هو
  إمامَه («مسجّل باسمك بالفعل»).
- `listPendingClaims` تقول للمشرف إنه نقل، **ومعه اسم من يُنزع منه وهاتفه**.
  والشاشة تعرضه في كتلة `error` فوق زرّ «اعتماد الملكية»: **الضغطة واحدة في
  الحالتين، والأثر ليس واحداً.**
- الاعتماد يُشعر الإمام السابق، ويُقيّد `mosque_transferred` في سجلّ المسجد —
  **بلا `note` وبلا أسماء**: السجلّ يقرؤه كل مستخدم، و`getMosqueAuditTrail`
  تُعيد الدور دون الهوية قصداً، فلا تُدخَل الأسماء من الباب الخلفي.
- **وحدٌّ ثلاثةُ طلباتٍ معلّقة للطالب الواحد.** فتحُ المسجَّل للطلبات يجعل
  الثمانية عشر ألفاً كلَّها قابلةً للمنازعة، والمشرف وحده هو الحاجز.

**وما كشفه المتصفّح ولم يكشفه الخادم:** فُتح الخادمُ للنقل، **والشاشة لم تكن
تعرض زرّاً على المسجد المسجَّل أصلاً** — `{!mosque.isClaimed && …}`. فطريقٌ
مفتوحٌ لا يبلغه أحد. وهذا ثالثُ عطبٍ من هذا الصنف يرصده اختبار المتصفّح وحده:
حقلٌ يصل ولا يُعرض، ونسبةٌ تُذكر في الكود ولا تُرى، وباب يُفتح بلا مقبض.

**وشاشةُ المشرف لم يكن لها اختبارُ متصفّحٍ قطّ** حتى اليوم — وفيها أخطرُ زرٍّ
في المنصّة. أُضيف مسارُها كاملاً: ترقيةٌ بالمفتاح الرئيس، ثم **جلسةٌ جديدة**
لأن الدور يُقرأ من تخزين المتصفّح لا من الخادم، فلا تظهر الترقية بإعادة تحميل.

**وسلكُ التعثّر شُدّ معها:** `include('mosqueId.imamId')` مسارٌ منقوط تفكّه Parse
إلى استعلامٍ تالٍ، واسمُ التابع فيه `include` نفسه — **فمسحُ المحمولية بالاسم
يمرّ تحته صامتاً.** أُضيف فحصٌ للمسارات المنقوطة كلٌّ على حدة. **وسلكُ تعثّرٍ
يمرّ تحته ما لم يُراجَع ليس سلكاً.**

**والدرس:** حين تُبنى أداةٌ تُقيّد إنساناً — إيقافُ حساب، سحبُ اعتماد — فالسؤال
ليس «هل تعمل؟» بل **«على من يقع أثرها ممّن لم يُذنب؟»**. ثلاث مرّات في هذا
المستودع كان الجواب: على غير المقصود بها.

---

## 🟠 أخطرُ لحظةٍ في عمر المنصّة، وأضعفُ أداةٍ بيد من يعيشها

**كيف وُجد:** سُئلتُ «هل البرنامج جاهز للإطلاق؟» فأجبتُ أن أكبر خطرٍ باقٍ هو
أن الكود لم يعمل على MongoDB يوماً، وأن **أوّل نشرٍ هو أوّل تشغيل**. ثم نظرتُ
فيما بيد المُشغّل عند تلك اللحظة بالضبط، فإذا هو `health`:

```js
Parse.Cloud.define('health', async () => ({ ok: true, version: '1.0.0', … }));
```

**لا تلمس القاعدة.** تُثبت أن الملفّ قُرئ ولا تُثبت شيئاً غيره. و`docs/DEPLOY.md`
كان يقول: «ارفع كود السحابة، **ثم تأكّد**» ويضع تحتها نداءها. فبين أوّل نشرٍ
وأوّل إمامٍ يستعمل المنصّة **لا شيء يخبر المُشغّل أن استعلاماً لا يعمل**، ولا
أن المخطط لم يُطبَّق، ولا أنه بلا مشرف فالطلبات تتراكم بلا اعتماد.

**الإصلاح:** `preflight` — بالمفتاح الرئيسي، تُشغَّل مرّةً بعد النشر
(`npm run preflight`). تُجري الصيغ التي يختلف فيها المحوّلان على **القاعدة
الحيّة**: `containsAll` على مصفوفة (وهي التي كشفت أوّل فرق)، وصندوق الإحاطة،
و`startsWith`، و`containedIn` والعدّ، والترتيب والتحميل المرافق ومساره المنقوط.
وثلاثة قيود في تصميمها:

1. **لا ترمي.** كلُّ فحصٍ يُلتقط خطؤه ويُعاد على حدة — فحصٌ يسقط عند أوّل خطأ
   يُخفي ما بعده، ويُعيد المُشغّل إلى الظلام الذي جاء يُخرجه منه.
2. **تقول ما لم تفحصه** (`unverifiable`): جدولة المهام، والفهرس الفريد، ورفع
   الملفات — لا تُقرأ من داخل Cloud Code. **وأخطر ما في تقريرٍ أخضر أن يُقرأ
   ضماناً وهو ليس ضماناً.**
3. **أهمّ اختبارٍ لها هو سلوكها على خادمٍ معطوب** — على خادمٍ بلا مخطط، وهي
   أوّل لحظةٍ تُنادى فيها فعلاً.

### وبناؤها كشف عطباً لم أكن أبحث عنه

`new Parse.Query('Mosques').count()` أعادت **صفراً** بينما `find()` على
الاستعلام نفسه تُعيد ثلاثين. قِيس ذلك هنا لا نُقل عن أحد:

```
find().length        : 30      count()              : 0
exists(objectId)     : 30      hasLocation=false    : 5
```

أي قيدٍ واحد يُعيد العدّ إلى صوابه؛ وبلا قيدٍ يُعيد صفراً **بلا خطأ ولا تحذير**.
وأسوأ ما فيه أنه لا يسقط: فحصٌ يقول «لا مسجد في القاعدة» وفيها ثمانية عشر ألفاً
يُرسل المُشغّل يستورد ما هو مستورد، وحدٌّ يقرأ صفراً يسمح بما كان يمنعه.

**والأسوأ أن `count` لم تكن في قائمة الصيغ المُراجَعة أصلاً.** قائمةُ سطح
الاستعلام في `portability.test.js` مكتوبةٌ باليد، فالتوابع الطرفية (`find`،
`first`، `count`) كانت خارجها — **فما لا تعرفه القائمة لا يراه المسح.** أُضيفت
الثلاثة بملاحظاتها، ومعها سلكُ تعثّرٍ يرفض `count` بلا قيد في كود السحابة.

### وحارسان كشفهما هذا العمل لا التخطيط له

- **ملفّ سحابةٍ جديد خارج الحزمة.** قائمةُ ملفات `build_single_file.py`
  مكتوبةٌ باليد، و`main.bundle.js` هو ما يُلصق في لوحة Back4app. فملفٌّ يُضاف
  إلى `main.js` ولا يُضاف إليها **يعمل في كل اختباراتنا ويغيب عن الإنتاج
  وحده** — وهو أسوأ أنواع الغياب. وقع بي حرفياً مع `preflight.js`. أُضيف حارسٌ
  يقابل استيرادات `main.js` بقائمة المولّد، **وجُرّب ساقطاً قبل قبوله.**
- **نداءٌ من الواجهة إلى دالةٍ لا وجود لها.** يسقط في وجه المستخدم برسالة
  `Invalid function`، لا في بناءٍ ولا في فحص. وقع بي في هذه الجلسة
  (`listPendingMosqueClaims` بدل `listPendingClaims`) ولم يكشفه إلا خادمٌ يعمل.
  أُضيف حارسٌ يقابل نداءات `api.js` بالدوال المسجَّلة. (والمسح اليوم يقول إن
  الوصلة سليمة كلُّها.)

**والدرس:** الأداةُ تُقاس بأشدّ لحظةٍ تُستعمل فيها لا بأهونها. و`health` كانت
تُقاس بالمراقبة الدوريّة — حيث يكفي «حيّ» — وتُستعمل في لحظة النشر الأولى حيث
لا يكفي. **ولمّا صنعتُ الأداة التي تليق بتلك اللحظة، دلّتني في أوّل تشغيلٍ لها
على عطبٍ في العدّ لم يكن أحدٌ يبحث عنه.**

---

## 🟠 رقمٌ فوق زرّ كان يُنفق ضعفَي الباقة الشهرية

**كيف وُجد:** بالقياس لا بالقراءة. `CLAUDE.md` يقول منذ زمن إن **الطلبات هي
القيد الملزم لا المساحة** (25 ألفاً شهرياً على باقة Back4app المجانية) —
ولم يُقَس قطُّ ما تكلّفه جلسةٌ عادية. فعُدّت طلبات Parse في متصفّحٍ حقيقي:

| الفعل | الطلبات |
|---|---|
| التسجيل حتى ظهور التبويبات | 5 |
| تبويب «حولي» | 3 |
| تبويب «مهامّي» | 4 |
| **تبويب «التنبيهات»** | **2** |
| تبويب «حسابي» | 2 |
| تبويب «الفرص» | 4 |

**وفي كلٍّ منها طلبٌ واحد للشارة.** `useUnread` كانت تُطلق
`getMyNotifications(1)` مع **كل تغيّر تبويب**. وعلى تبويب «التنبيهات» تظهر
العلّة عاريةً: طلبان لشيءٍ واحد — الشاشة تجلب الوارد وتعرف العدد ضمناً،
ثم تجلبه الشارة مرّةً أخرى في الضغطة نفسها.

**والحساب:** مئةُ مستخدمٍ يضغط كلٌّ منهم عشرين تبويباً يومياً = **ستون ألف طلب
شهرياً**، ضعفا الباقة كلِّها — **لرقمٍ فوق زرّ**. وحتى عشرون مستخدماً بعشر
ضغطات ينفقون ستة آلاف: رُبع الباقة على زينة.

**الإصلاح:** `app/src/unread.js` — العدد يُحفظ مع لحظة جلبه:

- يُقرأ المحفوظ ما دام طرياً (دقيقة)، فلا طلب مع كل ضغطة
- **والشاشاتُ تنشر ما تعرفه**: شاشة التنبيهات تنشر العدد الذي جلبته أصلاً،
  والتعليم مقروءاً ينشر صفراً — فتتحدّث الشارة **بلا طلبٍ واحد**
- والطراوة تنتهي فلا يتجمّد الرقم كذبةً بعد أوّل تنبيه
- **ويُنسى المحفوظ عند الخروج وعند زوال الجلسة:** الجهاز يستعمله أكثر من
  واحد، وشارةُ من مضى فوق واردِ من أتى **رقمٌ ليس له، بل خبرٌ عن غيره.**

**والقياس صار حارساً:** اختبار المتصفّح يعدّ نداءات `getMyNotifications`
فعلياً. **جُرّب على النسخة السابقة فسقط بـ«الشارة أنفقت 5 طلبات في خمس
ضغطات»** — والآن واحد.

**والدرس:** حدٌّ مكتوبٌ في وثيقةٍ ليس حدّاً مرصوداً. «الطلبات هي القيد الملزم»
كانت جملةً صحيحة في `CLAUDE.md` منذ أشهر، ولم يمنع صدقُها أن يُنفق أضعافُها
على زينة — **لأن أحداً لم يعُدّ.** والفرق بين قاعدةٍ مكتوبة وقاعدةٍ نافذة هو
وجود ما يقيسها.

**وطريقٌ لم يُسلَك في هذه الدورة:** حلقةُ التقليم في `maintenance.js` تكسر عند
`batch.length < PRUNE_BATCH`، وهو يفترض أن الخادم يُعيد ما طُلب بالضبط. قِيس
هنا: `limit(500)` تُعيد 500 من أصل 700، فالافتراض صادقٌ على محوّلنا. ولو
ضبط Back4app سقفاً أدنى لتوقّف التقليم عند الصفحة الأولى صامتاً. **لم يُغيَّر
شيء: عطبٌ مشروطٌ بإعدادٍ لم يُقَس ليس عطباً مرصوداً** — ويُذكر هنا لأن من يقرأ
لاحقاً أولى أن يجد القياس مكتوباً من أن يُعيده.

---

## 🟠 قائمةٌ تُجلب لتُرمى، وملفٌّ يُجلب ولا يُقرأ

**كيف وُجد:** الدورة السابقة عدّت الطلبات **كم**. وهذه عدّت **أيّها** — أيُّ
نداءٍ يقع في كل تبويب. والفرق بين السؤالين هو الفرق بين «الكلفة مقبولة»
و«الكلفة في غير موضعها»:

```
متطوّع:
  مهامّي : getMyProfile | getMyInterests | classes/ServiceRequests
  الفرص  : classes/ServiceRequests | updateMyLocation | getNearbyOpportunities
  حولي   : updateMyLocation | getNearbyMosques
```

**عطبان ظهرا في سطرين:**

**(١) «الفرص» تجلب القائمة مرّتين.** `useLocation` تبدأ بـ`loading: true`
و`point: null`. والشاشة تقرأ `Boolean(location.point)` فتفهم «تعذّر الموقع»
وهي في الحقيقة «لم يُحسم بعد» — فتجلب **فرص السلطنة كلَّها** (وهي الأثقل، إذ
لا يحدّها موقع) ثم تُرميها بعد ثانية حين يصل الموقع.

**والمستخدم يرى «جارٍ تحديد موقعك…» وتحتها قائمةٌ كاملة تُبدَّل تحت عينه.**

و«حولي» لا تفعل ذلك — تنتظر بـ`LocationGate`. لكنّ الانتظار وحده لا يصلح هنا:
**تعذُّر الموقع ليس مانعاً من العمل** في هذه الشاشة، بل يُعرض حينها كلُّ مفتوح.
فالناقص لم يكن انتظاراً بل **تمييز «لم يُحسم» من «حُسم بالتعذّر»** — وهما في
`Boolean(point)` سواء.

**(٢) «مهامّي» تجلب ملفَّ المتطوّع ولا تقرأ منه شيئاً.** لا يُقرأ منه إلا
`contractorStatus`، وهو `null` للمتطوّع دائماً — **وهم أكثر المستخدمين عدداً.**
والدورُ معروفٌ في العميل بلا نداء.

**الأثر:** جلسةُ متطوّعٍ تمرّ بالتبويبين تنزل من ستة نداءات إلى أربعة —
**الثلث.** وعلى باقةٍ حدُّها الملزم الطلبات لا المساحة، هذا ليس تحسيناً تجميلياً.

**والقياس صار حارسين:** «صفر `classes/ServiceRequests` في زيارة الفرص»،
و«صفر `getMyProfile` في مهامّي المتطوّع». **جُرّب كلٌّ منهما على النسخة
السابقة فسقط برسالته**، كلٌّ على حدة.

**والدرس:** «كم» و«أيّها» سؤالان لا سؤال. الأوّل أعطاني رقماً مقبولاً في
ظاهره — أربعة نداءات لتبويب — والثاني أرى أن أحدها يُجلب ليُرمى وآخرَ لا
يُقرأ. **والعدُّ وحده يُطمئن حيث ينبغي أن يُقلق.**

---

## 🔴 حدٌّ مكتوبٌ على بابٍ واحد، والباب الآخر مفتوح

**كيف وُجد:** الدورة بدأت بقياس **حجم** الحمولات — بعد أن قِيس عددُها وأيُّها.
والنتيجة: لا شيء. أكبر شاشة 6.6 كيلوبايت على بيانات وزارةٍ حقيقية. **فلم
يُختلق عطبٌ حيث لم يجد القياس واحداً**، وانتقل السؤال إلى المدخلات: أيُّ نصٍّ
يكتبه العميل بلا حدّ؟

مسحُ `cloud/` أظهر أن كل كتابةٍ مارّةٍ بدالة سحابة مقصوصة: `title` 120،
و`description` 2000، و`evidenceNote` 500، و`workerNotes` 1000، و`fullName` 80.
**ثم التسجيل.** `api.js#signUp` لا يمرّ بدالة سحابة أصلاً — يكتب على `_User`
مباشرةً بـ`user.signUp()`، و`beforeSave` يحرس الدور والاعتماد ولا يحرس طولاً.

**قِيس على خادمٍ حقيقي:**

```
حُفظ. طول الاسم: 200000 — الهاتف: 200000 — السجل: 200000
عبر الدالة — الاسم: 80 — الهاتف: 20
```

**الحدُّ معلومٌ ومكتوب، ولا يُطبَّق على الباب الذي يدخل منه كلُّ مستخدمٍ جديد.**

**والأثر ثلاثة:**

1. **قاعدةٌ سعتها 250 ميغابايت** يملؤها بضع مئات من التسجيلات — والتسجيل
   مفتوحٌ لغير المصادَق، **فالكلفة صفرٌ على فاعله.**
2. **`fullName` يُعرض للإمام** في بطاقة المهتمّ وهو يختار المنفّذ — واسمٌ من
   مئتي ألف حرفٍ يكسر الشاشة التي يُتّخذ فيها القرار.
3. وثالثٌ لم يكن في الحسبان: **بابٌ ثالث** — `_User` مفتوحةٌ لصاحبها، فيستطيع
   بعد الدخول أن يحفظ عليها مباشرةً بلا دالةٍ ولا تسجيل.

**الإصلاح — في `beforeSave` لا في الدوال:** `TEXT_LIMITS` و`clampUserText` في
`lib/auth.js`، **مصدرٌ واحد** تقرؤه `updateMyProfile` والمُشغّل معاً. و`beforeSave`
يمرّ به كلُّ كتابة: تسجيلاً كانت أو تحديثاً أو حفظاً مباشراً — **الأبواب
الثلاثة بحدٍّ واحد.** والقصُّ لا الرفض: من لصق اسمه بمسافاتٍ حوله لا يُردّ
تسجيلُه، ولا يُحفظ الفراغ معه.

**واختُبرت الأبواب الثلاثة كلٌّ على حدة**، وجُرّب الملفّ على النسخة السابقة
فسقط في أربعةٍ من ستّة.

**والدرس:** رقمان صحيحان في موضعين لا يصنعان حدّاً — **الحدُّ هو أن يمرّ كلُّ
داخلٍ من موضعٍ يُطبَّق فيه.** وقد كتبتُ `80` في `updateMyProfile` وأنا أحسبني
حددتُ الاسم، والحقيقة أنّي حددتُ **تعديلَه** لا **إنشاءه**. وهذا ثالث عطبٍ من
صنف «حارسٌ على بابٍ دون باب» في هذا المستودع: الحساب الموقوف، واعتماد الشركة،
وهذا. **فليُسأل عن كل حارس: كم باباً لهذه الغرفة؟**

---

## 🔴 السمعة التي يقرؤها الإمام كان يكتبها المنفّذ

**كيف وُجد:** بتطبيق درس الدورة السابقة — «كم باباً لهذه الغرفة؟» — مسحاً لا
تخميناً. جُرّبت **كل** كتابةٍ ممكنة من جلسة مستخدمٍ عاديّ على خادمٍ حقيقي:

```
الفئات السبع كلُّها       : رُدّ (Permission denied)
role                     : رُدّ    isVerifiedContractor : رُدّ
completedJobs            : قُبل → 999
avgRating                : قُبل → 5
abandonedJobs            : قُبل → 0
isActive                 : قُبل → false
contractorReviewedAt     : قُبل    lastLat : قُبل
```

**الفئات كلُّها محصَّنة، و`_User` مفتوحة.** والحارس في `beforeSave` كان يعرف
حقلين فقط — `role` و`isVerifiedContractor` — وما عداهما بلا حارس.

**وأخطر الثلاثة الأولى أنها ليست حقولاً داخلية:** هي بعينها ما تُعيده
`getRequestInterests` للإمام وهو يختار المنفّذ. بُنيت لتكون **بيّنته**، فإذا
هي **إقرارٌ من صاحب الشأن على نفسه**. ومتطوّعٌ يكتب `completedJobs: 999`
و`avgRating: 5` يتصدّر كل قائمةٍ يظهر فيها.

**وأخصُّها `abandonedJobs`.** كُتب ليُعرض للإمام تحذيراً («تغيّب عن ٣ تكليفات
سابقة») مع تعليقٍ صريح: «يُعرض ليختار الإمام عن بيّنة». **وكان من تغيّب يمحوه
بسطرٍ واحد** — فالتحذير يختفي من أمام من قامت به الحاجة، ويبقى لمن لم يُذنب.

**الإصلاح:** `PLATFORM_FIELDS` في `beforeSave`، بنمط `isVerifiedContractor`
نفسه لأن ثلاثةً منها لها `defaultValue` — و`dirty()` وحده لا يصلح حارساً عليها:

- **الحساب الجديد يُفرَض** على قيمة المنصّة (صفرٌ للعدّادين، ولا تقييم، ونشِط)
- **والقديم يُردّ إن مُسّ** برسالة «هذا الحقل تكتبه المنصّة لا صاحب الحساب»

**والحارس ضيّقٌ قصداً:** الاسم والهاتف والمهارات والمحافظة تبقى لصاحبها، وقد
اختُبر ذلك صراحةً — **حارسٌ يمنع ما لا يجب أن يمنعه يُلتفّ عليه بالتعطيل.**
واختُبر معه أن المنصّة نفسها ما تزال تكتبها: رحلةُ عملٍ كاملة تنتهي بـ
`completedJobs = 1` و`avgRating = 4`، وتغيّبٌ يُقيَّد ثم تُردّ محاولةُ محوه.

**وجُرّب على النسخة السابقة فسقط في أربعةٍ من ستّة**، برسالة «سُجّل بتسعمئةٍ
وتسعةٍ وتسعين عملاً منجزاً».

**والدرس:** الحارس الذي يُكتب حقلاً حقلاً يحرس ما خطر ببال كاتبه. `role`
و`isVerifiedContractor` خطرا لأنهما «صلاحيات»؛ و`abandonedJobs` لم يخطر لأنه
«عدّاد». **والفرق بينهما في الذهن لا في الأثر:** كلاهما يُغيّر ما يُبنى عليه
قرارُ إنسان. **فالسؤال ليس «أهذا حقلُ صلاحية؟» بل «من يتضرّر إن كذَب؟»**

---

## 🔴 ضغطتان في لحظةٍ واحدة — وإصلاحان لي سقطا قبل أن يقوم الثالث

**كيف وُجد:** مسحُ القراءة أوّلاً — ماذا يرى غريبٌ عن حساب غيره؟ **لا شيء:**
الـACL يقصر `_User` على صاحبها، والفئات الحسّاسة مردودة. **فلم يُختلق عطب**،
وانتقل السؤال إلى التزامن: ماذا يقع إن ضُغط الزرّ مرّتين قبل أن يردّ الأوّل؟

```
«يهمّني» مرّتين متوازيتين : نجحتا → اهتمامان مسجَّلان
تكليفان متوازيان         : نجحا → المكلَّف الثاني، وكلاهما أُخبر بأنه كُلِّف
اعتمادان متوازيان        : نجحا → completedJobs = 2 لعملٍ واحد
```

**والضغطة المتتالية محروسة** (قِيس: «الطلب ليس بانتظار الاعتماد») — فالعطب في
المتوازي وحده، وهو ما يقع على شبكةٍ بطيئة بزرٍّ لا يُعطَّل بين ضغطتين.

**وأخطرها الثاني والثالث:** متطوّعٌ يُخبَر بأنه كُلِّف وليس هو المكلَّف، **فيسافر
إلى المسجد وليس له فيه عمل**. وعملٌ واحد يُعدّ إنجازين — وهي السمعة نفسها التي
حُصّنت في الدورة السابقة من أن يكتبها صاحبها، **فإذا هي تُنفَخ بضغطة.**

### وسقط لي إصلاحان قبل أن يقوم الثالث

**الأوّل:** حارسُ تحوّلٍ في `beforeSave('ServiceRequests')` يقرأ
`request.original`. بُني على أن المُشغّل يرى الصفَّ المخزَّن عند الحفظ. **وأُعيد
القياس بعده فلم يتغيّر شيء**: المتوازيان يقرآن الحالة قبل أن يكتب أحدهما، فكِلا
`original` هو ما قبلهما.

**والثاني — وهو أسوأ:** بعد سقوطه أبقيتُ منه قاعدة «المُقفَل لا يُمَسّ» بوصفها
حصانةً عامّة. ثم تبيّن بالقراءة أن `payoutContractor` يكتب `isPaidOut` على طلبٍ
**منجَز**: فالحارس **يقطع الصرف**. ولم تكشفه الاختبارات لأن بديل Parse لا
يُنفّذ `request.original` أصلاً، ومسارُ التبرعات معطّلٌ بقرار — **فكان العطب
سيظهر بعد أشهر، عند تفعيل التبرعات وحدها.**

**فحُذف الحارسان.** لا يمنعان ما قِيس، ولا يُمكن إسقاطُهما باختبارٍ لأن لا
مسارَ يبلغهما، وأحدهما يكسر ما يعمل. **وما لا يُرى ساقطاً ولا يُصلح شيئاً لا
يُترك في الطريق.**

### والعلاج حيث وقع الأثر

**(١) السمعة تُشتقّ لا تُزاد.** كان `increment('completedJobs')` ومتوسطاً
تراكمياً، ومكتوبٌ فوقه أن اعتمادين متزامنين «نادران وأثرهما تقييم منحرف
قليلاً» — **وقد كُذّب هذا بالقياس.** صارت تُحسب من مصدرها: الطلبات المنجَزة
المسنَدة إليه. فاعتمادٌ مرّتين لطلبٍ واحد يعطي واحداً **لأن الطلب واحد**.

وهو نمط `openRequestsCount` نفسه — يُحسب باستعلامٍ لا بعدّاد. **وفوق ذلك
يُصلح ما انحرف قبله:** أوّل اعتمادٍ بعده يُعيد أيَّ عدّادٍ منفوخ إلى صوابه،
واختُبر ذلك صراحةً بعدّادٍ زُرع على 99.

**(٢) لا يُبشَّر إلا من صار له التكليف.** `assignWorker` تقرأ الطلب **بعد**
الحفظ: من لم يكن هو صاحب التكليف يُردّ بخبرٍ صحيح («كُلِّف غيرك في هذه
اللحظة») ولا يُرسَل إليه شيء.

**(٣) وما بقي:** الاهتمام المزدوج — اهتمامان مسجَّلان من ضغطتين متوازيتين.
أثرُه أهون (يظهر المتطوّع مرّتين في قائمة الإمام، ويُستهلك حدُّه مرّتين)،
وعلاجُه التامّ فهرسٌ فريد على `requestId + volunteerId` **لا يعبّر عنه مخطط
Parse** — فيُضاف يدوياً من لوحة Back4app، كنظيره على `externalId`. مُقيسٌ
ومكتوبٌ ولم يُدَّعَ إصلاحه.

**والدرس:** حين يُكذّب القياسُ إصلاحاً، فالخطر ليس في سقوطه بل في **ما يُنتشل
من حطامه**. أسقط القياسُ حارسي، فأبقيتُ منه «قاعدةً عامّة» بلا قياسٍ جديد —
وكادت تقطع الصرف. **والإصلاح الساقط يُحذف كلُّه، ولا يُورَّث منه شيء.**

---

## 🟠 صورٌ تُرفع مرّتين، وأداتا قياسٍ كذبتا عليّ

**كيف وُجد:** بقياس ما يبقى في التخزين إن سقط الإبلاغ بعد رفع الصور.
`ReportWork` ترفع الملفات **ثم** تنادي `markWorkDone`. فماذا لو سقط النداء؟

```
ملفات قبل الرفع: 0   ·   بعد رفع ثلاث صور: 3
الإبلاغ: رُدّ        ·   بعد سقوط الإبلاغ: 3  ← ثلاثٌ يتيمة
```

**ثلاثة ملفات لا يشير إليها شيء، ولا يحذفها شيء** — ولا سبيل إلى كنسها: Parse
لا يعرض واجهةً لسرد الملفات، فلا يُكتب لها مهمّةٌ دورية كما كُتب لسجلّ التدقيق
والوارد. **والتخزين والبيانات على باقةٍ واحدة سعتها 250 ميغابايت.**

**والأسوأ إعادةُ المحاولة.** المنفّذ يُبلغ من داخل مسجدٍ بشبكةٍ ضعيفة: يضغط
فيسقط، فيضغط — وكان يُعيد رفع الصور كلَّها من جديد. **ستُّ صورٍ في التخزين
لطلبٍ واحد، ثم تسع.** ويدفع هو ثمن باقته في كل مرّة.

**الإصلاح:** ما رُفع لا يُرفع مرّتين — `useRef` يحفظ رابط كلّ ملفٍّ رُفع،
فالمحاولة الثانية تستعمله. واختيارٌ جديد للملفات يُبطل المحفوظ.
**وما يُتِمّه هذا هو ألّا تُزاد اليتامى؛ أمّا القائم منها فلا يُكنَس.**

### وأداتا قياسٍ كذبتا عليّ في هذه الدورة وحدها

**الأولى:** عدّاد الملفات وُجّه إلى مجلّدٍ خاطئ، فطبع «0 قبل، 0 بعد» —
**وهي قراءةٌ تُقرأ «لا يتامى»**، وهي في الحقيقة «لا أرى شيئاً». ولولا أن
الرقم كان صفراً **قبل** الرفع أيضاً — وهو ما لا يستقيم — لمرّت.

**والثانية أخطر:** الاختبار كان ينتظر نصّ «بانتظار معاينة الإمام» بعد إعادة
المحاولة. **والنصُّ معروضٌ أصلاً على بطاقةٍ أخرى** في الشاشة نفسها، فمرّ
الانتظار قبل أن يقع شيء. ثم قرأتُ الخادم فوجدتُ «صور مسجّلة: 0» فاستنتجتُ
عطباً جسيماً: «الإبلاغ يمرّ بلا صورة، والإمام يعتمد عملاً لا يراه».

**ولم يكن ذلك واقعاً.** كان الطلب لم يُبلَّغ بعدُ حين قرأتُه. ولمّا صار
الانتظار على اختفاء نموذج الإبلاغ نفسه، بلغت الصور الثلاث الخادمَ في الحالين.

**وهذه ثالثة ثلاث في هذا المستودع من صنفٍ واحد**: شرطٌ مركّبٌ من لحظتين،
وصورةٌ كسولة خارج الشاشة، وهذا. **والقاسم: انتظارٌ على شرطٍ يصدق لسببٍ آخر.**
فليكن الانتظار على **ما لا يصدق إلا بوقوع ما نريد** — واختفاءُ النموذج لا
يقع إلا بنجاح الإبلاغ، ونصُّ الحالة يقع بعشرة أسباب.

**والدرس:** أداةُ القياس تُقاس قبل أن يُقاس بها. وأخطرُ عطلٍ فيها ما يُنتج
رقماً **معقولاً** — «صفر يتامى» و«صفر صور» كلاهما يُصدَّق، وكلاهما كان كذباً.
**والصفر أشدُّ الأرقام إغراءً بالتصديق، وأقلُّها بيّنةً على نفسه.**

---

## 🟠 «سبق أن سجّلت اهتمامك» — على فعلٍ نجح

**كيف بدأت الدورة، وكيف انتهت إلى غير ما بدأت به.**

درسُ الدورة السابقة — «انتظارٌ على شرطٍ يصدق لسببٍ آخر» — وقع ثلاث مرّات في
هذا المستودع. فبدأتُ بمسح الانتظارات كلَّها بدل انتظار الرابعة، وبنيتُ عدّاداً
يطبع كلَّ انتظارٍ كان شرطُه صادقاً **لحظة استدعائه**. فخرج سبعة عشر.

**والرقم لا يعني ما أردتُه.** أن يصدق الشرط لحظةَ الانتظار هو الحال الطبيعية
بعد فعلٍ أُنتظر تمامُه. **فالأداة لا تُميّز «صدق بفعلنا» من «صدق بغيره»** —
وهو الفارق كلُّه. (وهذا ثاني عدّادٍ يعطيني رقماً معقولاً لا يقيس ما أظنّه، بعد
عدّاد الملفات في الدورة الماضية.)

فأُعيد ضبطُه على الأثر الصحيح: انتظارٌ يُطابق **أكثر من عنصر** — إذ لا يدلّ
حينها على واحدٍ بعينه. فخرجت ستّة، وقُرئت كلُّها: خمسةٌ منها تنتظر جمعاً مقصوداً
(بطاقاتٌ يُعدّها الاختبار بعدها صراحةً)، والسادس مسنودٌ بتأكيداتٍ خاصّة قبله.

**فلا انتظارَ آخر مكسور. ولم أُعِد هيكلة ما لم يثبت عطبُه.**

### ثم قِيس سببُ ما عولج أثرُه

الدورة الخامسة أثبتت ضرر النداءين المتوازيين وعالجت **أثرهما**. وهذه قاست
**سببهما** في متصفّح حقيقي: ضغطتان سريعتان على «يهمّني» — زرٌّ لا يُعطَّل:

```
قبل: نداءان · اهتمامٌ واحد · ويرى المتطوّع «سبق أن سجّلت اهتمامك بهذا الطلب»
بعد: نداءٌ واحد · اهتمامٌ واحد · «سُجّل اهتمامك، والإمام يختار المنفّذ»
```

**والخادم كان يعمل بحقّ**: ردَّ الثاني وسجّل واحداً. لكنّ رسالة الردّ **حلّت
محلّ رسالة النجاح** على الشاشة. فالمتطوّع سجّل اهتمامه فعلاً، ويُقال له إنه
«سبق أن سجّل» — **فيظنّ فعلَه فشل، أو يظنّ نفسه سجّل من قبل ولم يفعل.**

**الإصلاح:** `useAction()` — فعلٌ لا يقع مرّتين، وزرٌّ يُعطَّل حتى يردّ نداؤه.
و`useRef` لا `useState` وحده: ضغطتان في دورةٍ واحدة تقرآن الحالة القديمة
كلتاهما. وطُبِّق على كل زرٍّ ينادي الخادم — «كلّفه بالعمل» و«اعتماد العمل»
و«سحب التكليف» و«اعتماد الملكية» — وهي التي قِيس ضررُ تزامنها من قبل.

**والدرس:** أن يكون الخادم على حقّ لا يعني أن المستخدم رأى الحقّ. ردُّ النداء
الثاني كان صواباً تامّاً في القاعدة، **وكذباً تامّاً على الشاشة** — لأن آخر
رسالةٍ تصل هي التي تُصدَّق. **وحين يُعالَج الأثر في الخادم، يبقى السؤال: وماذا
يرى صاحب الشأن؟**

---

## 🔴 سجلّ المسجد قال «كُلّف منفّذ» مرّتين لتكليفٍ واحد — وأنا من أوقعه

**كيف وُجد:** بتطبيق درس الدورة السابقة على عملي أنا. الدرس كان: «عولج الأثر
في الخادم — فماذا يرى صاحب الشأن؟» وأوّل ما يستحقّ السؤال هو ما أدخلتُه في
الدورة الخامسة.

هناك عالجتُ التكليفين المتوازيين بأن **البشرى لا تُرسَل إلا لمن صار له
التكليف**. فقِيس السجلّ بعد ذلك:

```
النداءان        : rejected، fulfilled
المكلَّف فعلاً    : واحد
«كُلّف منفّذ بالعمل» في السجلّ : 2   (المرجوّ 1)
```

**أصلحتُ البشرى وتركتُ القيد.** ترتيبُ الدالة كان: احفظ ← قيِّد ← أقفِل
الاهتمامات ← **ثم** تحقّق ← بشِّر. فمن لم يقم تكليفُه كان يترك في سجلّ المسجد
سطراً يقول إنه كُلِّف.

**وسجلّ المسجد ليس سجلّاً داخلياً**: هو أداةُ الشفافية التي تقوم عليها المنصّة،
يقرؤه المصلّي والمتبرّع كما يقرؤه الإمام. **وقارئُه لا يُميّز تكليفاً قام من
تكليفٍ نُسخ فوقه بعد لحظة.**

**الإصلاح:** التحقّق يسبق **كلَّ** أثر لا البشرى وحدها — القيد، وإقفال
الاهتمامات، والإشعار. ومن لم يقم تكليفُه لا يترك في المسجد أثراً.

**والدرس:** حين يُصلَح أثرٌ من آثارٍ عدّة لسببٍ واحد، **فالإصلاح الجزئي يُخفي
البقيّة** — لأنه يُطمئن. ولولا أني سألتُ السؤال نفسه على عملي أنا لبقي السطر
الكاذب في السجلّ، وقد كتبتُ في الدورة نفسها أن العلاج «حيث وقع الأثر». **وقعت
الآثار ثلاثة، فعالجتُ واحداً وحسبتُها كلَّها.**

### وقِيس معه اثنان من الصنف نفسه — ولم يُصلَحا

```
«اعتمد الإمام العمل» بعد اعتمادين متوازيين : 2   (المرجوّ 1)
«أُلغي الطلب» بعد إلغاءين متوازيين          : 2   (المرجوّ 1)
```

**والفرق بينهما وبين التكليف فرقٌ في النوع لا في الدرجة:** سطرا التكليف كانا
**يذكران منفّذاً لم يُكلَّف** — خبرٌ كاذب عن إنسانٍ بعينه. أمّا هذان فتكرارُ
خبرٍ صادق: الإمام اعتمد فعلاً، مرّةً، وسُجّل مرّتين.

ولا يُميّز النداءان المتوازيان أيُّهما أحدث التحوّل: كلاهما يكتب `completed`،
فالقراءة بعد الحفظ لا تفرّق بينهما — بخلاف التكليف حيث المنفّذ هو المميِّز.
والفصلُ يحتاج علامةً فريدة لكل نداء تُكتب ثم تُقرأ. **ولم تُبنَ:** الأثر
تكرارُ صدقٍ لا كذب، ولم أشأ أن أُضيف آلةً لم يُثبت قدرُ ضررها أنها تستحقّها —
كما لم أبنِ فهرساً وهمياً للاهتمام المزدوج. **مُقيسٌ ومكتوبٌ ولم يُدَّعَ
إصلاحه.**

---

## 🔴 العدّاد الثالث: وَسْمٌ بغيابٍ لم يقع

**كيف وُجد:** بسؤال درسِ الدورة السابقة على إصلاحي أنا. الدرس كان: «الإصلاح
الجزئي يُخفي البقيّة لأنه يُطمئن». وفي الدورة الخامسة اشتققتُ `completedJobs`
و`avgRating` من الطلبات بدل `increment`. **فماذا عن العدّاد الثالث؟**

```
سحبان متوازيان بعذر الغياب → abandonedJobs = 2   (المرجوّ 1)
```

**تُرك على `increment`.** وهو أشدُّ الثلاثة أذىً: يُقرأ في موضعٍ واحد — بطاقةُ
المهتمّ التي يختار الإمام على أساسها — فيُعرض «تغيّب عن 3 تكليفات سابقة».
**فضغطةٌ زائدة على «سحب التكليف — لم يحضر» تَسِم متطوّعاً بغيابٍ لم يقع، ويراه
كلُّ إمامٍ بعده.**

وهو أذىً لإنسانٍ بعينه لا خطأ عدٍّ — من صنف السطر الذي كان يذكر منفّذاً لم
يُكلَّف، لا من صنف «اعتمد الإمام العمل» المكرّر.

**والاشتقاق هنا لم يكن كاشتقاق أخويه.** الإنجاز يُشتقّ من الطلبات المنجَزة —
سجلٌّ دائمٌ قائم. أمّا الغياب فلا سجلَّ له إلا العدّاد نفسه: سجلُّ التدقيق
يُقلَّم بعد 180 يوماً، وصفُّ الاهتمام لا يوجد للشركات، ولا يُفرّق بين من تغيّب
ومن اعتذر.

**فأُنشئ له سجلُّه:** `noShowBy` على الطلب، تُكتب بـ`addUnique` — **عمليةٌ
ذرّية**: السحبان المتوازيان يكتبان معرّفاً واحداً. ومنه يُشتقّ العدّ. ويُصلح
ما وُسِم به ظلماً من قبل — واختُبر بعدّادٍ زُرع على أربعين فعاد إلى اثنين.

**والانسحاب المُعلن لا يُقيَّد**، واختُبر صراحةً: التفريق بينه وبين الغياب هو
ما يجعل الاعتذار متاحاً أصلاً.

### وبديل Parse كان ينقصه `addUnique`

فسقط مسار السحب كلُّه في اختبارات الوحدة — لا لأن الإصلاح خاطئ، بل لأن البديل
لا يعرف العملية. **وهذا ما يُراد من البديل**: أن يسقط حين يُستعمل ما لا يعرفه،
لا أن يتظاهر. فأُضيفت إليه بسطورٍ أربعة.

**والدرس:** حين يُشتقّ عدّادان من ثلاثة تُعرض في بطاقةٍ واحدة، **فالثالث لا
يبقى على حاله بل يصير أسوأ**: صار محاطاً برقمين لا ينحرفان، فيُقرأ بثقتهما.
**والانحراف الوحيد بين أرقامٍ سليمة أشدُّ تضليلاً من الانحراف بين أمثاله.**

---

## 🟠 «File upload by public is disabled.» — لمتطوّعٍ واقفٍ في مسجد

المنصّة كلُّها عربية: كل نصٍّ في الواجهة، وكل رسالة خطأٍ تصوغها دوالُّ السحابة
في `cloud/lib/errors.js`. **إلّا أن Parse نفسه لا يتكلّم العربية**، ورسائله
تصل المستخدم في مسارات لا تمرّ بدوالنا أصلاً: الدخول، والتسجيل، ورفع الصور.

فكان في `api.js` جدولُ ترجمةٍ لسبعة أكواد، وتحته سطرٌ واحد يُبطل ما فوقه:

```js
return PARSE_MESSAGES[error.code] || error.message || 'تعذّر إتمام العملية…';
```

`error.message` هذه **نصُّ Parse الإنجليزي**. أي أن الجدول لم يكن ترجمةً بل
تحسيناً لسبع حالات، وما عداها يُعرض بالإنجليزية كما هو.

### وقِيس أيُّها أرجحُ وقوعاً

على خادمٍ حقيقي لم يُفعَّل فيه رفع الملفات:

```
الكود: 130
الرسالة: «File upload by public is disabled.»
أعربيّة؟ لا — تُعرض للمستخدم كما هي
```

وليست هذه حالةً نادرة: تفعيل `fileUpload.enableForAuthenticatedUser` **خطوةُ
نشرٍ يدوية** مكتوبة في `docs/DEPLOY.md`. والخطوة اليدوية تُنسى. فإن نُسيت، فكلُّ
متطوّعٍ يُنهي عملاً في مسجدٍ ويحاول إرفاق صورته يُصدّه سطرٌ إنجليزيّ **لا
يقرؤه، ولا يخبره أن بوسعه الإبلاغ بلا صور**. يقف عند الباب ولا يرى أن بجانبه
باباً مفتوحاً.

### والقاعدة التي حلّت محلّ الجدول

في `app/src/errors.js` — وحده، لا شيء يستورده، فيُختبر خارج المتصفّح:
`api.js` صار يُعيد تصديره لا يملكه.

- الجدول تُوسِّع بما قِيس وصولُه: `119` و`124` و`130`.
- ومن لا ترجمة له **لا يُعرض نصُّه**، بل: «تعذّر إتمام العملية (رمز 141).
  حاول مرة أخرى، وإن تكرّر فأبلغ الإدارة.» — الرمزُ يُعين الإدارة على التشخيص
  ولا يُربك القارئ، والنصُّ الإنجليزي لا يصله بحال.
- ورسالة 130 تحمل مخرجها معها: «…ويمكنك الإبلاغ بلا صور.»

### والاختبار كشف ما لم يكشفه القياس

`tests/messages.test.js` يسأل سؤالاً واحداً — **هل ما سيُعرض عربيّ؟** — لا
يقارن نصّاً بنصّ. وسقط على `HEAD` في خمسٍ من سبع، وفيها واحدةٌ لم أكن أبحث
عنها:

```
not ok — عُرضت بلا عربية: «File upload by public is disabled.»
not ok — عُرضت بلا عربية: «Cloud function failed.»
not ok — عُرضت بلا عربية: «Network request failed»
```

الثالثة `new Error('Network request failed')` — **بلا كودٍ أصلاً**. كنت أظنّ
التسرّب محصوراً في الأكواد التي لا ترجمة لها، فإذا بكل خطأٍ عاديّ من المتصفّح
يمرّ من الباب نفسه. **قِستُ باباً فوجدت الجدارَ كلَّه.**

**والدرس:** حين يُكتب الاختبار على **الخاصّية** المطلوبة (عربيّ) لا على
**الحالة** المقيسة (الكود 130)، صار يمسح ما لم يُقَس. والفرق بينهما أن الأول
يسأل «ما الذي لا أقبله؟» والثاني «ما الذي وجدته؟» — **وسؤالُ الشرط أوسع من
سؤال المثال دائماً.**

---

## 🟠 «الصفة: admin» — لأوّل إنسانٍ على المنصّة

بدأتُ الدورة أسأل عن سجلّ التدقيق: **هل يُكتب كلُّ ما يُغيّر الحال؟** وقِيس:
تسعة عشر فعلاً في `ACTIONS`، وتسعة عشر موضعَ قيد، ولا فعلَ بلا قيد. ثم في
الاتجاه الآخر — **هل يُقرأ ما يُكتب؟** — فكلُّ قيدٍ يحمل `mosque` إلّا
`contractor_reviewed`، وذاك موثَّقٌ عمداً. **نتيجةٌ سالبة: السجلّ سليم.**

ثم نظرتُ في **عرضه** لا في كتابته، فوجدت الشكل نفسه الذي عالجته الدورةَ
الماضية في رسائل الأخطاء:

```jsx
{api.AUDIT_LABEL[entry.action] || entry.action}
```

جدولٌ إن نقص **لم يُعطِ خطأً بل أعطى الاسم البرمجيّ**. فمسحتُ كل جدولٍ من
هذا الشكل في الواجهة وقابلته بتعداده على الخادم:

| الجدول | التعداد | النقص |
|---|---|---|
| `AUDIT_LABEL` | 19 فعلاً | — |
| `STATUS_LABEL` | 8 حالات | — |
| `ACTOR_LABEL` | 5 أدوار | — |
| `ROLES` | 5 أدوار | **`admin`** |

### وقِيس ما يراه صاحبه

في متصفّحٍ حقيقي، على حساب مشرفٍ مرقّى بالمفتاح الرئيس:

```
الترويسة: "المشرف ·"          ← فاصلٌ يتلوه فراغ، في كل شاشة
حسابي:    "الصفة: admin"
```

**وأوّلُ إنسانٍ على المنصّة مشرف:** `docs/DEPLOY.md` يجعل إنشاءه شرطاً
للإطلاق، إذ بدونه تتراكم طلبات ملكية المساجد بلا اعتماد فتقف المنصّة. فأوّلُ
ما تُري المنصّةُ أوّلَ أهلها اسمُها الداخليّ.

### والسبب: جدولٌ واحد لغرضين متناقضين

`ROLES` كانت **قائمةَ اختيارٍ عند التسجيل** — ولا يجوز أن تعرض `admin` لأنه
يُرقّى بـ`promote_admin.js` ولا يُطلب — و**ترجمةَ دورٍ مخزَّن** ولا بدّ أن
تشمله. فغُلّب الغرض الأول، فسقط `admin` من الجدول كلِّه، وسرى نقصُ القائمة
إلى الترجمة.

فالغرضان مفصولان الآن: `ROLES` جدولُ الترجمة كاملاً، و`SIGNUP_ROLES` مشتقّةٌ
منه بحذف `admin` وحده — فلا يزيد أحدهما بلا الآخر.

### والخادم كان قد عالج نظيرها

في `cloud/lib/auth.js`، فوق `ROLE_LABEL` وفيها `admin`، تعليقٌ مكتوبٌ منذ
حين: «الرسالة كانت تسرد الأسماء البرمجية: «متاحة لـ: volunteer فقط» —
إنجليزيةٌ في واجهة عربية». **العطب نفسه، رُئي مرّةً وأُغلق بابُه الواحد.**

**والدرس:** لم يكن سؤالي «كم باباً لهذه الغرفة؟» كافياً — فالباب الثاني كان
مغلقاً، ومكتوبٌ عليه لماذا أُغلق. **والدرس الأدقّ: العطب الذي عولج في موضعٍ
يُبحث عنه في نظائره، لا في جيرانه.** ما شارك الخادمَ في المفردة (`ROLES`)
كان أولى بالفحص ممّا شاركه في الملفّ.

### والسدّ على الصنف لا على الحالة

`tests/labels.test.js` يقرأ التعدادات من `cloud/` والجداول من `api.js`
ويقابلها، فأيُّ قيمةٍ تُضاف على الخادم بلا اسمٍ عربيّ تُسقطه. وسقط على
`HEAD` في ثلاثٍ من سبع.

**وأداةُ القياس تُقاس أولاً:** الاستخراج بالتعابير النمطية يُخفق صامتاً إن
تغيّرت صياغة المصدر، فيصير «لا نقص» معناه «لم أقرأ شيئاً» — وقد كذبت عليّ
أداتان من هذا الصنف في دورةٍ سابقة. فكلُّ قائمةٍ تُستخرج هنا يُتحقَّق من
عددها الأدنى قبل أن يُبنى عليها حكم.

### وأداةٌ كذبت في هذه الدورة أيضاً

أوّلُ إثباتٍ للسقوط في المتصفّح أسقط الرحلة كلَّها — ستّ حالاتٍ من ستّ. ولم
يكن ذلك إثباتاً بل تشويشاً: نزعتُ `api.js` كلَّه فذهب معه `SIGNUP_ROLES`،
فانكسر نموذج التسجيل قبل أن يُقاس شيء. **وسقوطُ كلِّ شيءٍ ليس دليلاً على
شيء.** فعُزل النزع في السطر المقصود وحده، فسقطت حالةٌ واحدة بالنصّ المقيس:
«المشرف ·».

---

## 🔴 «إن كنت على تواصلٍ معه» — ومن هو؟

بدأتُ أسأل عن تسرّب البيانات الشخصية: دوالُّ السحابة تقرأ بالمفتاح الرئيس
وتبني ردودها بيدها، **فـ`protectedFields` لا تحرسها أصلاً**. فمسحتُ كل حقلٍ
شخصيّ يعبر حدّ دالّة، ووجدتُ كلَّ موضعٍ محروساً: الاسم والرقم لا يخرجان إلا
إلى مشرفٍ يراجع، أو إلى صاحبهما.

ثم انقلب السؤال: **لا شيء يتسرّب — فهل يصل ما ينبغي أن يصل؟**

### وقِيس في متصفّح حقيقي

شاشة الإمام بعد التكليف، بنصّها كما قرأتها الأداة:

```
تصليح إنارة الصحن | مُسنَد | بانتظار المنفّذ
كُلِّف المنفّذ منذ 12 يوماً ولمّا يبدأ بعد. وقد طال الأمر:
  إن كنت على تواصلٍ معه فانتظاره أولى، وإلا فاسحب التكليف…
سحب التكليف — لم يحضر
```

**«معه»** — ولا اسمَ على الشاشة ولا رقم. الصفحة تفترض تواصلاً لم تُعطِه، وتحت
الافتراض زرٌّ يُقيِّد على المنفّذ غياباً في `abandonedJobs` **يقرؤه كل إمامٍ
بعده**. ثم في المعاينة زرٌّ آخر يكتب له تقييماً في `avgRating`.

**حكمان دائمان على إنسانٍ لا يُرى.** والإمام رآه مرّةً واحدة: في قائمة
المهتمّين — و`closeInterests` تُغلقها لحظة التكليف، والشاشة لا تجلبها بعدها
أصلاً (`status === 'open_for_volunteers' ? … : []`). فالاسم يُعرض لحظةَ
الاختيار ويُمحى لحظةَ الحاجة إليه.

ونظيرُها عند المنفّذ: بطاقته تحمل اسم المسجد وقريته و**زرّ الطريق إليه** — ولا
تحمل من يسأل عنه إذا وصل. رجلٌ يقود إلى مسجدٍ لا يعرف فيه أحداً ليعمل بيديه.

### والحرس كان أوسع ممّا ظننت

خطّطتُ أن يأتي الاسم من العميل مباشرةً (`include` على المُسنَد إليه) والرقمَ
وحده من دالّة — لأن `protectedFields` على `_User` تحجب `phone` دون `fullName`.
ثم قِيس ما يراه مستخدمٌ مصادَق باستعلامٍ مباشر:

```
الفضوليّ يستعلم عن حساب المتطوّع  → null
الفضوليّ يُعدّد المستخدمين         → 1  (نفسه وحده)
ACL: { صاحبُه: قراءة وكتابة }
```

فالحساب لا يُحجب منه حقلٌ، **بل لا يُرجَع الكائن أصلاً**. ولولا القياس لبنيتُ
نصف الحلّ على بابٍ مغلق، ولظهر ذلك أوّلَ مرّةٍ عند مستخدمٍ حقيقي. **والخطّة
المبنيّة على قراءةِ مخطَّطٍ ليست قياساً.**

### فدالّةٌ واحدة، وحدُّها مدّةُ التكليف

`getRequestContact` تُعطي كلَّ طرفٍ الطرفَ الآخر — اسماً ورقماً — وتشترط
ثلاثاً: أن تكون الحالة `assigned` أو `in_progress` أو `pending_imam_approval`،
وأن يكون للطلب منفّذ، وأن يكون المستدعي أحد الطرفين **بمقارنة المعرّف لا بما
يقوله الطلب**. قبل التكليف لا طرفَ آخر، وبعد الاعتماد انقضى ما يُتواصل بشأنه —
فينقطع. والشركة تُعرف باسمها التجاري لا باسم من سجّلها.

### والاختبار على طبقتين، لأن العطب على طبقتين

- `tests/integration/contact.test.js` — على خادمٍ حقيقي، لأن الـACL
  و`protectedFields` حرسٌ **لا يعرفه بديل Parse أصلاً**. سقط على `HEAD` في
  ستٍّ من سبع؛ والسابعة — أن الاستعلام المباشر لا يُرجع أحداً — قامت وحدها،
  وهي توثيق الحرس القائم لا الإصلاح الجديد.
- `tests/e2e/journey.test.js` — في المتصفّح، حيث قِيس العطب. سقط بالنصّ نفسه:
  «يُحكم بالغياب على منفّذٍ لا يظهر اسمه على الشاشة».

### وثلاثُ أدواتٍ كذبت عليّ في دورةٍ واحدة

1. **مسبارٌ في المتصفّح** طبع الشاشة بعد سطرٍ جديد، و`grep` يقرأ سطراً سطراً —
   فرأيتُ العنوان فارغاً وظننتُ الشاشة فارغة.
2. **`grep -A12` على `protectedFields`** أصاب أوّل مطابقة وهي **لفئةٍ أخرى**،
   فقرأتُ حرساً ليس حرسَ `_User` وبنيتُ عليه تصميماً.
3. **إثباتُ السقوط بنزع الملفّ كلِّه** — نُزعت `api.js` فذهب معها ما لا علاقة
   له، فسقط كلُّ شيء. وقد وقع نظيرُه في الدورة الماضية، **فوقعتُ فيه ثانيةً**.

**والدرس:** الأداة التي تقرأ سطراً لا تُسأل عن فقرة، والأداة التي تقف عند أوّل
مطابقةٍ لا تُسأل «هل هذا هو؟» بل «كم واحداً غيره؟». **وثلاثتها أخطأت في
الاستخراج لا في المنطق** — أي في أوّل خطوة، حيث لا يظهر الخطأ لأنه يُعطي
جواباً معقولاً.

### وعطبٌ صغير كشفه البناء

كتبتُ الاستيراد النسبي على سطرين، **فبقي في الحزمة المدمجة** — نمطُ الحذف في
`build_single_file.py` مربوطٌ بالسطر. والنتيجة `Identifier 'requireUser' has
already been declared` في `main.bundle.js` وحدها. **وسقط صريحاً** في
`tests/schema.test.js` لا صامتاً، وهو ما يُراد من الحارس. فكُتب على السطر
الواحد، وكُتب في المولّد لماذا.

---

## 🔴 قُيِّم المتطوّع ولم يُخبَر — والصمت وقع على لحظة المكافأة وحدها

في الدورة الماضية مسحتُ سجلّ التدقيق فوجدته تامّاً. فأعدتُ المسح نفسه على
**القناة التي يقرؤها المستخدم فعلاً** — الوارد — لا على السجلّ الذي يقرؤه
المدقّق. وذلك سؤالٌ آخر: السجلّ يثبت أن الشيء وقع، والوارد يُعلم به صاحبَه.

فقُوبل كلُّ انتقالٍ في دورة الطلب بمن يتأثّر به:

| الانتقال | من يتأثّر | أيُخبَر؟ |
|---|---|---|
| النشر للتطوّع | متطوّعو الجوار | ✅ |
| تسجيل اهتمام | الإمام | ✅ |
| **سحب الاهتمام** | الإمام | ❌ |
| التكليف | المنفّذ | ✅ |
| سحب التكليف | الطرف الآخر | ✅ |
| **بدء العمل** | الإمام | ❌ |
| الإبلاغ بالإنجاز | الإمام | ✅ |
| **اعتماد العمل** | **المنفّذ** | ❌ |
| الإلغاء | المنفّذ | ✅ |

سبعةٌ تُخبِر وثلاثةٌ تسكت. **والسكوت لم يقع عشوائياً.**

### وقِيس أخطرُها على خادمٍ حقيقي

```
المتطوّع بعد التكليف:  1 — «تم تكليفك بـ "تصليح إنارة"…»
المتطوّع بعد الاعتماد: 1 — الرسالة نفسها، لا شيء جديد
وسمعتُه صارت:         completedJobs=1   avgRating=5
```

**قُيِّم ولم يُخبَر.** رجلٌ ذهب إلى مسجدٍ وعمل بيديه، ثم أبلغ وانتظر — فلا يصله
شيء. وفي اللحظة التي سكتت فيها المنصّة كتبت في سجلّه رقمين يقرؤهما كل إمامٍ
بعدها.

والمرحلة الأولى تُطلق **بمسار التطوّع العيني وحده**، أي أن المتطوّع هو المنصّة.
فالانتقال الوحيد الذي يُكافئه هو الانتقال الوحيد الذي لم يُبلَّغ به.

وأدقُّ من ذلك: «تم اعتماد العمل، **بارك الله فيكم**» كانت في ردّ الدالّة — أي
تُعرض **للإمام الذي ضغط الزرّ**، لا لمن عمل. الدعاء كان يذهب إلى الطرف الخطأ.

### والصامتان الآخران من الصنف نفسه

- **بدءُ العمل** لا يبلغ الإمام. عملٌ يجري في مسجده ولا يعلم به إلا إن فتح
  التطبيق.
- **سحبُ الاهتمام** لا يبلغه، وقد أُخبر بالاهتمام نفسه: «متطوّع مهتمّ — اختر
  المنفّذ من قائمة المهتمّين». فيُدعى إلى قائمةٍ صارت فارغة ولا يعرف لماذا.
  **والإخبار بالبدء دون النهاية أسوأ من الصمت فيهما جميعاً** — لأنه يُنشئ
  انتظاراً ثم يتركه.

### والاختبار على طبقتين

- `tests/integration/notified.test.js` — يمشي الدورة ويقيس **نموّ الوارد** عند
  كل انتقال، لا نصّاً بعينه. سقط على `HEAD` في أربعٍ من ست؛ والخامسة — ألّا
  يصل خبرُ أحدٍ إلى غيره — قامت وحدها، وهي توثيقُ حرسٍ قائم.
- `tests/e2e/journey.test.js` — عند النقطة التي يقرأ فيها المتطوّع «أعمال
  منجزة: 1»، يُفتح وارده أيضاً. سقط بالنصّ: «قُيِّم المتطوّع ولم يبلغه خبرُ
  اعتماد عمله».

### والدرس

في الدورة الماضية سألتُ السجلَّ في الاتجاهين — أيُكتب؟ أيُقرأ؟ — فجاء تامّاً،
فاطمأننتُ إلى بابٍ ظننتُه البابَ. **لكنّ السجلّ والوارد قناتان لا واحدة**:
الأولى للمساءلة والثانية للإنسان، وتمامُ إحداهما لا يقول شيئاً عن الأخرى. وقد
كان `REQUEST_COMPLETED` مقيَّداً في السجلّ كما ينبغي، **والمقيَّد عنه لا يعلم**.

**والقاعدة:** ما يُكتب عن إنسانٍ في غيابه يُبلَّغ به. وأشدُّ الصمت خفاءً ما وقع
على خبرٍ سارّ — فالمظلمة تُشتكى، والمكافأة الغائبة لا يعرف صاحبُها أنها كانت.

---

## 🔴 المنصّة تمنعه من الحضور، ثم تُقيّد عليه غيابه

بدأتُ الدورة أسأل عن اكتشاف الفرص: ماذا يرى متطوّعٌ رفض مشاركة موقعه؟ فإذا
`Opportunities` تفرّق بين «لم يُحسم الموقع» و«حُسم بالتعذّر»، وتعرض عند
الثانية كلَّ مفتوح. **نتيجةٌ سالبة.** ثم عن `openRequestsCount` — العدّاد الذي
يُخفي المسجد من قائمة الفرص إن كذب — فإذا هو محسوبٌ باستعلامٍ في `afterSave`
لا مُزاداً. **سالبةٌ ثانية.**

ثم لاحظتُ عدم تماثل: سحبُ اعتماد الشركة يستدعي `warnImamsOfSuspension` —
يُخبر كل إمامٍ عند الشركة عمل. **وإيقافُ الحساب لا يستدعي شيئاً**، وهو يقع على
كل دور، ويقع من `scripts/promote_admin.js` بالمفتاح الرئيس بلا مرورٍ بدالّة.

### وقِيست السلسلة كاملةً على خادمٍ حقيقي

أُوقف متطوّعٌ مكلَّفٌ بعملٍ في مسجد:

```
وارد الإمام:  1 ← 1        (لا خبر)
سجلّ المسجد:  3 ← 3        (لا قيد)
الموقوف مردود: «حسابك موقوف حالياً. راسل الإدارة…»
وُسم بالغياب:  abandonedJobs = 1
```

**أربعة أسطر تحكي ظلماً كاملاً.** المنصّة منعته من الدخول ومن `startWork`، ثم
تركت إمامه ينتظر بلا خبر، ثم قبلت من الإمام — وهو لا يعلم — أن يُقيّد عليه
غياباً. والإمام لم يُخطئ: شاشته تقول «كُلِّف المنفّذ منذ كذا ولمّا يبدأ بعد…
فاسحب التكليف»، فسحب.

**والوسم يبقى بعد إعادة إتاحته**، يقرؤه كل إمامٍ ينظر في قائمة المهتمّين. عقوبةٌ
على يومٍ حرّمت عليه المنصّةُ العملَ فيه.

### والحارس على تغيّر البيانات لا على المستدعي

`afterSave` على `_User` يلتقط `isActive` وهي تنتقل إلى `false`. ولم أضعه في
`promote_admin.js` لأن الإيقاف بابٌ من أبواب: اليوم سكربت، وغداً دالّة مشرف —
**ومن كتب البلاغ عند أحدها تركه مفتوحاً عند البقيّة.** وهذا الدرس نفسه كان
مكتوباً في هذا الملفّ عن حدّ الطول («حدٌّ مكتوبٌ على بابٍ واحد») — فطُبّق هنا
ابتداءً لا بعد وقوعه.

والمنطق واحدٌ في `cloud/lib/worker.js`: بابا الخروج — سحبُ الاعتماد وإيقافُ
الحساب — يستدعيان `warnImamsOfWorkerLoss` نفسها، ونسخةُ `users.js` القديمة
حُذفت لا نُسخت. **ولا يُسحب التكليف تلقائياً** كما في سحب الاعتماد سواءً:
عملٌ قد يكون نصفَ منجَز، وتُعطى البيّنة ويبقى القرار للإمام.

### والنصف الثاني: الوسم نفسه

`releaseAssignment` صار يقرأ حال الحساب قبل أن يُقيّد: **الموقوف لا يُقيَّد
عليه غياب.** والقيدُ على الخادم لا في الواجهة — الإمام لا يعرف حال حساب
المنفّذ ولا ينبغي أن يُطالَب بمعرفته. والبلاغ يقول له ذلك صراحةً: «ولك سحب
التكليف — ولن يُقيَّد عليه غياب»، فلا يتردّد بين إنصافٍ ومصلحة مسجده.

**وإصلاحُ نصفٍ دون نصف كان سيُرضي:** لو أخبرتُ الإمام وتركتُ الوسم، لصار الظلم
موثَّقاً بدل أن يكون خفيّاً — والإمام مضطرٌّ إلى السحب على كل حال.

### والاختبار يحرس الجهتين

`tests/integration/suspended-worker.test.js` — ثمانٍ، سقط منها أربع على
`HEAD`. والأربع الباقية تحرس **ألّا يُفرط الحارس**، فهي قائمةٌ قبل الإصلاح
وبعده قصداً:

- الغيابُ الحقيقي يُقيَّد كما كان — **ولولاها لكان «الإصلاح» تعطيلاً
  لـ`no_show` كلِّه ولا يُكشف.**
- إيقافُ من لا عملَ له لا يُبلِّغ أحداً — فلا يُغرق وارد الأئمة.
- وإعادةُ الإتاحة لا تُبلِّغ — الحارس على المنع لا على كل حفظٍ للحساب.

### والحارس القديم عمل لصالحي

أضفتُ `worker_suspended` إلى `audit.ACTIONS`، فكان `tests/labels.test.js` —
المكتوب في دورةٍ سابقة — سيُسقط البناء لو نسيتُ اسمه العربي. **وهذه أوّل مرّة
يمنع فيها حارسٌ من حراس الدورات السابقة عطباً قبل وقوعه لا بعده.**

---

## 🔴 «سيُراجع خلال أيام عمل» — وعدٌ يُقطع ثم لا يُوفى

المسح الذي كشف صمت `completeService` كان على دورة **الطلب**. فأُعيد على الدورة
الأخرى التي ينتظر فيها إنسانٌ قرار إنسان: **طلب ملكية المسجد**.

وقِيس على خادمٍ حقيقي:

```
ما قيل لمقدّم الطلب: «تم استلام طلبك من عند المسجد، سيُراجع خلال أيام عمل.»
وارده بعد الاعتماد:  []
وارده بعد الرفض:     []
```

**المنصّة تَعِد بالمراجعة صراحةً، ثم لا تقول شيئاً أبداً.** لا عند القبول ولا
عند الرفض.

وهذه **أوّلُ معاملةٍ للإمام مع المنصّة**، وبلا مسجدٍ معتمَد لا يستطيع شيئاً
البتّة: لا ينشر طلباً، ولا يرى مسجداً، ولا يصله متطوّع. فكان عليه أن يتذكّر
وحده أن يعود ويفتح «تسجيل مسجد» لينظر — والمراجعة بيد إنسانٍ فتطول أياماً.

### وأدلُّ ما في الأمر أن المبدأ كان مكتوباً

في `reviewMosqueClaim` نفسها بلاغٌ للإمام **السابق** حين يُنقل مسجده، وفوقه:

> «من يُنزع منه مسجده أولى الناس بأن يعلم.»

وهو الصواب. **لكنّ المبدأ لم يُطبَّق على صاحب الطلب** — وهو أوّلُ من ينتظر،
وأطولُ من ينتظر. كُتب البلاغ لمن يخسر ونُسي من يرجو.

### وثالثةٌ كانت موكولةً إلى إنسان

حين يُعتمد الإمام ويُردُّ موقعُه المُرسل، تُعيد الدالّة للمشرف رسالةً، وفوقها
تعليق:

> «بلا هذا يظنّ المسجد صار على الخريطة، فلا يتابع **ولا يُنبّه إمامه إلى
> الزرّ**.»

أي أن إبلاغ الإمام بأن مسجده ما زال خارج الخريطة كان **خطوةً بشرية** يفعلها
المشرف بيده. وكلُّ خطوةٍ بشرية في مسارٍ متكرّر تُنسى. فصار البلاغ يحمل ذلك:
«ما زال مجهول الموقع — **ثبّته** من «مساجدي» وأنت عنده ليجده المتطوّعون».

### وكلُّ بلاغٍ يحمل البابَ التالي

- المعتمَد: «يمكنك الآن نشر طلبات الصيانة **من «مساجدي»**» — الإمام لا يعرف
  من تلقاء نفسه أين يُنشر الطلب.
- المرفوض: «راجع الإدارة، **ولك تقديم طلبٍ جديد من عند المسجد**» — الرفض بلا
  مخرجٍ يُقفل الباب على من قد يكون إمام المسجد حقاً.

**وخبرٌ بلا فعلٍ تالٍ نصفُ خبر.**

### والاختبار

`tests/integration/claim-reviewed.test.js` — ستٌّ، سقط منها أربع على `HEAD`.
والسادسة (ألّا يصل قرارُ أحدٍ إلى غيره) قائمةٌ قبلُ وبعدُ، كسائر حرّاس
الإفراط.

**ولم يُكتب لهذه حالةُ متصفّح**: رحلة `journey.test.js` تُهيّئ المسجد بالمفتاح
الرئيس قصداً — «مسار طلب الملكية له اختباره في `flow.test.js`، وإقحامه هنا
يُطيل الرحلة بلا أن يضيف تغطية للواجهة». وإقحامُه الآن لأجل هذا وحده كان
سيُطيلها للسبب نفسه المرفوض. فالطبقة واحدة هنا، وهذا مذكورٌ لا مسكوتٌ عنه.

### وأداةٌ صحّحت لي فرضيةً كاذبة

أردتُ حالةَ «اعتُمد ورُدّ موقعه»، فبنيتُها بطلبٍ من ظفار لمسجدٍ في بوشر —
فسقط الاختبار برسالةٍ من الخادم: **الطلب مردودٌ عند التقديم أصلاً** بحدّ الـ80
كم. أي أن الحالة لا تُبلَغ من الباب الذي ظننته. وقراءةُ الشرط دلّت على بابها:
الكود نفسه يقول إن الطلب «قد يكون أُنشئ قبل وجود هذا الفحص أصلاً» — فبُنيت
الحالة كما وصفها الكود لا كما تخيّلتُها. **والاختبار الساقط ليس دائماً عطباً
في الشيفرة؛ أحياناً هو تصحيحٌ لفهمي إيّاها.**

---

## 🟠 المشرف يقرّر بلا أن يرى من انتظر أطول — وثلاثُ نتائج سالبة قبلها

هذه الدورة أنفقت أكثرها في سؤالٍ لم يُثمر، وتلك نتيجةٌ تُقال:

- **طوابير المشرف تجويعاً؟** `listPendingClaims` و`listPendingContractors`
  كلتاهما `ascending('createdAt')` — الأقدم أوّلاً، والسقف يقطع الأحدث لا
  الأقدم. **سالبة.**
- **التقليم يمحو خبراً لم يُقرأ؟** يحذف بالعمر لا بحال القراءة، وذلك مكتوبٌ
  ومُعلَّل: «الإشعار خبرٌ عاجل، ومن لم يقرأه في ثلاثة أشهر فاته أوانه، والأثر
  الدائم في `AuditLog` لا هنا». **سالبة.**
- **صلاحيةُ الكتابة؟** مُسحت كلُّ دالّةٍ تكتب: لكلٍّ حارسُ دور **وحارسُ
  ملكية** — `mosqueForImam` أو `loadAssignedRequest` أو مقارنة معرّف صريحة.
  لا ثغرة. **سالبة.**

ثم سُئل سؤالٌ رابع: **أيصل لوحةَ المشرف ما أُرسل إليها؟** وهو نظير سؤالٍ عولج
من قبل («قائمةٌ تُجلب لتُرمى»). فقُوبل ما تُعيده كلُّ دالّة بما تقرؤه الشاشة:

```
listPendingClaims      → createdAt              لا يُقرأ
listPendingContractors → createdAt، phone       لا يُقرآن
```

### والمدّة أثقلُ ما فقده

المشرف **عنق المنصّة** — `docs/DEPLOY.md`: «بلا التزامٍ منه تتراكم الطلبات
وينصرف الأئمة». وكان يراجع طلباً عمره أربعون يوماً كما يراجع طلبَ اليوم:
الترتيب صحيحٌ على الخادم، **والمدّة غائبةٌ عن عينه**. فلا يُحسّ ثقلَ الطابور،
ولا يعرف أيَّ وعدٍ تأخّر.

والمنصّة قالت لصاحب الطلب صراحةً: **«سيُراجع خلال أيام عمل»** — وعدٌ قُطع باسم
المشرف ولا يراه. فصار الحدّ `OVERDUE_REVIEW_DAYS = 7` مأخوذاً **من الوعد
نفسه** لا مخترَعاً، وما تجاوزه يُوسَم: «قُدّم منذ 12 يوماً — وقيل لصاحبه
يُراجَع خلال أيام عمل».

### وهذا العطب بعينه عولج من قبل

في `assignedAt`: «حكمٌ بالغياب يُطلب بلا المدّة التي يقوم عليها». وأدهى من
ذلك أن **`sinceLabel` كانت على هذه الشاشة نفسها**، تُستعمل لتاريخ سحب اعتماد
شركة — أي لتفصيلٍ ثانوي — **دون المدّة التي ينتظرها الناس**. الأداة في اليد
والباب مفتوح.

### والهاتف: يُعتمد من يدخل مساجد الناس بورقةٍ وحدها

`listPendingContractors` تُرسل `phone` ولا تُعرض. فالمشرف يعتمد شركةً تدخل
المساجد بسجلٍّ تجاريّ مكتوبٍ في حقلٍ نصّي — ولا وسيلة له أن يتّصل قبل أن
يعتمد. **ونظيرُه معروضٌ في البطاقة التي فوقها**: «الطالب: فلان · رقمه».

### والحارس هو أداةُ القياس نفسها

`tests/admin-fields.test.js` يقابل ما تُعيده الدالّتان بما تقرؤه `AdminHome`،
و`ALLOWED_UNREAD` فيه **تبدأ فارغة قصداً**: حقلٌ يُضاف ولا يُعرض يُسقط
الاختبار، فيُعرَض أو يُكتب سببُ تركه — **والقرار يُتّخذ ولا يُسكت عنه.** ومعه
حارسٌ يربط الحدّ بالوعد: إن تغيّر نصُّ «أيام عمل» في الخادم سقط الاختبار.

وسقط على `HEAD` في ثلاثٍ من خمس، وسقطت معه حالةُ المتصفّح بالنصّ:
«المشرف يرتّب أولوياته بلا أن يرى من انتظر أطول».

**والدرس:** ثلاث نتائج سالبة قبل الرابعة ليست وقتاً ضائعاً — لكنّ الرابعة
جاءت من سؤالٍ **سبق أن أثمر مرّتين**: «أيصل ما أُرسل إلى العين؟». والأسئلة
التي أثمرت تُعاد على غرفٍ لم تُسأل بعد.

---

## 🟠 الساعةُ أُعطيت للناظر ومُنعت عن المنتظِر

في الدورة الماضية أُعطي المشرفُ مدّةَ انتظار من ينتظره. فأُعيدت الأداةُ نفسها
على الغرف الأخرى — وهذا ما أوصت به خاتمتُها — فإذا **الجهة الأخرى عمياء
تماماً**:

```
ImamHome    ← getMyClaims          createdAt، reviewedAt   لا يُقرآن
MyTasks     ← getMyInterests       createdAt               لا يُقرأ
RequestDetail ← getRequestInterests createdAt              لا يُقرأ
```

فالإمام يرى «قيد المراجعة»، والمتطوّع يرى «بانتظار اختيار الإمام» — **بلا يومٍ
ولا شهر**. ومن لا يعرف كم انتظر لا يعرف متى يسأل: أنُسي طلبُه، أم لم يحن دوره؟

### وبطاقة الإمام كانت تكذب مع الوقت

تحت «قيد المراجعة» سطرٌ مكتوب: **«سيراجع المشرف طلبك خلال أيام عمل.»** وهو
صادقٌ في اليوم الأول، **وكاذبٌ في الشهر الثالث** — والشاشة تبقى على قولها.
فصارت المدّة تحته مباشرةً، وما تجاوز الوعد يُوسَم: «قُدّم منذ 40 يوماً — وقد
تجاوز ما وُعدت به، فراجع الإدارة».

**والوسم حيث وُعد فقط.** اختيارُ الإمام بين المهتمّين لا موعدَ له، فتُعرض
المدّة عارية بلا تحذير: `overdueAfter = null`. **وتخويفُ المتطوّع بحدٍّ مخترَع
أسوأ من السكوت** — والحدود المخترعة تصير أعرافاً.

### وأداةُ القياس كذبت مرّتين في مسحٍ واحد

1. افترضَت أن الشاشات تربط `row.` وحده — و`ImamHome` تربط `mosque.` و`claim.`
   فأعلنت أن **كلّ** حقلٍ فيها غير مقروء. لو صدّقتُها لـ«أصلحتُ» تسعة حقولٍ
   سليمة.
2. ثم قالت إن `hasLocation` و`locationSource` لا تُقرآن — وهما يُقرآن في
   **مكوّنٍ ابن** لم تدخله. والتعليق على الخادم يقول إن `hasLocation` موجودةٌ
   لأجل زرّ التثبيت، فلو صدّقتُها لأعلنتُ عطباً في حارسٍ قائم.

**والدرس:** الأداة التي تفترض اسماً واحداً للربط تقيس التسمية لا الحقيقة،
والتي تقف عند حدّ الدالّة تقيس الملفّ لا الشجرة. **وكلتاهما تُخطئ في اتجاه
«وجدتُ عطباً» — وهو الاتجاه الذي يُصدَّق بلا تمحيص.** فصار الحارس يحمل اسم
الربط لكل شاشة صراحةً.

### وحارسٌ تذبذب فأُصلح لا احتُمل

حالة المتصفّح الجديدة **سقطت مرّةً ثم قامت مرّتين بلا تغيير شيفرة**. والسبب
أنّي قرأت الصفحة بعد ظهور زرّ «بدأت العمل» — وهو من قائمة «مهامّي»، بينما
«اهتماماتي» قائمةٌ ثانية تُجلب بنداءٍ ثانٍ لم يكن قد وصل.

**وحارسٌ يتذبذب أسوأ من لا حارس:** يُعلَّم يوماً بأنه «غير مستقرّ» فيُهمَل
سقوطُه الحقيقي. فأُبدل الانتظار إلى الشرط المقصود نفسه (`waitUntil`)، وشُغِّلت
الرحلة ثلاثاً فاستقرّت.

### والحارس امتدّ لا تكرّر

`tests/admin-fields.test.js` صار يغطّي خمس بوّابات لا اثنتين، ومعه `ALLOWED_UNREAD`
تحمل الآن أسباباً مكتوبة لما لا يُعرض قصداً — كنصّ الطالب الذي كتبه هو قبل
لحظات. **والقرار مكتوبٌ لا مسكوتٌ عنه**، وذاك كلُّ ما يُطلب من قائمةٍ كهذه.
وسقط على `HEAD` في أربعٍ من ثمان.

---

## 🟠 نداءٌ لكل بطاقة — وأنا من أوقعه قبل ستّ دورات

نتيجتان سالبتان أوّلاً: رسائلُ القوائم الفارغة معقولة، وأهمُّها يدلّ على الفعل
التالي («ابحث عنه من تبويب تسجيل مسجد»). و`Listing` **يقدّم الخطأ على
الفراغ**، فلا يُقال «لا توجد فرص» لمن انقطعت عنه الشبكة — وهو عطبٌ عولج من قبل
في شاشة «حسابي» ولم يتكرّر هنا.

ثم نظرتُ فيما أضفتُه أنا. `Counterpart` — المكوّن الذي كتبتُه في الدورة
الثانية عشرة ليرى كلُّ طرفٍ الطرفَ الآخر — يُجلب **لنفسه** داخل `map` على
البطاقات. فقِيس في متصفّح حقيقي على متطوّعٍ له ثلاثة تكليفات:

```
مجموع النداءات لفتح «مهامّي»: 8
  getMyNotifications 1 · updateMyLocation 1 · getNearbyOpportunities 1
  getMyInterests 1 · ServiceRequests 1 · getRequestContact 3   ←
```

**ثلاثةٌ حيث يكفي واحد.** وحدُّ التكليفات ثلاثة، فهذه أسوأ حالةٍ ممكنة —
والباقة المجانية 25 ألف طلبٍ شهرياً: خمسون منفّذاً يفتحون شاشتهم خمس مرّاتٍ
يومياً يُنفقونها **كلَّها على هذا وحده**.

ونظيرُه قِيس مرّتين من قبل في هذا المستودع — شارةُ الوارد، ثم قائمةٌ تُجلب
لتُرمى. **والقاعدة واحدة: ما يُطلب لقائمةٍ يُطلب مرّةً للقائمة.**

### والدفعة تُسقط ما ليس لصاحبها ولا تسقط كلُّها

`getRequestContacts` تأخذ قائمة معرّفات وتُعيد خريطة. وما ليس المستدعي طرفاً
فيه **يغيب من الخريطة** ولا يرمي: القائمةُ تُطلب لصفوفٍ بيد صاحبها، **وسقوطها
كلِّها لأجل صفٍّ واحد يُعمي البقيّة**. والحرس نفسه لم يتغيّر — الصفة من الكائن
المخزَّن، والهوية بمقارنة المعرّف، والحالة في مدّة التكليف وحدها. وقِيس ذلك
صراحةً بدفعةٍ فيها معرّفٌ غريب.

والحسابات تُجلب دفعةً واحدة أيضاً: المسجد الواحد له إمامٌ واحد، والمنفّذ قد
يكون هو نفسه في أكثر من طلب — فاستعلامٌ لكلٍّ يُعيد الشيء مرّاتٍ.

### والحارس يعدّ على الشبكة لا في الشيفرة

`tests/e2e/requests-per-screen.test.js` يفتح «مهامّي» بثلاثة تكليفات ويعدّ ما
يمرّ فعلاً، **ويُسقط أيَّ نداءٍ يتكرّر** — لا نداءً بعينه. فقاعدةُ «مرّةً
للقائمة» محروسةٌ لكل نداءٍ يُضاف بعد اليوم، لا لهذا وحده. وسقط على `HEAD`
بالتفصيل المقيس نفسه.

**ولا يُقاس هذا إلا في متصفّح:** الشيفرة تبدو سليمة في الحالين، والفرق على
الشبكة.

### وإضافةُ ملفٍّ ثانٍ للمتصفّح كسرت الرحلة

بعد إضافة الحارس سقطت الرحلة كلُّها. والسبب أن `test:e2e` كان يُشغّل الملفات
**بالتوازي**، وكلُّ ملفٍّ يرفع PostgreSQL وparse-server ومتصفّحاً — فتزاحمت
على المنافذ. واختبارُ التكامل محروسٌ بـ`--test-concurrency=1` منذ حين، والمتصفّح
لم يكن يحتاجه لأنه كان ملفاً واحداً.

**والدرس:** إعدادٌ يصحّ لملفٍّ واحد ليس إعداداً — هو صدفةٌ لم تُختبر. وقد ظهر
هذا فوراً لا بعد حين، لأن الطبقات تُشغَّل كلُّها في `npm run verify` قبل كل
دفع.

---

## 🟠 الخطوات اليدوية تُفحص لا يُوثق بها

تغيّرت الأولوية من «ابحث عن عطبٍ آخر» إلى «أغلق ما يمنع التسليم». فبدأتُ من
ملفّات التسليم لا من الشيفرة.

**ونتيجةٌ سالبة أولاً — وكدتُ أُبلغ عطباً مُختلَقاً.** قِستُ متغيّرات البيئة
فوجدتُ الواجهة تقرأ أربعة `VITE_*`، ثم قال `ls app/` إن لا قالب لها. وكان ذلك
كذباً: الأمرُ نُفّذ من مجلّدٍ آخر لأن نداءَين متوازيَين لا يتشاركان `cd`.
و`app/.env.example` موجود، والدليل يشرح بناء الواجهة ونشرها. **ولولا التحقّق
لكتبتُ إصلاحاً لعطبٍ لا وجود له.**

### ثم الخطوات التي تُنسى

في `docs/DEPLOY.md` ثلاثُ خطواتٍ تُنفَّذ **بيد إنسان** من لوحة Back4app، وكان
`preflight` يذكرها في `unverifiable` — أي يقول «لم أفحصها» ويمضي. **وخطوةٌ
يدوية في مسارٍ يُنفَّذ مرّةً تُنسى**، ولا يظهر أثرُها إلا بعد أشهر: قاعدةٌ
تمتلئ، أو مساجد تتكرّر، أو متطوّعٌ يُصدّ عن رفع صورته.

فقُسّمت الثلاث بحسب ما يُقاس فعلاً:

| الخطوة | كانت | صارت |
|---|---|---|
| الفهرس الفريد على `externalId` | تُذكر ولا تُفحص | **تُختبر بمحاولة كتابة معرّفٍ مكرَّر** |
| جدولة المهام الدورية | تُذكر ولا تُفحص | **يُقاس أثرُها في `_JobStatus`** |
| تفعيل رفع الملفات | تُذكر بلا سبب | تبقى، **ومعها سببُ امتناعها** |

**والتفرّد يُختبر بمحاولته**: مخطط Parse لا يعبّر عن الفهرس الفريد فلا سبيل
إلى قراءته — والسبيل الوحيد أن يُكتب صفٌّ مكرَّر ويُنظر أيُردّ. والكتابة على
قاعدةٍ حيّة لا تُترك للحظّ: المعرّف موسومٌ بـ`__preflight__`، والحذف في
`finally` فيقع وإن سقط الفحص. **ويُقاس في الاختبار أن القاعدة لم تبقَ فيها
بقيّة.**

**والجدولة تُقاس بأثرها الماضي لا بوجودها**: كلُّ تشغيلٍ يكتب صفّاً في
`_JobStatus`، فغيابُه بعد أسبوعين يعني أنها لم تُجدوَل. وخادمٌ في يومه الأول
لا عيب فيه — فيُقال «لم تُشغَّل بعد» ولا يُسقط الفحص.

### وأهمُّ ما تُرك، تُرك بسببٍ لا بسكوت

**رفع الملفات لا يُفحص من هنا بحال.** الفحص يجري بالمفتاح الرئيس، والرفع
بالمفتاح الرئيس ينجح **ولو كان معطّلاً للمصادَقين** — فلو فُحص لأعطى **أخضرَ
كاذباً، وهو أسوأ من لا فحص**. فصار المذكور: لماذا يمتنع، وكيف يفحصه القارئ
بنفسه (من التطبيق بحساب متطوّع).

**و«لم يُفحص» بلا سبب يُقرأ كسلاً فيُهمَل.** وسببُ الامتناع هو ما يدلّ القارئ
على البديل.

### والتقرير لم يعد يخضرّ بالكود وحده

وهذا **مقصود**، وهو أهمّ ما في التغيير: `preflight` يبقى **أحمر** على أي خادمٍ
جديد حتى يُضاف الفهرس الفريد يدوياً. وقِيس ذلك على مكدّس الاختبار نفسه — فهو
بلا فهرسٍ فريد، والفحص كشفه.

كان الأخضر يعني «الكود سليم»، وصار يعني «الخادم جاهز». **والفرق بينهما هو كلُّ
ما يقع في أوّل نشر.**

---

## 🟠 `npm run seed` — البابُ الوحيد الذي لا رجعة منه

في الدورة الماضية صار `preflight` يفحص الخطوات اليدوية لا يثق بها. فأُعيد
السؤال على أدواتٍ أخرى: **أين نصيحةٌ في وثيقةٍ لا يسندها الكود؟**

و`docs/DEPLOY.md` يقول منذ حين:

> شغّل محافظةً أولاً، وانظر عدّاد الطلبات في لوحة Back4app، ثم استورد الباقي.
> وإن تبيّن أن الاحتساب لكل كائنٍ لا لكل دفعة، فوزّع الاستيراد على شهرين.

**والسكربت لا يعرف بهذا شيئاً.** قِيس:

```
$ node scripts/seed_mosques.js
→ 18214 سجلاً جاهزاً للاستيراد
… ثم يكتبها
```

بلا وسائط، بلا تحذير، بلا ذكرٍ للكلفة. ومن يقرأ في `package.json` سطر
`"seed"` يُشغّله.

### وما يقع حينها لا يُستردّ

الكلفة مقيسة: SDK يجمع عشرين كائناً في طلبٍ واحد، فالكامل ≈**911** طلباً. لكن
إن كان Back4app يحتسب كلَّ كائنٍ داخل الدفعة طلباً مستقلاً صارت ≈**18,214** —
أي **ثلاثة أرباع الباقة المجانية في عمليةٍ واحدة، قبل أن يصل المنصّةَ مستخدمٌ
واحد**.

والفرق بين الاحتسابين **عشرون ضعفاً، ولا نعرف أيَّهما يعتمده Back4app**. فهذه
لحظةٌ يُقرّرها من يملك عدّاداً أمامه، لا من يقرأ سكربتاً.

**وهذا هو البابُ الوحيد في المستودع الذي لا رجعة منه:** كلُّ ما عداه
idempotent يُعاد تشغيله بلا ثمن — والمخطط، والترقية، والفحص. **وإنفاقُ الباقة
لا يُعاد.**

### فيُطلب باسمه لا يُمنع

`--all` صراحةً، ومن قصده يكتبها في ثانية. ومن لم يقصدها يرى:

```
✗ هذا استيرادٌ كامل: 18214 مسجداً.
  الكلفة المقيسة: ≈911 طلباً إن احتُسبت الدفعة طلباً،
  و≈18214 إن احتُسب كلُّ كائنٍ على حدة — من باقةٍ شهرية 25,000.

  ابدأ بمحافظةٍ واحدة وانظر عدّاد الطلبات في لوحة Back4app:
    node scripts/seed_mosques.js --governorate musandam   # 264 مسجداً
```

**والرقمان معاً لا أحدهما**: ذكرُ الأدنى وحده يُطمئن بلا وجه، وذكرُ الأعلى
وحده يُفزع — وذكرُهما يقول الحقيقة، وهي أن القرار بعد القياس لا قبله. **ومنعٌ
بلا مخرجٍ يُقرأ عطباً في الأداة فيُلتفّ عليه**، فمعه البديل الرخيص وطريقةُ
الطلب الصريح.

### والحارس يُشغّل السكربت لا يقرؤه

`tests/seed-guard.test.js` يستدعيه عمليةً كاملة كما يستدعيه المُشغّل — **لأن
قراءة السطر لا تُثبت أنه يُنفَّذ قبل الكتابة**. وفيه حارسان ضدّ الإفراط: أن
`--all` يمضي (فالحارس ليس منعاً)، وأن `--dry-run` لا يُحرَس (فهي لا تكتب ولا
تُنفق). وسقط على `HEAD` في ثلاثٍ من ستّ.

**والدرس:** الوثيقة تقول للقارئ، **والكود يقول لمن لم يقرأ**. وكلُّ تحذيرٍ في
وثيقةٍ هو عطبٌ مؤجَّل حتى يسنده الكود أو يُحذف.

### وفي هذه الدورة دفعتُ بناءً أحمر — وأقولها

شغّلتُ `npm run verify` موصولاً بـ`| tail` داخل سلسلة `&&`. **والأنبوب يجعل
رمز الخروج رمزَ آخر أمرٍ فيه** — أي `tail` وهو صفرٌ دائماً. فمضت السلسلة إلى
`commit` و`push` رغم سقوط طبقة التكامل.

والحارس لم يُخطئ: `verify.js` خرج برمزٍ غير صفر كما ينبغي، **وأنا من أخفاه**.
والقاعدة التي في `verify.js` نفسه — «التخطّي يُعدّ سقوطاً» — نقضتُها بأنبوب.

### والسقوط الذي كشفه كان حقيقياً

`search.test.js` سقط في المجموعة وقام وحده، ثم قام ثلاثاً بعدها. **ولم أتركه
عند «لعلّه عارض»:**

```js
.exists('nameTokens').limit(300).find(…)   // بلا ترتيب
const multi = sample.find(…)               // «أوّل» ما ردّته القاعدة
```

استعلامٌ **بلا ترتيبٍ مقطوعٌ بسقف**، فالمسجد المختار يختلف بين تشغيلٍ وآخر،
والكلمةُ المبحوث عنها معه. وهذا الدرس بعينه مكتوبٌ في `getNearbyOpportunities`
منذ حين: «بلا ترتيبٍ يكون المقطوع بالسقف عشوائياً» — **طُبّق على شيفرة الإنتاج
ولم يُطبَّق على مِرقاة الاختبار.**

فأُضيف `ascending('externalId')`، وشُغّل ثلاثاً فاستقرّ.

**ودرسان:** أنّ الحارس يُحرَس أيضاً — ومِرقاةُ الاختبار شيفرةٌ تخضع لقواعد
الشيفرة. وأنّ **أدوات القياس تُفسد بالأنابيب**: ما يُخفي رمز الخروج يُخفي
الحقيقة كلَّها، وقد كذبت عليّ الأدوات في كل دورةٍ تقريباً — وهذه المرّة كذبتُ
أنا على نفسي بها.

---

## 🟠 أدواتٌ تكتب على خادمٍ ولا تقول أيَّه

نتيجةٌ سالبة أولاً: مُسح المستودع المتتبَّع عن سرٍّ مُودَع — مفتاحٍ أو رمزٍ —
فلم يُوجد إلا ثابتٌ محلّيٌّ في مِرقاة الاختبار. و`apply_schema.js` **لا يحذف
شيئاً** بنصّ توثيقه وبفحص شيفرته. **سالبتان.**

ثم سؤالٌ من صنف الدورة الماضية: أين فعلٌ خطِرٌ لا يُخبر صاحبه بما يفعل؟

```
seed_mosques    → لا تقول على أي خادمٍ تكتب
apply_schema    → لا تقول
promote_admin   → لا تقول
preflight       → تقول   ← وهي القراءة المحضة
```

**ثلاثٌ من أربع.** والوحيدة التي تقول هي التي لا تكتب شيئاً — أي أقلُّها ضرراً
لو أخطأت الوجهة.

### وليست حالةً شاذّة بل الحالة المتوقَّعة

`docs/DEPLOY.md` **يوصي بتطبيقٍ تجريبي قبل الحقيقي**، ويوصي بمحافظةٍ واحدة قبل
السلطنة. فوجودُ تطبيقين على جهاز المُشغّل هو ما يطلبه الدليل نفسه، والفرق
بينهما **سطرٌ في `.env` يُبدَّل باليد**.

وأثرُ الخطأ ليس واحداً: ترقيةُ مشرفٍ في التطبيق الخطأ تُصحَّح بأمرٍ آخر،
**واستيرادُ 18,214 مسجداً في التطبيق الخطأ يُنفق باقتَه ولا يُستردّ.**

### والوجهة تُقال قبل الفعل، وفي الردّ أيضاً

`scripts/lib/target.js` — مضيفٌ ومقدّمةُ معرّف التطبيق قبل كل عمل. **ولا
يُطبع المفتاح الرئيس ولا المعرّف كاملاً**: المقصود أن يُميّز المُشغّل بين
تطبيقيه، لا أن يُنسخ سرٌّ إلى سجلٍّ أو لقطة شاشة. وستّةُ أحرفٍ تكفي للتمييز
ولا تكفي للاستعمال.

وأُضيفت إلى **نصّ الردّ** في حارس الاستيراد الكامل كذلك: من يُوقَف عن فعلٍ
يحتاج أن يعرف **أين** كان سيقع — وإلا فقد يُعيد المحاولة بـ`--all` على
الوجهة الخطأ نفسها.

### والحارس يمنع الإفشاء كما يمنع الصمت

`tests/seed-guard.test.js` يُشغّل الأدوات الأربع عملياتٍ كاملة، ويطلب من كلٍّ
منها ثلاثاً: أن تذكر المضيف، **وألّا تطبع المفتاح الرئيس، وألّا تطبع معرّف
التطبيق كاملاً**. فالإصلاح الذي يُفشي سرّاً ليس إصلاحاً.

وسقط على `HEAD` في أربعٍ من ستّ جديدة، و`preflight` قائمٌ قبلُ وبعدُ — فهو
حارسُ الإفراط هنا.

---

## 🟠 أوّلُ ملفٍّ يفتحه المتسلِّم كان منحرفاً في أربعة مواضع

`README.md` هو الباب الأوّل: من يتسلّم المستودع — مشترياً كان أو مطوّراً — يبدأ
منه. وقِيس فيه **أربعة انحرافات مجتمعة**:

| الانحراف | الأثر |
|---|---|
| `node scripts/seed_mosques.js` بلا `--all` | **يُردّ اليوم**، فيظنّ القارئ العطبَ في الأداة |
| «فهرس 2dsphere ← **إلزامي**» | و`DEPLOY.md` يقول إنه يُطبَّق آلياً وإن القرب يعمل بدونه |
| لا ذكرَ لـ`docs/DEPLOY.md` | من يتبع البابَ الأوّل **لا يبلغ المرجع أبداً** |
| لا ذكرَ لـ`preflight` ولا `verify` ولا `admin` | أهمُّ ثلاثة أوامر لمالكٍ جديد، وأحدها بدونه المنصّة معطّلة |

والأوّل نتيجةٌ مباشرة لدورةٍ سابقة: حرستُ الاستيراد الكامل، **وحدّثتُ
`DEPLOY.md` ونسيتُ `README.md`**. أي أن إصلاحي هو الذي كسر السطر.

### والوثيقة المنحرفة أسوأ من غيابها

غيابُها يدفع القارئ إلى السؤال، **وانحرافُها يدفعه إلى الثقة**. ولا يكشف ذلك
تشغيلٌ ولا فحصُ شيفرة: **الوثائق لا تُنفَّذ، فلا تسقط.** ولذلك بقي التناقض
قائماً وثيقتين تقولان للقارئ الواحد جوابين.

### والتزامٌ قانوني كان غائباً عن الباب الأوّل

بيانات **OpenStreetMap** بترخيص ODbL، **والنسبة شرطٌ فيه**. وهي معروضةٌ في
شاشة «حسابي» ومكتوبةٌ في كل ملفٍّ يُخرجه السكربت — ومذكورةٌ في `DEPLOY.md`.
**ولم تكن في `README.md`.** ومن يتسلّم المستودع ولا يعلم بذلك قد يحذفها من
الواجهة وهو يظنّها زينة. فصارت في الباب الأوّل.

### والحارس ينفّذ ما تقوله الوثيقة

`tests/docs-drift.test.js` يستخرج كلَّ أمرٍ من كتل الشيفرة في `README.md`
و`DEPLOY.md`، ويقابله بما هو موجود: `npm run X` بجدول السكربتات، و`node
scripts/Y.js` بالملفّ. ويقيس التناقض بين الوثيقتين، وحضورَ المرجع والالتزام.
وسقط على `HEAD` في خمسٍ من ثمان.

**وأداةُ القياس أخطأت أوّلاً**: أعلنت أن `npm run build` و`verify:pwa` غير
موجودين — وهما في `app/package.json` ويُنفَّذان بعد `cd app`. **فقرأت السطر
بلا سياقه فأعلنت انحرافاً حيث لا انحراف.** فصارت تتتبّع `cd`.

**والدرس:** كلُّ إصلاحٍ يغيّر أمراً يُبحث عن ذلك الأمر في كل وثيقة، لا في
الوثيقة التي بيدي. **والوثائق تُختبَر كما تُختبَر الشيفرة، وإلا انحرفت في
صمت** — وهي أكثرُ ما يُقرأ عند التسليم وأقلُّ ما يُشغَّل.

---

## 🔴 وعدُ الشفافية يُحذف بجدولٍ أسبوعيّ، ولا وسيلة لحفظه

الدورتان الماضيتان سألتا: أين نصيحةٌ في وثيقةٍ لا يسندها الكود؟ فأُعيد السؤال
على أخطر ما فيها. وفي `docs/REVIEW.md` مكتوبٌ منذ حين:

> **التقليم يحذف ولا يؤرشف.** `pruneAuditLog` يُسقط ما تجاوز 180 يوماً بلا
> نسخة خارجية… فالتصدير قبل الحذف **مسؤولية خارجية**.

وقِيس: **لا سكربت تصدير في المستودع، ولا ذكرَ لنسخةٍ احتياطية في دليل النشر.**

فالمنصّة تَعِد بالشفافية، **وتُجدوِل حذفَ دليلها أسبوعياً**، ولا تُعطي وسيلةً
لحفظه. و«مسؤولية خارجية» هنا تعني: لا أحد. **ووعدٌ يُمحى دليلُه بجدولٍ ليس
وعداً.**

### وما لا يُصدَّر أهمُّ ممّا يُصدَّر

`_User` **مستثنىً قصداً**: فيه هواتف الأئمة والمتطوّعين، وتصديرُه يُنشئ ملفّاً
نصّياً ببيانات الناس يُنسخ ويُرسل ويُنسى على قرص. **وسجلُّ التدقيق نفسه مبنيٌّ
على أن الفاعل يُذكر بصفته لا باسمه** — فتصديرُ الحسابات معه ينقض ذلك من الباب
الخلفي. والاختبار يقيس ذلك بهاتفين حقيقيين في القاعدة ويطلب ألّا يظهرا في
الملفّ.

**والملفّ يقول ما ينقصه في رأسه**: من يفتحه بعد سنةٍ لا يقرأ السكربت الذي
أنشأه، **ونسخةٌ لا تقول ما ينقصها تُقرأ كاملة**. ويقول صراحةً إنه للمساءلة
والانتقال **لا للاسترجاع الآليّ** — فملفٌّ يُظنّ به ما ليس فيه أسوأ من غيابه.

### وثلاثةُ أخطاءٍ لي في دورةٍ واحدة، اثنان منها في أداة القياس

1. **مررتُ مفاتيح المِرقاة من `stack.APP_ID`** — وهي ثوابتُ وحدةٍ لا خصائصُ
   مكدّس، فكانت `undefined`. فمضى السكربت بمفتاحٍ خاطئ.
2. **ثم شخّصتُ العطب بلا قياس.** كتبتُ في الشيفرة تعليقاً يقول: «قِيس: السكربت
   كتب الملفّ ثم بقي معلَّقاً» — **والملفّ لم يُكتب أصلاً**. أثبتُّ سبباً لم
   أقسه، ووثّقتُه كأنه مقيس. وهذا أسوأ من الخطأ نفسه: **الوثيقة الكاذبة تُورَّث.**
3. **والسبب الحقيقي كان في أداة القياس:** `spawnSync` **يحجب حلقةَ أحداث
   العملية التي يعمل فيها `parse-server` نفسه**. فالطفل يطلب HTTP ولا أحد
   يُجيب، ويبقى معلَّقاً حتى تقتله المهلة — `status=null` بلا رسالةٍ واحدة.
   **وأداةُ قياسٍ تحجب المقيس لا تقيسه.**

**والدرس:** كلُّ تعليقٍ يبدأ بـ«قِيس» عهدٌ على قائله. وقد كتبتُ واحداً بلا
قياس، ولم يكشفه إلا أن الاختبار بقي ساقطاً — **ولو قام لَبقي الكذبُ في
الشيفرة إلى الأبد.**

---

## 🔴 نسخةٌ احتياطية تفقد صفوفاً في صمت — وتعليقي كان يَعِد بألّا تفعل

سكربتُ التصدير كُتب في الدورة الماضية، وفوق حلقة التصفّح فيه تعليقٌ بخطّي:

> الترتيب صريحٌ بـ`createdAt`: بلا ترتيبٍ يكون التقسيم بالصفحات عشوائياً،
> **فيتكرّر صفٌّ ويسقط آخر بلا أن يُعرف**.

**سمّيتُ العطب بدقّة، ثم لم أمنعه.** `createdAt` ليس فريداً: صفوفٌ تُكتب دفعةً
واحدة — `saveAll` في `closeInterests` و`warnImamsOfWorkerLoss` — تحمل الطابع
نفسه، **وترتيبُ المتساويَين غير معرَّف**.

### وقِيس على خادمٍ حقيقي

أوّل قياسٍ لم يُثبت شيئاً: ستّة عشر قيداً كُتبت بنداءات HTTP متتابعة، فلا طابع
مكرّر ولا فقدان. **والنفي هنا كان أثرَ العيّنة لا أثرَ الشيفرة** — فأُعيد
القياس على الحالة التي تُصادم فعلاً:

```
قيود: 56 · طوابع مكرّرة: 3
بالتصفّح: 56 صفّاً، منها 50 فريداً — مفقود 6

وبإضافة objectId: 56 من 56 — مفقود 0
```

**فُقد 11٪ من السجلّ صامتاً.** وفي نسخةٍ احتياطية هذا أسوأ ما يكون: لا يُكتشف
إلا يوم يُحتاج إليها، وحينها **لا يُعرف ما الذي ضاع أصلاً** — فالملفّ يبدو
تامّاً، وعدّادُه يوافق ما فيه لا ما كان.

### والحارس يُجبر التصفّح على العمل

حجم الصفحة صار يُضبط من البيئة (`MASJIDI_EXPORT_PAGE`)، وإلا لما مرّ الاختبار
بالتصفّح إلا بستّمئة صفٍّ حقيقي — **فيبقى أخطرُ ما في السكربت بلا حارس**.
والاختبار يكتب ستّين قيداً دفعةً، ويُصدِّر بصفحاتٍ من سبعة، ويقابل العدد
بالعدّ الحقيقي. وسقط بلا المفتاح الثاني بالنصّ: «تكرّر صفٌّ في التصفّح».

### والسبب الأعمق: نطاق الحارس أضيق من الخطر

`tests/portability.test.js` سلكُ تعثّرٍ يرفض أي صيغة استعلامٍ لم تُراجَع
محموليتها بين المحوّلين — **وكان يمسح `cloud/` وحده**. و`scripts/` تستعلم من
القاعدة الحيّة نفسها بالمفتاح الرئيس: تستورد، وتصدّر، وتُرقّي.

فصيغتا `skip` و`addAscending` دخلتا المستودع **بلا أن يمرّ عليهما الحارس
أصلاً**. ولمّا وُسّع النطاق إلى `scripts/` سقط فوراً وسمّاهما — وهو ما وُضع له.

وقُيّدت الصيغتان بحكمهما: `addAscending` محمولة **ولازمة مع التصفّح**،
و`skip` محمولة **ومكلفة على MongoDB** فلا تُستعمل في مسارٍ يراه مستخدم.

**والدرس:** حارسٌ نطاقُه أضيق من الخطر يُعطي أماناً بقدر نطاقه ويُقرأ بقدر
اسمه. وقد قرأتُ «سلك تعثّر لمحمولية الاستعلامات» فظننتُه يحرس الاستعلامات
كلَّها، **وهو يحرس مجلّداً**.

**والأدهى:** هذه ثاني مرّة في دورتين أكتب فيها دعوى في تعليقٍ لم أقسها. المرّة
الأولى كذبتُ في التشخيص، وهذه كذبتُ في الضمان — **ولم يكشفهما إلا القياس بعد
الكتابة**. فليكن كلُّ «هذا يمنع كذا» في تعليقٍ **اختباراً لا جملة**.

---

## 🔴 فحصٌ أحمرُ دائماً بسببٍ لا صلة له بما يفحص

الدورتان الماضيتان وجدتا عطباً في شيفرةٍ كتبتُها قبلهما بيوم. **فقصدتُ هذه
المرّة إضافاتي نفسها**، وبدأتُ بأخطرها: فحصُ الفهرس الفريد في `preflight` —
وهو **يكتب على القاعدة الحيّة**.

وقِيس على خادمٍ حقيقي:

```
فحص الفهرس: ok=false · التفصيل: governorate is required
```

**لم يختبر الفهرس قطّ.** المخطط يشترط `governorate` على `Mosques`، وصفُّ
الفحص كان يحمل `externalId` و`name` و`hasLocation` وحدها — **فيسقط على تحقّق
المخطط قبل أن يبلغ التكرار أصلاً**.

### وأثرُه أسوأ من غيابه

كتبتُ في الدورة التاسعة عشرة أن التقرير **يبقى أحمر حتى تُضاف الخطوة اليدوية،
وأن ذلك مقصود**. فماذا يقع فعلاً؟ المُشغّل يضيف الفهرس من لوحة Back4app —
**ويبقى التقرير أحمر**، بالرسالة نفسها، إلى الأبد. فلا يعرف أنه أصاب، ولا
يعرف أن الفحص كاذب.

**وفحصٌ أحمرُ دائماً يُعلَّم أنه ضجيج فيُهمَل** — ومعه يُهمَل ما يصدق. وهذا
أسوأ من لا فحص: لا فحصَ يُبقي السؤال مفتوحاً، وفحصٌ كاذب يُغلقه على الخطأ.

### والاختبار كان يمرّ لسببٍ خاطئ

كتبتُ في تلك الدورة:

```js
assert.equal(unique.ok, false, 'قُبل معرّفٌ مكرَّر ومرّ الفحص');
```

**وهو صحيحٌ ويمرّ — والفحص لا يعمل.** أكّدتُ **النتيجة** ولم أؤكّد **الآلية**:
سقط، نعم، لكن لسببٍ غير الذي وُضع له. فصار الاختبار يقيس التفصيل نفسه:
أن يذكر «معرّفٌ خارجيّ مكرَّر»، وألّا يذكر `required`.

### وتمييزُ الردّ عن العطب

وأُضيف شرطٌ ثالث: **ليس كلُّ ردٍّ بيّنةً على الفهرس**. لو رُدّ الصفُّ الثاني
لأي سببٍ آخر — مُشغّلٌ يمنع، أو حقلٌ يتغيّر — لقُرئ ذلك «التكرار مرفوض» وهو
ليس كذلك. فيُطالَب الردُّ بأن يذكر التفرّد (`duplicate`/`unique`/`E11000`)،
وما عداه يُرفع كما هو.

### ثالثةُ ثلاثٍ

هذه ثالث دورةٍ على التوالي يكون العطبُ فيها في شيفرةٍ كتبتُها أنا، وثالث مرّة
يكون الجذرُ واحداً:

| الدورة | ما ادّعيتُه | ما كان |
|---|---|---|
| ٢٣ | «قِيس: كتب الملفّ ثم بقي معلَّقاً» | الملفّ لم يُكتب |
| ٢٤ | «الترتيب الصريح يمنع فقدان الصفوف» | لم يمنعه — الطابع ليس فريداً |
| ٢٥ | «الفحص يختبر الفهرس الفريد» | يسقط على حقلٍ مطلوب قبله |

**والجذر واحد: أؤكّد النتيجة ولا أؤكّد الآلية.** والاختبار الذي يسأل «أسقط؟»
يمرّ على عطبين متعاكسين، والذي يسأل «لماذا سقط؟» لا يمرّ إلا على واحد.

**فالقاعدة:** اختبارُ حارسٍ يؤكّد **رسالته** لا حالَه وحده. و«سقط» ليست
نتيجة — هي بدايةُ سؤال.

---

## 🔴 أوّلُ أمرٍ في النشر يخرج بصفر وقد سقط أخطرُ صنفٍ في المنصّة

`docs/DEPLOY.md` يرتّب الساعة الأولى: املأ `.env`، ثم **`npm run schema`**، ثم
ارفع كود السحابة، ثم `npm run preflight`. فبدأتُ من أوّلها.

وقِيس على خادمٍ حقيقي، بحقن فهرسٍ واحدٍ على حقلٍ غير معرَّف في `ServiceRequests`:

```
✓ Mosques (+5 فهرساً)
✓ Transactions (+2 فهرساً)
…
✓ الفهارس المطبَّقة في هذه الجولة: 17
✗ ServiceRequests: Field fieldThatDoesNotExist does not exist, cannot add index.
  ✗ فهرس status_mosque: Class ServiceRequests does not exist.
رمز الخروج: 0
```

ثلاثةُ أكاذيب في مخرجٍ واحد. **الصنف لم يُنشأ أصلاً** — لا حقوله ولا صلاحياته،
والمحاولة الثانية تُعيد الفهارس وحدها فتصطدم بأنه غير موجود. والعدّاد يقول
سبعةَ عشر والمطبَّق أربعةَ عشر: `applied += plain.length` كان يجري خارج
`try/catch` فيحسب الساقط ناجحاً. **ورمز الخروج صفر.**

### وأثرُه ليس فهرساً ناقصاً

`ServiceRequests` أحد الثلاثة المقفلة للكتابة من العميل — والقفل **صلاحياتٌ في
المخطط** (`create: {}`, `update: {}`, `delete: {}`)، لا شيء غيرها. وصنفٌ غائب
يُنشئه أوّل من يكتب فيه بصلاحياتٍ افتراضية. و`beforeSave` يحرس الكتابة، **ولا
`beforeDelete` في المستودع كلِّه** — فالحذف بلا حارسٍ ثانٍ.

وأسوأ ما فيه أن **لا شيء بعده يلتقطه**: `preflight` يفحص وجود الأصناف وحقولها
وصيغ الاستعلام، **ولا يفحص الصلاحيات إطلاقاً**. فلو قام الصنف ناقصَ قفله لَما
قال أحدٌ شيئاً.

### القفل شرطُ تشغيل، والفهرس تحسينٌ يُؤجَّل

فالإنقاذ صار على مرحلتين: **الحقول والصلاحيات أوّلاً وحدها بلا فهارس**، ثم
الفهارس واحداً واحداً بعد أن يقوم الصنف. والحالة تُقرأ من القاعدة من جديد قبل
الإنقاذ: `update` يضيف الحقول ثم الفهارس بلا معاملة، فقد تكون الحقول ثبتت وسقط
الفهرس — وإعادةُ حقلٍ ثابت خطأ تُسقط الإنقاذ كلَّه.

والخلاصة تفرّق بين **«أصنافٌ لم تقم»** (قف، لا تمضِ) و**«أصنافٌ بلا بعض
فهارسها»** (تعمل، وبطيئةً على ثمانية عشر ألف مسجد)، وكلتاهما تخرج بغير صفر،
**ولا يُطبع سطرُ نجاحٍ فوق عطب**.

### والحارس يجرّب مسار العطب على خادمٍ حقيقي

ولا سبيل إليه إلا بمخططٍ مكسورٍ عمداً، فأُتيح `MASJIDI_SCHEMA_FILE` — كما أُتيح
`MASJIDI_EXPORT_PAGE` من قبل، وللعلّة نفسها: **ما لا يُشتغَّل في الاختبار يبقى
بلا حارس.** والاختبار يُشغّل السكربت الحقيقي بـ`execFile` غير المتزامن (المتزامن
يُجمّد العملية التي تستضيف الخادم فيتعلّق إلى الأبد — وقعت فيها في الدورة ٢٣).

وثلاث حالات: مخططٌ سليم يخرج بصفر رغم سقوط الفهرسين المكانيّين المعتاد — وبلا
هذه لصار الأمر أحمرَ في كل تشغيلة؛ وفهرسٌ فاسد على صنفٍ **جديد كلَّ مرّة** —
والاختبار يقرأ CLP من القاعدة ويطابقه بالمكتوب في `cloud/schema.json`، أي يؤكّد
**أن القفل وصل** لا أن الأمر احمرّ؛ وصنفٌ لا يقوم أصلاً، فيُقال إنه لم يقم.

وأُثبت سقوطه على `HEAD` بالسكربت القديم حرفاً بحرف ولا زيادة إلا منفذ الاختبار:

```
ok 1 - على مخططٍ سليم: يخرج بصفر…            ← يمرّ، فليس سقوطاً بيئياً
not ok 2 - وفهرسٌ فاسد لا يمنع الصلاحيات…     سقط الفهرس وخرج الأمر بـ0
not ok 3 - وصنفٌ لم يقم يُقال إنه لم يقم…     صنفٌ لم يقم وخرج الأمر بـ0
```

### وأوّلُ قياسٍ لي في هذه الدورة لم يُثبت شيئاً

أخرجتُ نسخة `HEAD` بـ`git show HEAD:scripts/apply_schema.js` — والمستودع جذرُه
فوق `masjidi/`، فردّ git بالخطأ **وأنشأت إعادةُ التوجيه ملفاً فارغاً**. فنسختُ
صفراً من البايتات فوق السكربت وشغّلتُ الاختبار: سقطت الحالات الثلاث، وكاد ذلك
يُقرأ إثباتاً. **وسقوطُ اختبارٍ على ملفٍّ فارغ لا يقول شيئاً عن `HEAD`.**

والذي كشفه ليس حَدْساً بل أن الحالة الأولى — «مخططٌ سليم يخرج بصفر» — سقطت هي
أيضاً، ولا سبب لسقوطها في العطب المقيس. فأعدتُ القياس بالمسار الصحيح، وحينها
مرّت الأولى وسقطت الأخريان: **وهذا هو شكل الإثبات.**

وهو الدرس نفسه من زاويةٍ أخرى: نتيجةٌ حمراء ليست بيّنة حتى يُعرف **لماذا**
احمرّت. ولولا أن في الحارس حالةً يجب أن تبقى خضراء لَمَرَّ الخطأ.

---

## 🔴 قفلُ المنصّة كلِّه في سطرٍ لا يقرؤه أحد بعد النشر

الدورة الماضية أوصلت الصلاحيات إلى القاعدة عند التطبيق. **وما بعد التطبيق؟**

وقِيس على خادمٍ حقيقي: فُتح `ServiceRequests` بـ`create/update/delete: {"*":true}`
— ضغطتان في لوحة Back4app — ثم شُغّل الفحص:

```
قبل الفتح — الساقط: ["الفهرس الفريد على Mosques.externalId"] · 16 فحصاً
بعد الفتح — الساقط: ["الفهرس الفريد على Mosques.externalId"] · لا شيء تغيّر
أيُّ فحصٍ يذكر الصلاحيات؟ 0
```

وقِيس أثرُ ذلك الفتح، بحسابِ متطوّعٍ لا صلة له بالطلب:

```
بالقفل المكتوب — حذفَ غريبٌ الطلب: رُدّ (119) Permission denied
بالقفل المفتوح — حذفَ غريبٌ الطلب: **نجح** · ذهب فعلاً من القاعدة
```

**وهذه هي الطبقة الوحيدة.** الكتابة يحرسها `beforeSave` فوق الصلاحيات، **ولا
`beforeDelete` في المستودع كلِّه** — فتحتَ الحذف لا شيء. ولو كان الصنفُ
`AuditLog` لَمُحيت الشفافيةُ التي قامت عليها المنصّة، بالأداة التي تُثبتها.

### والقاعدة لا تُقابَل بالملفّ

أوّل ما خطر أن يُقرأ `cloud/schema.json` ويُقابَل بما في القاعدة. **وهو خطأ**:
الحالةُ التي نبحث عنها بعينها هي **ملفٌّ صحيحٌ لم يُطبَّق** — وهي حالة الدورة
الماضية حرفاً بحرف. فمقابلةُ الملفّ بنفسه تُخضّرها.

فالمكتوب في `preflight` قائمتان صريحتان: سبعةُ أصنافٍ لا يكتب فيها عميلٌ بحال،
وثلاثةُ أصنافٍ لها حقولٌ لا تصله ولو قرأ الصفّ. وثمنُ ذلك أن تنحرف القائمة عن
الملفّ — **وصنفٌ يُقفل هناك ولا يُذكر هنا يبقى بلا فحصٍ ولا يسأل عنه أحد**.
فالحارس على الثمن في `tests/schema.test.js`: يقرأ المخطط، ويأخذ كلَّ صنفٍ
`create/update/delete` فيه فارغة، ويطالب بذكره. وأُثبت أنه يمسك الانحراف فعلاً
بحذف `AuditLog` من القائمة:

```
AuditLog مقفلٌ في المخطط وغير مذكورٍ في LOCKED_CLASSES — فقفلُه بلا فحص
```

### والغيابُ يُقرأ فتحاً لا قفلاً

قِيس شكلُ الردّ قبل كتابة الشرط: `parse-server` يعيد المفاتيح كلَّها صريحةً —
`{}` للمقفل و`{"*":true}` للافتراضي. فلا حاجة إلى تخمين. لكنّ الشرط كُتب على
أن **مفتاحاً غائباً يُعدّ مفتوحاً**: محوّلٌ آخر قد يصمت، وصمتُه ليس بيّنةَ قفل.

والرسالة تُسمّي البابَ ومن فُتح له (`ServiceRequests.create ← *`) لا تقول
«الصلاحيات غير مطابقة»: من يقرأ عطباً في قفلٍ يحتاج أن يعرف أيَّ بابٍ يُغلق.

### والحارس فيه حالةٌ يجب أن تبقى خضراء

ثلاثُ حالات: قفلٌ سليم يُقرأ خضراً، وبابان يُفتحان فيُسمَّيان **ولا يُتَّهم صنفٌ
قفلُه سليم**، ثم يُعاد القفل فيعود أخضر — **فليس أحمرَ دائماً**. ومثلُها للحجب:
يُنزع `phone` من `_User` وحده، فيُذكر ولا يُذكر `crNumber` الباقي.

وعلى `HEAD`: ثمانيةٌ تمرّ واثنتان تسقطان، الأولى برسالتها الصريحة **«الصلاحيات
لا تُفحص أصلاً»**. والثمانيةُ المارّة هي البيّنة على أن السقوط مقصودٌ لا بيئيّ —
وهو الدرس المستفاد من زلّة الدورة الماضية.

---

## 🔴 سجلُّ التدقيق يُزوَّر ويُمحى من متصفّح — إن سقط سطرٌ واحد

الدورة الماضية جعلت `preflight` يرى القفل إن فُتح. **والرؤية ليست منعاً.**
فسُئل: ما الذي يقف خلف القفل إن سقط؟

وقِيس على خادمٍ حقيقي: فُتحت أقفال الأصناف الخمسة، ثم جُرّب كلُّ باب بحساب
متطوّعٍ عاديّ لا صلة له بشيء:

```
--- AuditLog ---         إنشاء: **نجح**   تعديل: **نجح**   حذف: **نجح**
--- ServiceRequests ---  إنشاء: رُدّ (119)  تعديل: رُدّ (119)  حذف: **نجح**
--- MosqueClaims ---     تعديل: **نجح**   حذف: **نجح**
--- Notifications ---    تعديل: **نجح**   حذف: **نجح**
--- TaskInterests ---    حذف: **نجح**
```

فكُتب من متصفّحه قيدُ تدقيقٍ يقول `action: payout_released` بصفة `admin`.
**وسجلٌّ يُزوَّر ويُمحى من متصفّح ليس سجلَّ مساءلة** — وهو وعدُ المنصّة كلُّه.

### والردُّ العارض ليس حماية

ثلاثةٌ من الأبواب رُدّت بـ`142 … is required` — أي **تحقّق المخطط** لا حارس.
ومن يملأ الحقول يمرّ. وواحدٌ رُدّ بـ`101 Object not found` بلا سببٍ عرفتُه،
**ولم أعدّه حمايةً**: لم يأتِ من شيءٍ كتبناه، وبابُ الحذف على الصنف نفسه مرّ.

### وحارسُ الكتابة لا يُقرأ حارساً على المحو

`ServiceRequests` له `beforeSave` منذ البداية، وقد ردّ الإنشاء والتعديل —
**ومرّ الحذف**. لأن `beforeSave` لا يُنادى عند الحذف أصلاً. فوجودُ حارسٍ على
صنفٍ لا يعني أن أبوابه محروسة.

والحصيلة: `beforeSave` على ثلاثةٍ من سبعة، و`beforeDelete` على **صفر**.

### والقاعدة المكتوبة في هذا المستودع تقول ما يُفعل

«الشرط الذي يُفحص عند الدخول ثم يُنسى ليس شرطاً» — الاعتماد يُفحص في
`assignWorker` **وفي `startWork`**، وإيقافُ الحساب في `beforeLogin` **وفي
`requireUser`**. فهذا بابُ الأصناف: `beforeSave` للأربعة التي بلا حارس،
و`beforeDelete` للسبعة جميعاً، بالصيغة القائمة نفسها (`if (!request.master)`).

### والحارس الذي يمنع المهام الدورية يكسر ما يحمي

التقليم يحذف `AuditLog` و`Notifications` أسبوعياً **بالمفتاح الرئيس**، وحارسٌ
يمنع الحذف مطلقاً يوقفه فتمتلئ الباقة بعد أشهرٍ بلا سببٍ ظاهر. فالشرط على
**الجلسة** لا على الفعل.

**وما لم يُقَس قيل:** حدُّ التقليم أدناه ثلاثون يوماً، و`createdAt` **لا
يُؤرَّخ رجعياً** — قِيس: طُلب 2025-07-06 فكُتب اليوم. فلا سبيل إلى تشغيل
المهمّة على صفوفٍ يبلغها حدُّها. فقِيس **نداءُ الحذف الذي تصدره حرفاً بحرف**
(`destroyAll` بالمفتاح الرئيس) لا المهمّة كلَّها — وهو موضع الخطر بعينه.

### وثلاثُ حالاتٍ يجب أن تبقى خضراء

القفل المكتوب يردّ العميل (والحارس لا يُنادى أصلاً — فالطبقة الثانية **لا
تُختبر** في الحالة السليمة)، والمفتاح الرئيس يحذف كما كان، و`preflight` يقرأ
الأقفال سليمةً بعد إعادتها. وعلى `HEAD`: **ثلاثٌ تمرّ وثلاثٌ تسقط** بـ
`Missing expected rejection` — أي أن التزوير والمحو **وقعا فعلاً**.

---

## 🔴 عمليةٌ تمّت وأُبلغ عنها بالسقوط — فألغى الإمامُ طلبه

السؤال المفتوح من الدورة الماضية: `_User` بلا `beforeDelete` قصداً — فماذا يقع
لِما يشير إليه حسابٌ زال؟

فقِيس: حُذف حساب المكلَّف وهو مكلَّف، ثم سحب الإمامُ تكليفَه.

```
[قبل السحب]  الحالة=assigned            · متطوّع=5TT2jSXGRi · noShowBy=[]
releaseAssignment: **سقط** (101) Object not found.
[بعد السحب]  الحالة=open_for_volunteers · متطوّع=لا شيء · noShowBy=["5TT2jSXGRi"]
```

**العملية تمّت.** الطلب تحرّر، والغياب قُيّد — ثم رُمي `101` من `fetchPointer`
وهي **بعد** `serviceRequest.save`. فالإمام يرى رسالةً إنجليزية على فعلٍ نجح،
فيضغط ثانيةً فيُقال له «لا يُسحب التكليف إلا قبل بدء التنفيذ» — لأن الحالة
تغيّرت فعلاً — **فيظنّ طلبه عالقاً فيُلغيه**. وهو ما فعلتُه في القياس نفسه قبل
أن أفهم ما جرى: `cancelServiceRequest` نجح، والطلب ذهب.

### وليست حالةً بل صنفاً

فمُسح الصنف: `fetchPointer(pointer, '_User')` بعد حفظٍ ثابت. وموضعٌ ثانٍ:
`completeService` يحفظ الطلب `completed` في السطر ٧٧٣، ثم `recordWorkerRating`
تجلب المنفّذ في ٩٠٩ لتكتب سمعته. **فيُعتمد العملُ في القاعدة ويُقال للإمام إن
اعتماده سقط** — وهو أشدّ: العمل أُنجز في المسجد فعلاً.

### والقاعدة كانت مكتوبة

«الآثار الجانبية لا تُسقط العملية — الإشعار والتدقيق يُسجّلان الفشل
ويبتلعانه». وهذا أشدُّ منها: أثرٌ جانبيّ **يقلب نتيجةَ عمليةٍ ثبتت**. والإشعار
والتدقيق محميّان منذ زمن، **وبحثُ الحساب لم يكن**.

والإصلاح لا يزيد شرطاً بل يحذف نداءً: المكلَّف مقروءٌ أصلاً في السطر ٥٩٠
(`.catch(() => null)` — وُضع لتمييز الموقوف)، فكان يُجلب مرّتين، **والثانية بلا
حماية الأولى**. فصار `const worker = assignee`. وفي `recordWorkerRating` صمتٌ
صريح: حسابٌ زال لا سمعة له تُكتب، ولا أحد يختار من لا حساب له.

### وخمسُ حالات، اثنتان يجب أن تبقيا خضراوين

المكلَّف القائم: السحب يعمل والغياب يُقيَّد عليه (`abandonedJobs = 1`)،
والاعتماد يكتب سمعته (`completedJobs = 1`, `avgRating = 4`). **وحارسٌ يُنجّي
المحذوف ويُسقط الحيّ لا ينفع.** والثلاث الأخرى تؤكّد **الآلية**: لا يكفي ألّا
يرمي — يجب أن يقول ما صار إليه الطلب، وأن يقبل الطلبُ مكلَّفاً جديداً بعده
(أي أن الرحلة لم تنقطع).

وعلى `HEAD`: **اثنتان تمرّان وثلاثٌ تسقط** بـ`Object not found.` حرفياً.

### وثلاثةُ أخطاءٍ لي في أدوات القياس هذه الدورة

| ما ظهر | ما كان |
|---|---|
| لا شيء يُطبع البتّة | `process.exit(1)` يبتر المخرَج المعلَّق عند الأنبوب |
| `(119) Permission denied` على التسجيل | كتبتُ `name` والحقل `fullName` — و`addField: {}` ردّه بحقّ |
| «المشرف اعتمد فسُجّل رفضاً» | المُعامل `approve` لا `decision` — فمرّ `undefined` |

الثالث كان سيصير «عطباً» يُكتب في هذه الوثيقة لولا أن سُئل: **لماذا رُفض؟**
ونتيجةٌ حمراء ليست بيّنةً حتى يُعرف سببها — وقد كتبتُ هذه القاعدة في الدورة
الماضية، فأدركتْني في هذه.

---

## 🔴 إمامٌ يبحث عن مسجده كما يسمّيه فلا يجد شيئاً

في التصميم المنشور ظهرت أقربُ نتيجةٍ لبحث «مسجد» من مسقط على **69 كم**، وكتبتُ
حينها أنه «عطبٌ وظيفيّ». فقِستُ قبل أن أصلح.

**والفرضية الأولى سقطت:** ظننتُ المِرقاة — أزرع أوّل 400 سجلّ فلعلّها بعيدة. وقِيس:

```
أوّل 400 (مِرقاتي)     · أقربها من مسقط: 2.8 كم
البيانات كاملةً        · أقربها من مسقط: 0.2 كم
```

فالمساجد قريبة، والفرزُ بالقرب يعمل. والسببُ في **كلمة البحث** نفسها:

```
من أوّل 400: اسمُه فيه كلمة «مسجد»: 14 من 400
وأقربُ ما اسمُه فيه «مسجد»: 69.0 كم — مسجد صومحان
```

### والعلّة في البيانات لا في البحث

بيانات الوزارة تفصل الاسم عن النوع: الاسم **علَمٌ مجرَّد** («العلوية»،
«المجيب»، «الرسول الأعظم»)، والنوع في حقلٍ آخر. وقِيس على الثمانية عشر ألفاً:

| النوع | السجلّات | اسمُه يحمل نوعَه |
|---|---|---|
| **مسجد** | **14,339** | **1,422 (10٪)** |
| جامع | 1,952 | 711 (36٪) |
| مصلى | 752 | 210 (28٪) |
| مصلى العيدين | 929 | 741 (80٪) |

و`nameTokens` كانت تُبنى من الاسم والقرية وحدهما. فمن يبحث عن مسجده **كما
يسمّيه كلُّ عُمانيّ** يكتب كلمةً ليست في الفهرس، و`containsAll` تشترط الكلَّ:

```
بحث «العلوية»        → 2 نتيجة · وجده
بحث «مسجد العلوية»   → **0 نتيجة**
```

أربعٌ من ستّ أنواعٍ جُرّبت أعطت **صفراً تامّاً**. وهذا أوّل باب المنصّة: إمامٌ
لا يجد مسجده لا يسجّله، ولا يصل إليه متطوّع، ولا يقوم عليه شيءٌ ممّا بعده.

### والنوع كان يُرسَل إلى الشاشة ولا يُقرأ

`type` في `PUBLIC_FIELDS` منذ البداية — يصل الواجهة في كل نتيجة بحث — والبطاقة
تعرض `{mosque.name}` وحده. فالإمام يمرّ على «العلوية» في القائمة ولا يعرف أنها
مسجده. **وحقلٌ يُكتب ويُرسَل ولا يُقرأ نيّةٌ لا سياسة** — وهي قاعدةٌ مكتوبة في
هذا المستودع منذ حين، وُجدت مخالفتُها في أظهر موضع.

### والفخّ الذي كاد يُمرّر إصلاحاً لا يُصلح

`normalizeArabic('مصلى')` تُعيد **`'مصلي'`**، والاستعلام يُطبَّع كذلك. فلو
أُضيف النوع إلى الفهرس **خاماً** لَما طابق أبداً — ولَبدا الإصلاح واقعاً وهو لم
يقع، ولَمرّ اختبارٌ يسأل «أدخل النوع الفهرس؟» ولا يسأل «أبأيّ صورة؟». فصار في
الحارس شرطٌ يقرأ الكلمات من القاعدة ويرفض الألف المقصورة فيها.

وسقفُ الكلمات لم يكن قيداً: قِيس أن ما يبلغه **سجلٌّ واحد** من 18,214، واثنان
بعد إضافة النوع. ويجيء النوع آخراً — فإن أُزيح شيءٌ فالعلَم أولى بالبقاء.

### والحارسان

على الخادم: مسجدٌ اسمُه بلا نوعه يُوجد بالعلَم وحده (**حالةٌ يجب أن تبقى
خضراء**) ويُوجد بالنوع معه. وعلى الواجهة: `mosqueTitle` لا تُكرّر النوع إن كان
في الاسم («مسجد مسجد صومحان»)، وتأخذ رأس النوع لا النوع كاملاً («مصلى النساء»
لا «مصلى نساء ( خاص ) النساء»).

وعلى `HEAD`: **سبعٌ تمرّ واثنتان تسقطان** — «مسجد الراحة» لا يجد مسجداً نوعُه
«مسجد» واسمُه «الراحة»، والنوع «مصلى نساء ( خاص )» دخل الفهرس خاماً.

**وقاعدةٌ مستوردةٌ قبل هذا الإصدار تحتاج إعادة استيراد** — الكلمات تُحسب عند
الاستيراد. وذلك مكتوبٌ في `docs/DEPLOY.md`، وإعادتُه تُحدّث ولا تُكرّر.

---

## 🟠 آخرُ طابورٍ بقيت ساعتُه مطفأة

بدأتُ هذه الدورة بمسحٍ على المخطط كلِّه لا بمرشَّحٍ بعينه: **أيُّ حقلٍ يُكتب
ولا يُقرأ، أو يُقرأ ولا يُكتب؟** — وهو صنفُ العطب الذي أثمر مراراً في هذا
المستودع (`isActive` كان يُكتب ولا يُقرأ فكان الإيقاف زينة).

**وأوّل مسحٍ أعطى «كلُّ الحقول غير مستعملة».** وهو مستحيل — `name` و`status`
في كل سطر. فالأداة كانت مكسورةً في تهريب التعابير النمطية عبر الصَّدَفة، ولو
قُرئت نتيجتُها لصارت الوثيقةُ فزَعاً بلا سبب. فأُعيدت الأداة في ملفٍّ مستقلّ،
**وضُبطت على حقولٍ يُعرف اتجاهُها يقيناً** قبل أن يُقرأ لها حكم.

وبها خرج ستّةَ عشرَ مرشَّحاً — والمسح يدلّ ولا يحكم، فقُرئ كلٌّ في موضعه:

| المرشَّح | ما تبيّن |
|---|---|
| `lastLat` · `lastLng` | تُقرأ **باسمها وسيطاً** في `geo.withinBox(…, 'lastLat', 'lastLng')` — لا يلتقطها نمط |
| `noShowBy` | يُكتب بـ`addUnique` لا بـ`set` — قصورُ الأداة لا الشيفرة |
| `AuditLog.actorId` | يُكتب ولا يُقرأ **قصداً**: السجلّ يذكر الصفة لا الهوية |
| `ServiceRequests.workDoneAt` | **يُكتب ولا يُقرأ — وهذا هو العطب** |

### والقاعدة كانت مكتوبةً ومطبَّقةً في كل بابٍ إلا هذا

«كلُّ طابورٍ ينتظر فيه إنسانٌ قرارَ إنسان يُعرض فيه كم انتظر — للناظر
وللمنتظِر جميعاً». طُبّقت على طلبات الملكية، وعلى اعتماد الشركات، وعلى
اهتمامات المتطوّعين، وعلى `assignedAt` (وفي تعليقها: «كانا يُكتبان على الخادم
ولا يُقرآن في موضع»). **وبقي `pending_imam_approval`** — وهو الطابور الذي فيه
عملٌ أُنجز في مسجدٍ فعلاً وينتظر صاحبُه اعترافاً به.

وقِيس في الشيفرة:

```
المتطوّع:  <p>بانتظار معاينة الإمام واعتماده.</p>      ← بلا مدّة
الإمام:    الصور · الملاحظة · التواصل · زرّ الاعتماد   ← بلا متى أُبلغ
```

والحقل مكتوبٌ في `markWorkDone` منذ أوّل يوم (`set('workDoneAt', new Date())`)
**ولا يعبر السلك**: `listRequests` تُعيد `assignedAt` و`startedAt` ولا تُعيده.

فالمتطوّع يرى الجملة نفسها في يومه الأوّل وفي شهره الثالث، ولا يدري أنُسي أم
لم يحن دوره. والإمام يُطلب منه اعتمادُ عملٍ بلا أن يعرف كم انتظر صاحبُه —
**وهو أثقلُ ما في الأمر: عملٌ بُذل في المسجد، والاعتراف به مؤجَّلٌ بلا علم**.

### ولا وسمَ تأخّرٍ — لأنه لا وعد

القاعدة تقول إن حدَّ التأخّر يُؤخذ من وعدٍ قُطع في الخادم لا يُبتدع في
الواجهة. وقِيس: **لا وعدَ بمدّةٍ للاعتماد** في رسائل `markWorkDone` ولا غيرها
— بخلاف طلب الملكية («يُراجَع خلال أيام عمل»). فالمدّة تُعرض و`overdueAfter`
تبقى `null`، **وحدٌّ مخترَع يصير عُرفاً ويُخوّف بلا وجه حقّ**. وللحارس شرطٌ
يمنع أن يُدسّ وسمُ تأخّرٍ لاحقاً بلا وعدٍ يسنده.

وعلى `HEAD`: حارسا `assignedAt` يمرّان (**وهما ما يجب أن يبقى أخضر**)، وثلاثةٌ
جديدة تسقط، أوّلها بالنصّ: «`workDoneAt` يُكتب على الخادم ولا يُرسَل إلى
العميل — فيبقى غير مقروء».

---

## 🟠 التحذير كُتب، والضغطة بقيت واحدة

قصدتُ في هذه الدورة ما **لم يُضغط** لا ما لم يُقرأ: اختبار المتصفّح ينتظر زرّ
«اعتماد الملكية» على طلب نقلٍ **ولا يضغطه**. فالمشرف — وهو أوّل إنسانٍ على
المنصّة — قرارُه الحاسم غيرُ مجرَّبٍ من طرفه.

وفرضيتان سقطتا قبل ذلك، وأقولهما: ظننتُ مسار الشركات غيرَ مجرَّبٍ في متصفّح
فإذا هو مغطّىً بالتسجيل والانتظار وسحب الاعتماد؛ وظننتُ توصيل زرّ الاعتماد
مشبوهاً (وقد أخطأتُ أنا في مُعامله في دورةٍ سابقة) فإذا هو سليم.

ثم قِيست الضغطة في متصفّح حقيقي على طلب نقلٍ يَنزع مسجداً من إمامٍ قائم:

```
=== ما يراه المشرف قبل الضغط ===
  هذا طلب نقل، لا تسجيلٌ أوّل. المسجد مسجَّل الآن باسم … اعتمادك يَنزعه منه
  اعتماد الملكية
=== الضغطة ===
  حواراتُ تأكيدٍ اعترضت: 0
  انتقل فعلاً: true
```

### والعطب ليس في الخبر بل في شكل الفعل

التحذير ممتاز، ومكتوبٌ فوقه في الشيفرة: «**نقلٌ لا تسجيلٌ أوّل: الضغطة نفسها،
والأثر ليس واحداً**». فالمنصّة **عرفت المشكلة وسمّتها ثم تركت الضغطتين على شكلٍ
واحد** — التمييز خبرٌ لا يقف في طريق اليد.

والمشرف يمرّ على طابورٍ فيه تسجيلاتٌ أولى لا ثمن لها ونقلٌ بينها، فيعتمد
بالتتابع. ويصل المنزوع منه: «نُقلت إمامة مسجدك إلى غيرك بقرار الإدارة». وهو
يُستردّ بمسار نقلٍ آخر — لكن بعد أن قيل للرجل إن مسجده أُخذ منه.

**وقاعدةُ هذا المستودع مكتوبة: ما لا رجعة فيه يُطلب باسمه** — كما يُطلب
الاستيراد الكامل بـ`--all`، ونصيحةٌ في وثيقةٍ لا يسندها الكود ليست حارساً.
وهنا: نصيحةٌ في **الشاشة** لا يسندها شكلُ الزرّ.

### والتسجيل الأوّل يبقى ضغطةً واحدة

هذا شرطُ الإصلاح لا زينتُه: ثمنٌ يُفرض على كل اعتمادٍ يُبطئ الطابور ويُعلَّم
أنه روتين، فيُضغط بالعادة كما كان. فالنقل وحده خطوتان، والثانية تُسمّي من
يُنزع ومن يُمنح — ومعها «تراجع» لمن فتحها بالخطأ.

### والحارس في متصفّح — لأن العطب في شكل الشاشة لا في منطقها

`tests/e2e/journey.test.js` يؤكّد على **بطاقة النقل وحدها** ألّا يكون فيها زرُّ
التسجيل الأوّل، وأن يفتح التأكيدُ نصّاً يُسمّي المنزوع منه، وأن يعمل التراجع —
**ويؤكّد في الوقت نفسه أن بطاقة التسجيل الأوّل تُبقي ضغطتها الواحدة**.

وأوّل صيغةٍ من هذا التأكيد كانت خاطئة: عددتُ الزرّ في الصفحة كلِّها فوجدتُه
مرّةً، وفي الطابور طلبُ تسجيلٍ أوّل يُبقيه بحقّ. **فالعدد لم يكن عطباً بل
تأكيداً غير مُضيَّق** — فضُيّق على البطاقة.

وعلى `HEAD`: سبعٌ وعشرون تمرّ وواحدةٌ تسقط بالنصّ: «النقل يُعرض بزرّ التسجيل
الأوّل نفسه — فيُضغط بالعادة».

---

## 🟠 «هذا مسجدي» — وعدُ انتماءٍ لا يُوفى

قصدتُ دوراً يختاره الناس عند التسجيل ولا أعرف ماذا يجدون: **المتبرّع**،
والتبرعات معطّلة بقرار. فقِستُ تجربته في متصفّح حقيقي.

**وفرضيةٌ سقطت أوّلاً:** رأيتُ زرّ «هذا مسجدي» معروضاً على المتبرّع فظننتُه
زرّ تسجيل الملكية — وهو للأئمة وحدهم فيُردّ. وقرأتُ الشيفرة قبل أن أكتب: الزرّ
ينادي `setFavoriteMosque` لا `claimMosque`، **فهو يعمل**. ولو كتبتُ قبل أن
أقرأ لَسجّلتُ عطباً لا وجود له.

ثم قِيست الضغطة وما بعدها:

```
=== الضغطة ===
  ما قيل له: مصلى النساء بالبطين صار مسجدك.
  حُفظ في الحساب: DeMjz6Eqhf ✓ · ويُعرض في «حسابي»: نعم ✓
=== ثم يُنشر احتياجٌ في «مسجده» ===
  وارد المتبرّع: 0 إشعاراً   ·   «لا تنبيهات بعد.»
```

**فالضغطة تعمل، والحفظ يعمل، والعرض يعمل — ولا يترتّب عليها شيء أبداً.**

### والعلّة أن الانتماء ليس معياراً في أيّ إشعار

`pushToNearbyVolunteers` تُصفّي بشرطين: `role === 'volunteer'`، **وموقع الجهاز
الآن** (`lastLat`/`lastLng`) أو المحافظة بديلاً. فمن أعلن انتماءه لمسجدٍ وهو في
بيته على بُعد ثلاثين كيلومتراً لا يعلم أن مسجده يحتاج شيئاً، **والمتبرّع لا
يُشعَر بحال** لأن دوره ليس «متطوّع».

و`favoriteMosqueId` — التعبير الوحيد عن الانتماء في المنصّة كلِّها — يُقرأ في
موضعٍ واحد: `getMyProfile`، أي ليُعرض على صاحبه. **حقلٌ يُكتب ويُعرض ولا يفعل
شيئاً.**

وهذا هو أوّل ما وعد به المنتجُ نفسه: «هذا مسجدك · هذه احتياجاته · وهذا أثرك».
الخطوة الأولى قائمة، والثانية لا تصل أبداً.

### والحفظ في الوارد هنا لا اختياريّ

الإشعار القريب يمرّ بـ`{ store: false }` قصداً: للمتطوّع شاشة «الفرص» يفتحها
متى شاء، فالإشعار تحسينٌ فوق قناةٍ قائمة. **ومن أعلن انتماءه لا شاشة له** —
المتبرّع لا يرى «الفرص» أصلاً — فالوارد قناتُه الوحيدة، وبلا حفظٍ لا يبلغه شيء.

### والحارس يؤكّد الرسالة والحدود معاً

نموُّ الوارد وحده لا يكفي: قد يجيء من خبرٍ آخر. فيُطالَب النصّ بأن يقول
«احتياجٌ جديد» وأن يحمل اسم المسجد. ومعه حدّان **يجب أن يبقيا خضراوين**: من
أعلن انتماءه لمسجدٍ آخر لا يُزعَج، والإمام لا يُخبَر بخبرٍ نشره بنفسه.

وعلى `HEAD`: أربعٌ تمرّ وواحدة تسقط بالنصّ: «أعلن أن هذا مسجده فلم يُخبَر
باحتياجه — ووعدُ الانتماء لا يُوفى».

---

## 🟠 قناةٌ تُنذر ولا تُطمئن

فُتحت في الدورة الماضية قناةٌ لمن أعلن أن هذا مسجده. فالسؤال الذي يليها:
**ماذا يصله فيها؟**

قِيست دورة الطلب كاملةً على `parse-server` حقيقي فوق PostgreSQL، من عين
المنتمي، ستَّ محطّات:

| المحطّة | وارد المنتمي |
|---|---|
| نُشر الاحتياج | ١ |
| تطوّع متطوّع | ١ |
| كُلّف المنفّذ | ١ |
| بدأ العمل | ١ |
| أُبلغ عن الإنجاز | ١ |
| **اعتمد الإمام العمل** | **١** |

والرسالة الواحدة هي هي: «احتياجٌ جديد في مصلى النساء بالبطين: إصلاح مكيّفات
المصلّى». وفي المقابل: الإمام يصله ثلاث رسائل، والمنفّذ اثنتان.

**فالقناة تحمل الخبر السيّئ وحده.** تقول لصاحبها إنّ في مسجده خللاً، ولا تقول
له إنّه أُصلح. وهذا أسوأ من الصمت: قلقٌ يتراكم بلا خبرٍ يُنهيه، على منصّةٍ
قامت كلُّها على الشفافية. ومن يفتح التطبيق بعد شهرٍ يرى خمسة أعطالٍ في مسجده
لا يعلم أنّ أربعةً منها قد أُصلحت.

### والنهايتان كلتاهما كانتا صامتتين

`completeService` تُشعر المنفّذ وحده، و`cancelServiceRequest` تُشعر المنفّذ
وحده. فأُلحِق بكلٍّ منهما نداءُ أهل المسجد:

- عند الاعتماد: «أُنجز في {المسجد}: {العنوان} — جزى الله من قام به.»
- عند الإلغاء: «لم يعد "{العنوان}" مطلوباً في {المسجد}.»

والإلغاء ليس تفصيلاً: تركُه بلا خبرٍ يُبقي في وارد المنتمي احتياجاً لم يعد
قائماً يظنّه ما زال ينتظر.

### ويُستثنى اثنان

الإمام ضغط الزرّ بنفسه، والمنفّذ وصلته رسالةٌ أدفأ باسمه («اعتمد الإمام
عملك… بارك الله فيك»). فمن كان منهما منتمياً للمسجد لا يُثنّى عليه الخبر.

### والحارسان

في `tests/integration/notified.test.js`: دورةٌ كاملة حتى الاعتماد ثم إلغاءٌ
منفصل. ويُطالَب النصّ لا العدد — «أُنجز»، واسم الطلب، واسم المسجد. ومعه ثلاثة
حدودٍ **يجب أن تبقى خضراء**: منتمي مسجدٍ آخر لا يُزعَج بفرح غيره، والإمام لا
يُخبَر بخبرٍ صنعه بنفسه، والمنفّذ لا يُخبَر مرّتين بإنجازٍ واحد.

وعلى `HEAD` (٤٧٨٩٤ بايتاً — فُحص الحجم قبل قراءة الحُمرة): ستٌّ تمرّ واثنتان
تسقطان بالنصّ الذي قِيس: «أُنجز العمل في مسجده ولم يُخبَر — الوارد كما هو:
["احتياجٌ جديد في جامع البلاغ: تجديد فرش المصلّى"]».

---

## 🟠 سجلٌّ لا يقول عن أيّ احتياجٍ يتكلّم

القناة صارت تحمل الخبرين، فتُبع أثرُ الخبر: من قرأ «احتياجٌ جديد في مسجدك»
أين يذهب به؟ قِيس في متصفّح حقيقي من عين المتبرّع: بطاقةُ التنبيه **لا شيء
فيها يُضغط** (`requestId` يصل من الخادم وتُسقطه الشاشة)، و«حولي» لا تذكر
الاحتياج، و«حسابي» لا تذكره. والباب الوحيد «سجلّ المسجد».

ثم قِيس السجلّ نفسه في مسجدٍ له **ثلاثة احتياجات** — وهو الحال المعتاد لا
النادر:

```
 1. أُلغي الطلب                 2. كُلّف منفّذ بالعمل
 3. سجّل متطوّع اهتمامه          4. بدأ العمل
 5. كُلّف منفّذ بالعمل            6. سجّل متطوّع اهتمامه
 7. طلب صيانة جديد              8. طلب صيانة جديد
 9. طلب صيانة جديد
```

**تسعةُ أسطرٍ، لا يقول واحدٌ منها موضوعَه.** ثلاثةُ «طلب صيانة جديد» متطابقة،
ومرّتا «كُلّف منفّذ». فمن قرأه لم يعرف أنُقل الفرش أم أُصلحت المكيّفات، ولا
أيّ الثلاثة أُلغي. وهذه هي الشاشة التي وُصفت في الكود بأنها «أداةُ الشفافية لا
سطرٌ يُثبت أن شيئاً وقع» — والقاعدة كانت مكتوبةً في الملفّ نفسه ومطبَّقةً على
**تصويب الموقع وحده**، بينما الاحتياجات — موضوع المنصّة كلِّها وأكثرُ ما
يُقيَّد فيها — بلا اسم.

### والعنوان يُجلب باستعلامين مهما طال السجلّ

جمعُ المعرّفات ثم استعلامٌ واحد بـ`containedIn` و`select('title')`. والباقة
٢٥ ألف طلبٍ شهرياً، فاستعلامٌ لكل سطرٍ يعني تسعةً في فتحةِ شاشةٍ واحدة —
والعناوين تتكرّر بين الأسطر أصلاً.

### والسطران الأخيران كشفا خطأً في الحارس نفسه

بعد الإصلاح الأول صار سبعةٌ من تسعة يقول موضوعه، وبقي «سجّل متطوّع اهتمامه»
صامتاً: قيدُه على كائن `TaskInterests` لا على الطلب. **وحارسي كان يصفّي
بـ`targetClass === 'ServiceRequests'` فمرّ أخضرَ على سطرين ما زالا صامتين** —
لولا أنّ القياس أُعيد بعد الإصلاح لا قبله وحده. فصارت التصفية **بالفعل لا
بالفئة**، والاهتمام يُترجَم إلى طلبه باستعلامٍ ثانٍ. وصار تسعةٌ من تسعة.

### والحدود التي تبقى خضراء

سطرٌ ليس عن احتياج (تصويب الموقع) لا يُنسب إليه عنوان؛ وطلبٌ حُذف يُبقي سطرَه
بعنوانٍ `null` **ولا يُختلق له اسم**؛ وهوية الفاعل لا تُكشف مع العنوان.

وعلى `HEAD` (٢٤١٧٧ بايتاً — فُحص الحجم قبل قراءة الحُمرة): اثنتان تمرّان
واثنتان تسقطان، والثالثة تسقط عند شرطها المسبق «لم يُقيَّد الاحتياج أصلاً
فالحدّ غير مقيس» — وهو نصُّ العطب بعينه.

---

## 🔴 بوّابةٌ لم تكن تنظر إلى الوارد

في الدورة الماضية سُجّل أنّ بطاقة التنبيه لا تُضغط. وهذه الدورة سُئل سؤالٌ
أدقّ: **لماذا لم يمسكه الحارس؟** ففي المستودع بوّابةٌ بُنيت لهذا الصنف بعينه —
`tests/admin-fields.test.js`: «ما يُرسَل إلى الشاشة يُقرأ فيها، أو يُكتب سببُ
تركه في `ALLOWED_UNREAD`».

والجواب: **الحارس قائمةٌ منتقاة من خمسة أزواج، وصندوق الوارد ليس فيها.** فهو
لم يفشل، بل لم يُسأل. وقياسٌ بأداته نفسها، بضابطين — حقلٌ يُقرأ يقيناً وآخر لا
وجود له — أعطى:

```
ما تُرسله getMyNotifications: items · id · body · kind · requestId · readAt · createdAt
ما لا تقرؤه الشاشة:           kind · requestId
   kind:      يُذكر في الشاشة بأي صيغة؟ لا، أبداً
   requestId: يُذكر في الشاشة بأي صيغة؟ لا، أبداً
```

فوُصل الزوج بالبوّابة، فسمّى العطبَ بنفسه: «حقولٌ تعبر السلك ولا تصل عيناً:
kind، requestId».

### `requestId` — الخبر بلا باب

يصل من الخادم مع كل إشعارٍ منذ أوّل يوم، وتُسقطه الشاشة. فالمتطوّع له «الفرص»
والإمام له «مساجدي»، **والمتبرّع لا شاشة له غير هذه** — قيل له «احتياجٌ جديد في
مسجدك» ولم يملك أن يرى ما هو. فصارت البطاقة تفتح الاحتياج: عنوانُه وحالتُه
ومسجدُه وموضعُه ووصفُه — **قراءةً لا فعلاً**، فالأفعال لها شاشاتها.

### `kind` — لا كاتبَ له ولا قارئ

قِيس فلم يمرّره أيُّ مستدعٍ لـ`pushToUsers` قطّ، ولا تذكره الشاشة. وحقلٌ بهذا
الوصف ليس تصنيفاً مؤجّلاً بل حمولةٌ فارغة على كل إشعار — والقاعدة ٥١ تسمّيه
«نيّةً لا سياسة». فرُفع من السلك ومن `push.js`، **والعمود يبقى في المخطط موسوماً
بأنه محجوز** — والقرار مكتوبٌ لا مسكوتٌ عنه.

### وحارسان لا واحد

البوّابة الحقلية تُثبت أنّ `requestId` صار يُقرأ، **ولا تُثبت أن الضغطة تُفضي
إلى شيء** — زرٌّ لا يفعل شيئاً يمرّ بها. فالحارس الثاني في متصفّح حقيقي
(`tests/e2e/notified-request.test.js`): متبرّعٌ يعلن انتماءه، فيُنشر احتياج،
فيضغط الخبر فيرى العنوان **والوصف** واسم المسجد. ومعه ثلاثة حدود: الرجوع يُعيده
إلى واردِه لا يُخرجه، ولا يُعرض عليه «يهمّني» وهو فعلُ متطوّع، **وحالةٌ تبقى
خضراء على `HEAD`** — وصولُ الخبر ونصُّه — إذ بقيّةُ الحالات تتسلسل من بابٍ
واحد، فلولا هذه لما دلّت الحمرةُ على شيء.

وعلى `HEAD` (٨٢٧٣٠ بايتاً لـ`screens.jsx` — فُحص الحجم قبل قراءة الحُمرة):
البوّابة الحقلية تسقط بالنصّ أعلاه، والمتصفّح يسقط بـ«الخبر بلا بابٍ يُفتح —
البطاقة نصٌّ وتاريخٌ ثم لا شيء»، وتبقى الحالة الخضراء خضراء.

---

## 🔴 أولويةٌ في المخطط لا وجود لها في المنصّة

البوّابة الحقلية كانت عمياء عن الوارد، فأُدخل الوارد فيها. والسؤال الذي يلي
مباشرةً: **من بقي خارجها؟** فمُسحت دوالُّ السحابة التي يستدعيها العميل، فإذا
البوّابة تنظر إلى ستّة أزواج **وأربعٌ منها تُعيد صفوفاً ذات حقول ولا تُفحص —
ستّةٌ وأربعون حقلاً لم تُفحص قطّ**: `getMosqueAuditTrail` و`getMyMosques`
و`getMyProfile` و`getNearbyOpportunities`.

وفيها وُجدت `urgency`.

### مُعطَّلةٌ من طرفيها

- الخادم يقبلها في `createServiceRequest` ويكتبها على **كل** طلب.
- `getNearbyOpportunities` وقائمةُ العميل تُرسلانها مع **كل** صفّ.
- ولا نموذجَ يرسلها ولا شاشةَ تذكرها — قِيس فلم تُذكر كلمة `urgency` في
  `screens.jsx` كلِّها ولا مرّة، ولا اسمَ عربيّ لها في `api.js`.

فكلُّ طلبٍ في الإنتاج `normal` إلى الأبد. **وقارئُ `schema.json` يرى
`low | normal | high` فيظنّ أنّ في المنصّة أولوية** — وهو أوّل ما يقرؤه مشترٍ.

فصار الإمام يسمّي الدرجة عند النشر، ويراها وسماً على بطاقته، ويراها المتطوّع في
«الفرص». **و«عادي» بلا وسم**: الوسمُ الذي يُعلَّق على كل بطاقةٍ لا يميّز شيئاً.

### وثغرةٌ فُتحت باللحظة نفسها

`urgency || 'normal'` و`category || 'other'` تكتبان ما يصل كما يصل. وما دامت
الدرجة لا تُعرض فالضرر محدود؛ **ولمّا صارت وسماً يقرأ `URGENCIES[x] || x` صار
نصٌّ حرٌّ من إمامٍ يظهر كما كُتب على شاشة كل متطوّع**. فقُيّد التعدادان بقائمتين
على الخادم، وهما القائمتان اللتان يحرس ترجمتَهما `labels.test.js` الآن.

### والبوّابة نفسها كانت تكذب على ImamHome

أعلنت أنّ `hasLocation` و`locationSource` و`type` لا تُقرأ. والحقيقة أن
الأوّلين يقرؤهما مكوّنٌ **ابن** (`LocateMosque`) تُمرَّر إليه الصفوف، وأنّ `type`
يُقرأ داخل `api.mosqueTitle(mosque)` التي تأخذ الكائن كلَّه — والأداة ترى وصولاً
إلى خاصّية لا تمريرَ كائن. فوُسّعت لتقرأ أبناء الشاشة المذكورين، وسُجّل الباقي
عذراً مكتوباً. **ونتيجةٌ حمراء ليست بيّنةً حتى يُعرف لماذا احمرّت** — ولو صُدّقت
هنا لأُصلح ما ليس معطوباً.

### وقراران آخران أخذتهما البوّابة من يدي

- **`amount` في سجلّ المسجد**: «قُيّد تبرّع» و«صُرفت مستحقات» بلا مبلغٍ تقولان
  إنّ مالاً تحرّك ولا تقولان كم — في شاشة الشفافية نفسها. فصار يُعرض. والمسار
  المالي معطَّلٌ في المرحلة الأولى فلا سطر له اليوم، **ويوم يُفعَّل لا يُطلَق
  ناقصاً**.
- **`_User.wilayat`**: ميّتٌ في الجهات الثلاث — لا نموذجَ يكتبه (شاشة التعديل
  تعرض المحافظة وحدها)، ولا شاشةَ تعرضه، ولا قارئَ له على الخادم (كلُّ
  `get('wilayat')` في المستودع على `Mosques`). فرُفع من السلك ومن مُدخلات
  `updateMyProfile`، والعمود محجوز. **والمحافظة تبقى**: تُقرأ فعلاً في خطة
  الإشعار البديلة.

### الحدود التي تبقى خضراء

على `HEAD` (٤٩٨٩٤ بايتاً لـ`requests.js`، و٨٤٩٠٠ لـ`screens.jsx`): ثلاثٌ من
حالات التكامل تمرّ — الخادم كان يقبل `high` ويُرسله، فالعطب كان في العميل وحده —
وتسقط حالتا التقييد. وفي المتصفّح تمرّ **ثماني عشرة** حالةً قبل موضع السقوط، ثم
تسقط الرحلة عند «درجة الاستعجال» لأن الحقل غير موجود. **ومن لم يسمِّ درجةً
فطلبُه عاديّ** — حدٌّ يبقى أخضر، فلا يصير السكوتُ استعجالاً.

---

## 🔴 «مسجد العيدين» — والمصلّى ليس مسجداً

سُجّل في الدورة الماضية أن بطاقة «الفرص» تقول «البلاغ» والصحيح «جامع البلاغ».
فلمّا فُتح البابُ تبيّن أنّ الأمر أوسع من بطاقة: **الإشعارات كلُّها تكتب بادئة
الاسم بيدها.**

```
cloud/functions/requests.js:  ... — مسجد ${mosque.get('name')}      × ٥ مواضع
cloud/functions/requests.js:  احتياجٌ جديد في ${mosque.get('name')}  × ٦ مواضع (عارياً)
```

وقِيس على بيانات الوزارة كاملةً: **5,297 من 18,214 — 29.1٪ — يُخطئ فيها نصُّ
«مسجد ‹الاسم›»**. ومنها ما ليس خطأً في الدقّة وحدها:

| النوع | ما يقوله الإشعار | الصواب |
|---|---|---|
| جامع (1,952) | مسجد المجيب | **جامع** المجيب |
| مصلى العيدين (929) | مسجد العيدين | **مصلى** العيدين |
| مصلى نساء (210) | مسجد النساء | **مصلى** النساء |

فمصلّى العيدين يُنادى مسجداً، ومصلّى النساء يُنادى مسجداً. وهذا في سياق
المساجد في السلطنة ليس ركاكةً في الصياغة.

### والحلّ نظيرةٌ لا نسخة

`cloud/lib/mosque-name.js` — **نظيرةُ `mosqueTitle` في `app/src/api.js` حرفاً
بحرف**، ويحرس تطابقَهما `tests/mosque-title.test.js` على البيانات كاملةً، كما
يحرس `text-clean` تطابقَ المطبِّعَين. فمنطقٌ واحدٌ في نسختين بلا حارسٍ يفترق بلا
أن يُلحَظ، فيُنادى المسجد في الإشعار بغير ما يُنادى به في الشاشة. وحارسٌ ثالث
يمسح المصادر فيرفض أن تعود بادئةٌ مكتوبةٌ بيد.

### وإصلاحٌ بدا مطبَّقاً ولم يقع

بعد تركيب الاسم على الخادم، بقيت البطاقة تقول «البلاغ». والسبب أنّ
`getNearbyOpportunities` تقصر `select` على `name` دون `type` — **فتردّ
`mosqueTitle` العلَم عارياً وهي تظنّ أنها ركّبته**.

ولم تكشفه بوّابةٌ ولا اختبارُ وحدةٍ ولا اختبارُ تكامل: كشفته **لقطةُ شاشةٍ
للبطاقة نفسها**، أُخذت بعد الإصلاح لا قبله. وهو نظير ما وقع في الدورة الثلاثين
حرفاً بحرف — إصلاحٌ صحيحٌ في موضعه لا يبلغ عين القارئ لأن البيانات لا تصل إليه.
**فالحارس الآن على النصّ الواصل (`hit.mosqueName === 'جامع البلاغ'`) لا على
الشيفرة.**

### الحدود التي تبقى خضراء

على `HEAD`: ستَّ عشرة حالةً تمرّ، وتسقط اثنتان بالنصّ المقيس — ورسالةُ السقوط
هي العطب بلسانه: «تم تكليفك بـ "فرش ساحة المصلّى" في **مسجد العيدين**… في
**مسجد جامع البلاغ**». ومعها حدودٌ باقية: ما لا نوع له يُعرض كما هو، ولا يُثنّى
النوع إن كان في الاسم أصلاً («جامع نبر» لا «جامع جامع نبر»).

---

## 🟠 مسجدٌ واحد باسمين في شاشةٍ واحدة

الإشعارات صارت تنادي المسجد بما هو. فالسؤال التالي: **وأين يبقى الاسم عارياً؟**
وقِيس في متصفّح على مصلّى عيدين — و929 سجلاً في بيانات الوزارة نوعُها ذلك:

```
بطاقة «مساجدي»:        مصلى العيدين   ✓
عنوان «طلبات الصيانة»: العيدين        ✗
عنوان «سجلّ المسجد»:    سجلّ العيدين    ✗
```

فالبطاقةُ تركّب الاسم، والعنوانُ الذي تفتحه **بالضغط على تلك البطاقة نفسها**
يأخذه خاماً. والمستعمل يرى الاسمين في ضغطةٍ واحدة.

### وثلاثةُ إصلاحاتٍ لم تكن كافية

المواضع الثلاثة الظاهرة (`mosqueName={trail.name}` مرّتين و`mosqueName:
mosque.name`) أُصلحت، ووُسّع الحارس النصّي ليرفض النمط. **ثم بقيت الشاشة على
حالها.**

والرابع كان يبني كائناً بحقلين:

```js
setTrail({ objectId: mosque.id, name: mosque.name })   // ← يُسقط `type`
```

فتُنادى `mosqueTitle` صحيحةً **وهي جائعة**، فتردّ العلَم عارياً. وهو نظير ما وقع
في `select` بالدورة الماضية حرفاً بحرف: **الدالّة تُستدعى في موضعها الصحيح
ويُمنع عنها ما تحتاجه.** فحارسٌ على موضع النداء لا يكفي — الحارس على **النصّ
المرسوم**.

### وحارسٌ في المكان الخطأ لا يسقط ولو كان العطب قائماً

وُضع الفحص أوّلاً في `journey.test.js` فمرّ أخضرَ على `HEAD` **مرّتين**. ولم
يُصدَّق ذلك: كُتب القياس إلى ملفّ من داخل المتصفّح فإذا البطاقة عندها **«جامع
الرحمة»** — واسمٌ يحمل نوعَه أصلاً يستوي فيه المركَّبُ والخام. فالحارس لم يكن
يقيس شيئاً.

فنُقل إلى `tests/e2e/mosque-naming.test.js` بمِرقاةٍ تحت اليد. **وحالُ شاشةٍ
ليس تحت اليد ليس موضعاً لحارس.**

### والمِرقاة كانت تُخضّر ما لا يعمل

مِرقاة الرحلة تكتب `name: 'مصلى العيدين'` **بلا `type`** — والوزارة تفصل
الاثنين. فصُحّحت إلى `name: 'العيدين', type: 'مصلى العيدين'`. **ومِرقاةٌ لا
تُشبه الإنتاج تُخضّر ما لا يعمل.**

### والبوّابة الحقلية طلبت قراراً

لمّا زال آخرُ موضعٍ يقرأ `mosque.name` خاماً، أعلنت البوّابة أن `name` «يعبر
السلك ولا يصل عيناً». وهو يُقرأ داخل `mosqueTitle(mosque)` التي تأخذ الكائن
كلَّه — فانضمّ إلى `type` في العذر المكتوب. **والحمرة هنا أثرُ إصلاحٍ لا انحراف.**

وعلى `HEAD` (٨٦٩٤٦ بايتاً لـ`screens.jsx`): حالتان تسقطان بالنصّ المقيس،
وحالتان تبقيان خضراوين — البطاقة تحمل نوعه، وما حمل اسمُه نوعَه لا يُثنّى
(«جامع الرحمة» لا «جامع جامع الرحمة»).

---

## 🔴 الأوّلُ ليس بالضرورة الأحقّ

سؤالُ الأسبوع الأول: **ماذا لو تنازع اثنان مسجداً واحداً؟** وقِيس على خادمٍ
حقيقي: الثاني يُردّ بسطرٍ واحد — «يوجد طلب ملكية معلّق لهذا المسجد.» — **وفيه
ثلاثةُ أعطاب**:

1. **لا يُفرَّق بين طلبي أنا وطلبِ غيري.** من أعاد الإرسال قيل له الجملةُ نفسها،
   فيظنّ أنّ غيرَه سبقه إلى مسجده.
2. **ولا بابَ بعده.** إمامُ المسجد الحقيقيّ يُردّ ولا يُقال له ماذا يفعل ولا كم
   ينتظر — وقاعدةُ المستودع أنّ «خبراً بلا فعلٍ تالٍ نصفُ خبر».
3. **والمشرف لا يعلم.** وهذا أثقلُها: أصعبُ سؤالٍ مفتوحٍ في هذه المنصّة — مكتوبٌ
   في `CLAUDE.md` نفسه — **كيف يُثبت الإمام أنه إمام؟** وأن يتقدّم اثنان على
   مسجدٍ واحد قرينةٌ من الطراز الأول على أنّ الملكية منازَعة. وكانت المحاولةُ
   الثانية تُردّ عند الباب ولا يُقيَّد منها شيء، **فيعتمد المشرفُ الأوّلَ لأنه
   الأوّل لا لأنه الأحقّ.**

### والقرينة تُقيَّد على الطلب لا تُلقى

`MosqueClaims.contestedCount` يُزاد ذرّياً كلّما رُدّ أحدٌ عن هذا المسجد. **وهو
سجلُّ الحدث لا اشتقاقٌ منه** — والفرق مقصود: القاعدة ٢٣ تمنع العدّادات التي
تنحرف عن مصدرها، ولا مصدرَ هنا لأنّ الطلب المُبعَد لا يُحفظ. ولا يُسقط الردَّ
إن أخفق الحفظ: المنعُ قائمٌ بذاته والقرينة أثرٌ جانبيّ.

وعلى بطاقة المشرف تحذيرٌ بلون التحذير لا بلون الطمأنينة — كتحذير النقل:
«**ملكية هذا المسجد منازَعة.** تقدّم شخصٌ آخر بطلبٍ عليه فرُدّ لوجود هذا الطلب.
تحقّق قبل الاعتماد — الأوّلُ ليس بالضرورة الأحقّ.»

### وحارسان، لأنّ واحداً لا يكفي

الأوّل على الخادم: أنّ العدّاد يُقيَّد ويصل الطابور. **ولا يُثبت أنّ المشرف
يراه** — وقد قِيس في الدورتين الماضيتين مرّتين أنّ حقلاً يصل صحيحاً وتُسقطه
الشاشة. فالثاني في متصفّح: التحذير موجودٌ على البطاقة المنازَعة بنصّه وفعله
التالي، **وغائبٌ عن بطاقةٍ لم ينازعها أحد** — إذ تحذيرٌ على كل بطاقةٍ لا يميّز
شيئاً.

وعلى `HEAD` (٤٢٢٤٧ بايتاً لـ`mosques.js`): تسقط خمسٌ وتبقى واحدة خضراء —
وأُضيفت تلك عمداً بعد أن سقطت الستُّ جميعاً في أول قياس، إذ **حقلٌ لا وجود له
يُقرأ `undefined` فيُسقط حتى الحدودَ التي وُضعت لتبقى خضراء**، فلا تعود الحمرةُ
الشاملة دالّةً على شيء.

### وأربعةُ فروضٍ سقطت قبل هذا

سُئلت هذه الدورة أربعةُ أسئلةٍ فكان الجواب في كلٍّ منها «الأمر سليم»، وتُذكر
لأنّ النتيجة السالبة قياسٌ لا فراغ:

- **الرسائل الإنجليزية عند انقطاع الشبكة**: الكود 100 مترجَمٌ أصلاً.
- **العمل بلا إنترنت**: قِيس في متصفّح — عامل الخدمة يُسجَّل، و14 ملفاً تُخزَّن
  مسبقاً، والتطبيق يفتح مقطوعاً برسالةٍ عربية: «لا يوجد اتصال — التطبيق مفتوح،
  لكن البيانات لا تُحدَّث حتى يعود الاتصال.» ولا تسرّبَ إنجليزيّ ولا انهيار.
- **خادمٌ بلا مشرف**: `preflight` يفحصه ويُسمّي الأمر الذي يُصلحه.
- **اعتمادُ طلبين على مسجدٍ واحد**: ممتنعٌ أصلاً — ومن هذا الامتناع خرج ما فوق.

---

## 🔴 ولا أحد يملك مسجداً

**تصحيحٌ من صاحب المشروع**، وهو أثقلُ ممّا يبدو: كانت المنصّة كلُّها تقول
«ملكية». والمساجد في السلطنة تتبع **وزارة الأوقاف والشؤون الدينية** — لا
يملكها إمامٌ ولا وكيلٌ ولا هذه المنصّة. وما يُسجَّل هنا **إشرافٌ على شؤون
الصيانة**.

فالخطأ ليس ركاكةً في الصياغة: هو **وصفٌ خاطئ لما تفعله المنصّة**، وقد يُقرأ
اقتحاماً لاختصاص الوزارة في بلدٍ تُنظَّم فيه المساجد بقانون — وهو أوّل ما يقرؤه
إمامٌ أو مشترٍ أو موظّفُ أوقاف.

### ما كان يُقرأ وما صار

| قبل | بعد |
|---|---|
| طلبات ملكية المساجد | طلبات الإشراف على المساجد |
| اعتماد الملكية | اعتماد التسجيل |
| ملكية هذا المسجد منازَعة | الإشراف على هذا المسجد منازَع |
| هذا المسجد مسجّل باسمك | أنت مسجَّلٌ على هذا المسجد |
| هذا المسجد غير مسجّل باسمك | لستَ مسجَّلاً على هذا المسجد |
| اعتمادك يَنزعه منه | اعتمادك ينقل الإشراف منه |
| نعم، انقل الإمامة | نعم، انقل الإشراف |
| نُقلت إمامة المسجد | نُقل الإشراف على المسجد |
| روجعت ملكية المسجد | روجع تسجيل المسجد |

وعُدّلت معها التعليقاتُ والوثائق — فوصفٌ خاطئ في تعليقٍ يقرؤه المالك الجديد
يُعيد العطبَ إلى الواجهة في أوّل شاشةٍ تُكتب بعده.

### ومساعدُ الإمام صفةٌ ثالثة

كانت الصفات اثنتين: إمامٌ ووكيل. وأُضيفت **`assistant` — مساعد الإمام** بنصّ
التصحيح. وحرمانُ الوكيل والمساعد يُعطّل مساجد، ويُجبر صاحبَه أن يسمّي نفسه
إماماً **فيكذب على المشرف** — والصفة إنّما تُقال ليتحقّق المشرف بما يناسبها.

### والحارس على ما يُعرض لا على التعليقات

`tests/messages.test.js` يمسح الشيفرة التي تصل المستخدم ويرفض «ملكية» و«مسجّل
باسمك» و«يُنزع». **والكتلةُ تُمحى كتلةً لا سطراً سطراً**: أوّلُ صياغةٍ للحارس
صفّت الأسطر التي تبدأ بعلامة تعليق، فعدّت متنَ تعليقات JSX كلاماً معروضاً —
وأسوأ من ذلك أنّ التصفية أزاحت الترقيم فأشارت إلى أسطرٍ غير التي وقع فيها ما
وُجد. فصارت الكتل تُستبدل بأسطرٍ فارغة بعددها: يُمحى المتن ويبقى الترقيم صادقاً.

وأُضيف حارسٌ ثانٍ في `labels.test.js` على جدولَي `CAPACITIES` — على الخادم وفي
الواجهة — فصفةٌ يقبلها الخادم بلا اسمٍ عربيّ تُعرض بحروفها الإنجليزية **على
بطاقة المشرف، وهي التي يقرّر عليها**. وقد كشف الحارسُ عمى أداته أوّلاً:
«قُرئت 0 قيمة والمنتظر 3 فأكثر» — لأن المفاتيح هي التعداد هنا لا القيم.

وأُثبت سقوطهما: الأوّل على `HEAD` بأربعة مواضع بأرقام أسطرها الصحيحة، والثاني
بحذف `assistant` من جدول الواجهة فقال: «صفاتٌ تُعرض بأسمائها البرمجية: assistant».

### وما لم يُغيَّر عمداً

**أسماءُ الفئات والدوالّ بقيت** — `MosqueClaims` و`claimMosque` و`isClaimed`.
و«claim» في الإنجليزية ادّعاءُ صفةٍ أو مسؤولية لا تملُّك (كـ«claim this
listing»)، فليست خاطئة. وتغييرُ اسم فئةٍ في Parse **هجرةُ بيانات** لا يقابلها
أثرٌ يراه مستخدم — والقاعدة أنّ ما يُقرأ يُصحَّح أوّلاً.

---

## 🟠 ووكيلُ المسجد يُجبَر أن يعلن إمامةً ليست له

التصحيحُ السابق أضاف «مساعد الإمام» إلى الصفات، فبقي سؤالٌ أسبقُ منه: **بأيّ
صفةٍ يدخل الوكيلُ أصلاً؟** والتسجيل على مسجدٍ يلزمه الدور `imam`، واسمُه المعروض
كان «إمام مسجد».

وقِيس في متصفّح على وكيلٍ سجّل حسابه:

```
حسابي:    «الصفة: إمام مسجد»
الترويسة: «سالم الوكيل · إمام مسجد»      ← في كلّ شاشة
ثم الصفات المتاحة عند التسجيل على المسجد:
          · إمام المسجد   · وكيل المسجد   · مساعد الإمام
```

**فهو الكذبُ نفسه الذي وُضعت `CAPACITIES` لتمنعه — أسبقُ منه وأظهر.** الصفةُ
الدقيقة مدفونةٌ في نموذجٍ يُملأ مرّةً، وإعلانُ الإمامة في ترويسة كلّ شاشة وفي
حسابه.

### والدور واحدٌ لأن الصلاحيات واحدة

فلا تُضاف أدوارٌ جديدة — يُصحَّح **اسمُه**:

| الجدول | قبل | بعد |
|---|---|---|
| `ROLES.imam` (الترويسة والحساب والتسجيل) | إمام مسجد | **قائم على مسجد** |
| `ROLE_LABEL.imam` (رسائل الخادم) | أئمة المساجد | **القائمين على المساجد** |
| `ACTOR_LABEL.imam` (سجلّ المسجد) | الإمام | **القائم على المسجد** |

ومن يقرأ «قائم على مسجد» يسأل: أهذا أنا؟ فيُقال له تحت الاختيار مباشرةً:
«إمامُ المسجد أو وكيلُه أو مساعدُ الإمام — وتُحدَّد صفتُك بالضبط عند تسجيلك على
المسجد.» **فخبرٌ بلا فعلٍ تالٍ نصفُ خبر**، واسمٌ عامٌّ بلا شرحٍ يصدّ من يخصّه.

### والحارس في متصفّح — ومعه حدٌّ يمنع أن يكون العلاجُ حذفاً

يسجّل وكيلٌ حساباً، فيُطالَب: أن يجد في النموذج ما يدلّه أنّ هذا بابُه، وألّا
تقول الترويسةُ ولا حسابُه «إمام مسجد»، **وأن تقول «الصفة: قائم على مسجد»** —
فلو حُذفت الصفة أو بقيت برمجيةً لمرّ النفيُ وحده أخضر.

وعلى `HEAD`: حالتان تمرّان وواحدة تسقط. **وأوّلُ حمرةٍ لم تكن هي**: سقط الفحص
بانتهاء مهلة `main`، وهي لا تُركَّب قبل الدخول أصلاً — فالنصّ يُقرأ من `body`.
ولو قُبلت تلك الحمرةُ بيّنةً لأُعلن إصلاحٌ لم يُقَس.

---

## 🟠 صُحّح الاسمُ وبقيت الجُمل

الدورة الماضية صحّحت **اسم الدور** إلى «قائم على مسجد». فبقي سؤال: **وماذا عن
الجُمل التي تخاطبه أو تصفه؟** ومُسحت النصوص التي تصل المستخدم، فإذا اثنتا عشرة
جملةً تفترض إمامةً:

```
app/src/api.js:66   'بانتظار اعتماد الإمام'          ← يقرؤها المنفّذ في مهامّه
app/src/api.js:97   'اعتمد الإمام العمل'             ← يقرؤها المصلّي في سجلّ المسجد
app/src/api.js:99   'صوّب إمام المسجد موقعه…'
screens.jsx:282     'الإمام يرى مهاراتك…'            ← تحت اختيار المهارات
screens.jsx:929     'بانتظار معاينة الإمام واعتماده'
screens.jsx:951     'بانتظار اختيار الإمام'
screens.jsx:1287    'إمام المسجد'                    ← على بطاقة التواصل
screens.jsx:1552    'وأنا إمامه الآن — أطلب نقل الإمامة'
requests.js:241     'والإمام يختار المنفّذ'
requests.js:833     'اعتمد الإمام عملك…'
mosques.js:763      'اعتُمد الإمام، ولم يُعتمد الموقع…'
```

وأثقلُها **بطاقة التواصل**: يقرؤها منفّذٌ على وشك أن يهاتفه، فيُناديه بما ليس
له في أوّل كلمة.

### والسجلُّ يُسمّي الفاعل مرّةً لا مرّتين

في سجلّ المسجد يُعرض الفاعلُ بصفته إلى جانب الفعل («· القائم على المسجد»). فـ
«اعتمد الإمام العمل» كانت **تكراراً وكذباً معاً**: صارت «اعتُمد العمل»،
و«صوّب إمام المسجد موقعه» صارت «صُوّب موقع المسجد على الخريطة». والصفةُ إلى
جانبها كما كانت.

### والاستثناء مكتوبٌ لا مسكوتٌ عنه

«إمام المسجد» تبقى في `CAPACITIES` وفي الدلالة تحت اختيار الدور وفي نصّ الرفض
الذي يعدّد الصفات الثلاث — **لأنها هناك اختيارٌ يختاره صاحبه لا وصفٌ يُفرض
عليه**. فالحارس في `tests/messages.test.js` يحمل قائمةَ استثناءٍ صريحة، ومعها
ضابطٌ يمنع أن يبتلع الاستثناءُ كلَّ شيء: يُتحقَّق أنّ «اعتمد الإمام العمل»
**لا** يطابق أياً من الاستثناءات.

وعلى `HEAD`: يسقط الحارس بالاثنتي عشرة جملةً بأرقام أسطرها، **ويبقى حارسُ
التملُّك إلى جانبه أخضر** — فالحمرة تدلّ على بابها لا على البيئة.

وكشف الحارسُ جملتين لم يرهما القياسُ الأول: «الإمام يرى مهاراتك» و«وأنا إمامه
الآن»، إذ كان المسحُ الأوّل يشترط علامة اقتباس في السطر — **ونصُّ JSX ليس بين
علامتين**.

---

## 🔴 الشارةُ تنطفئ في اللحظة التي تلزم فيها

`openRequestsCount` عدّادٌ يُحسب باستعلامٍ من مصدره — لا يُزاد بـ`increment` —
فهو موافقٌ للقاعدة ٢٣. **والعطب لم يكن في طريقة الحساب بل في قائمة المعدود**:
`pending_imam_approval` خارجها.

وقِيست دورة الطلب على خادمٍ حقيقي من عين القائم على المسجد:

| المحطّة | الشارة | غيرُ المغلقة فعلاً |
|---|---|---|
| نُشر الاحتياج | 1 | 1 |
| كُلّف المنفّذ | 1 | 1 |
| بدأ العمل | 1 | 1 |
| **أُبلغ بالإنجاز — ينتظر معاينته** | **0** | **1** |
| اعتمده | 0 | 0 |

**فاللحظةُ التي يصير فيها الطلبُ بانتظاره هو، هي اللحظة التي تختفي فيها
شارتُه.** يفتح «مساجدي» فيرى مسجداً لا شيء معلّقاً فيه، وفي الطابور عملٌ أُنجز
ينتظر معاينته — **وينتظر معه متطوّعٌ سمعتُه وساعاتُه موقوفةٌ على ضغطته**.

وهذا هو الطابور الذي سبق أن أُصلحت ساعتُه في دورةٍ ماضية («آخرُ طابورٍ بقيت
ساعتُه مطفأة»): عُرضت فيه المدّة، **وبقي البابُ الذي يقود إليه مطفأً**.

### وطلبٌ ينتظر الاعتماد ليس مغلقاً

لا يُعدّ مفتوحاً لأنه يقبل متطوّعين — لا يقبلهم — بل لأنّ أحداً لم يفرغ منه.
والشارة تقول «طلب مفتوح» لا «فرصة تطوّع».

### ولا فرصةٌ كاذبة للمتطوّع

`getNearbyOpportunities` تستعمل العدّاد **تصفيةً أوّلية** ثم تستعلم الطلبات
بـ`open_for_volunteers` وحدها. فمسجدٌ كلُّ ما فيه طلبٌ ينتظر الاعتماد يدخل
المرشّحين ويخرج بلا فرصة — **مرشَّحٌ زائد لا فرصةٌ كاذبة**. وهذا حدٌّ مقيسٌ في
الحارس لا مستنتَج.

### الحدود التي تبقى خضراء

على `HEAD` (١٤٢٣١ بايتاً لـ`triggers.js`): أربعٌ تمرّ — المنشور يُعدّ،
والمكلَّف يبقى، والمعتمَد يُسقط، والملغى يُسقط — وتسقط اثنتان بالنصّ المقيس:
«صار الطلبُ بانتظار صاحب المسجد فاختفى من شارته — وهي اللحظة التي يلزمه فيها.
0 !== 1».

---

## ما لم يُعالَج بعد

- **`nearestKnownInWilayat` تُعيد `name` ولا يقرؤه مستهلك.** الثلاثةُ الذين
  يستدعونها يقرؤون `km` و`blind` وحدهما. حقلٌ يُحسب ويُعاد ولا يُقرأ — صغيرٌ،
  ولم يُعمل هذه الدورة.
- **اختبار التكامل يعمل على PostgreSQL لا MongoDB — وهذا أكبر قيدٍ باقٍ.**
  حاولتُ إغلاقه فتبيّن أن كل طرقه مقفلة في بيئة التطوير هذه: مضيفات MongoDB
  نفسها و`repo.mongodb.org` تردّان 403، وإصدارات GitHub كذلك، ولا خفيّ Docker،
  ولا حزمة على npm تُرفق الملفّ التنفيذي. ورفضتُ البدائل الموهِمة — FerretDB
  لا يُطابق ما يطلبه Parse، والمحاكاة تعطي ثقةً كاذبة وهي أسوأ من لا شيء.
  فالبديل سلكُ تعثّر لا تغطية: `tests/portability.test.js` يمسح `cloud/` ويرفض
  أي صيغة استعلام لم تُراجَع محموليتها، ويرفض قائمةً صريحة من الصيغ التي تفرض
  فهارس أو دعماً يختلف بين المحوّلين. **لا يُثبت أن الموجود يعمل على MongoDB —
  يمنع أن يدخل جديدٌ بلا قرار.** والقيد الأصلي قائم كما هو: `npm run test:integration`
  المنطق واحد، ويبقى خارج التغطية: سلوك الفهارس، والاستعلام الجغرافي (يحتاج
  PostGIS محلياً و`2dsphere` هناك)، وذرّية `increment` تحت التزامن الحقيقي.
  **أوّل نشرٍ على Back4app هو أوّل تشغيلٍ حقيقي على MongoDB** — فليكن على بيانات
  تجريبية أولاً.
- **الطبقة الثانية تمنع ولا تُصحّح.** `beforeSave`/`beforeDelete` يردّان الجلسة
  حين يسقط القفل، **ولا يُعيدان القفل**. فالتقرير من `preflight` يبقى هو
  الطريق إلى معرفة أن شيئاً سقط — والحارس يشتري الوقت لا يُغني عن الإصلاح.
- **حسابٌ يُحذف فلا يُوقَف.** قِيس في الدورة ٢٩: صاحب الحساب يحذفه بجلسته
  فينجح، ولا `beforeDelete` على `_User`. والمنصّة لها أداةٌ مصمَّمة لكفّ أحد —
  **الإيقاف** (`--suspend`)، وهو يُبلّغ الأئمة ولا يُقيّد غياباً على ممنوع —
  **والحذف يتخطّاها كلَّها بصمت**. والدوال صارت تحتمل زوال الحساب، لكن
  المسجد يبقى `isClaimed` بمؤشّرٍ معلَّق (يُستعاد بمسار النقل — قِيس)، وسجلّ
  التدقيق يبقى بلا فاعلٍ يُعرف.
- **`isTransfer` يختلف عليه ندااءان.** قِيس: `claimMosque` يقول لمقدّم الطلب
  «طلب **نقل**»، و`listPendingClaims` يُعطي المشرف `isTransfer: false` للطلب
  نفسه — لأن `include('mosqueId.imamId')` **يُسقط المؤشّر المعلَّق كلَّه** حين
  يكون الإمام محذوفاً، فيُقرأ المسجد بلا صاحب. والعلاج أن يُسجَّل على الطلب
  لحظة تقديمه كما يُسجَّل `claimDistanceKm` — **ما يُشتقّ لاحقاً ينحرف.**
- **سرّ الـwebhook يُدار يدوياً.** لا تدوير للمفتاح ولا تحقق من توقيع البوابة
  نفسها (`HMAC`) — السرّ المشترك أضعف من التوقيع لكنه ما تدعمه ثواني حالياً.
- **الاسترداد قيدٌ محاسبي لا تحويل.** `refundDonation` يُعيد الرصيد ويُرجع الطلب
  للتمويل، لكن إعادة المال إلى المتبرّع تتم خارج النظام كما في الصرف. ولا يعمل
  بعد صرف المستحقات — تلك تسوية يدوية.
- **`searchMosques` يمسح في حالة واحدة باقية:** جزءٌ من داخل كلمة («لنور» في
  «النور»). الكلمات والبادئة تغطّيان ما قبلها، و`contains` يبقى خطة أخيرة نادرة
  الوقوع. الحل التام فهرس نصّي (`$text` على MongoDB) وهو غير معبَّر عنه في مخطط
  Parse — يُضاف يدوياً من لوحة Back4app إن لزم.
- **`nameTokens` تُحسب عند الاستيراد لا في المخطط.** المساجد المستوردة قبل
  إضافة الحقل لا تحمله، فلا يلتقطها بحث الكلمات (تبقى البادئة و`contains`).
  إعادة تشغيل `seed_mosques.js` تملؤها لأن الاستيراد idempotent.
- **التفرّد الحقيقي على `externalId` يبقى يدوياً.** مخطط Parse لا يعبّر عنه،
  فأضِف فهرساً فريداً من لوحة Back4app. حتى ذلك الحين الحماية كشفٌ بعد الوقوع
  (`seed_mosques.js --verify`) لا منعٌ قبله.
- **الفهرسان المكانيّان (`geo`, `volunteer_geo`) لم يُطبَّقا محلياً** — يحتاجان
  MongoDB أو PostGIS. القرب يعمل بدونهما على `geo_box`، فهما تحسينٌ لا شرط.
- **كلفة الاستيراد على باقة Back4app المجانية.** قِيس محلياً: SDK يجمع عشرين
  كائناً في طلب HTTP واحد، فالاستيراد الكامل ≈911 طلباً. لكن الباقة تحدّ 25 ألف
  طلب شهرياً، وإن كان Back4app يحتسب كل كائن داخل الدفعة طلباً مستقلاً صارت
  الكلفة ≈18,214 — أي ثلاثة أرباع الباقة في عملية واحدة. **تحقّق من طريقة
  الاحتساب قبل الاستيراد الكامل**، وشغّل `--limit` أولاً وراقب العدّاد.
- **التقليم يحذف ولا يؤرشف — والتصدير صار في المستودع.** `pruneAuditLog` يُسقط
  ما تجاوز 180 يوماً، و`npm run export` يحفظه قبل ذلك. **ولا استرجاع آليّ**:
  الملفّ للمساءلة والانتقال، وإعادته إلى القاعدة عملٌ يدويّ لم يُكتب بعد.

---

## 🟡 جولة ثالثة — ما أُغلق بعد ذلك

| المشكلة | الإصلاح |
|---|---|
| `payoutContractor` يقرأ الرصيد ثم يُنقصه، فصرفان متزامنان يجتازان الفحص معاً | الخصم أولاً بـ`increment` الذرّي ثم التحقق، ومع السالب تُعوَّض العملية ويُرفض الصرف. `isPaidOut` يُضبط فور تأمين المبلغ لتضييق نافذة الصرف المزدوج — إغلاقها تماماً يحتاج قيداً في قاعدة البيانات |
| `cancelServiceRequest` يُلغي عملاً قيد التنفيذ بلا إشعار المنفّذ | يُرفض الإلغاء عند `in_progress` و`pending_imam_approval`، ويُشعَر المكلَّف عند إلغاء طلب أُسند إليه |
| المستخدم يغيّر دوره متى شاء عدا `admin` | الدور يُختار عند التسجيل ويُثبَّت بعده؛ تغييره لاحقاً بـ Master Key فقط |
| الإمام لا يعرف مصير طلب ملكيته | `getMyClaims` — الفئة تبقى مقفلة والدالة تُعيد طلباته وحدها |
| `completedJobs` و`avgRating` معرّفان ولا يُكتبان | `completeService` يُحدّث العدد والمتوسط التراكمي للمنفّذ |
| `Number(estimatedCost) \|\| 0` يبتلع NaN فيصير مدخل فاسد طلباً تطوّعياً | تحقق صريح بـ`Number.isFinite` ورفض غير الرقمي |
| لا سجل لتغييرات الحالة، فلا جواب عن "من ألغى هذا الطلب ومتى؟" | فئة `AuditLog` و`lib/audit.js`، والقيد من الدوال لا من `afterSave` — المُشغّل لا يرى الفاعل لأن الحفظ بـ Master Key. القيد لا يُسقط العملية التي يوثّقها، ويُعرَض عبر `getMosqueAuditTrail` بالدور لا بالهوية |
| متبرّع يدفع ثم يُغلق التطبيق فيبقى مالُه غير مقيَّد | `paymentWebhook` يُقيّد لحظة الدفع، ومهمة `reviewPendingDonations` شبكة أمان لما يضيع منه. كلاهما يمرّ بـ`captureDonation` نفسها |
| المولّد يحذف كل سطر `require` بما فيها وحدات Node، فيبقى مرجع غير معرّف في المدمج | الحذف صار مقصوراً على الاستيراد النسبي (`./` و`../`) |
| `AuditLog` ينمو بلا حد على باقة 250 ميغابايت | مهمة `pruneAuditLog` تحذف ما تجاوز 180 يوماً، وبحدّ أدنى 30 يوماً مهما طُلب |
