node_modules/
.env
*.log
.DS_Store
data/*.xlsx
logs/
dist/
app/dist/

# ملفّات يكتبها محوّل التخزين في اختبار التكامل — تُحذف عند الإيقاف
# والاستثناء هنا لئلا يتسرّب شيء منها إن انقطع التشغيل
files/
backups/

# ناتجُ بناء نسخة العرض — يُولَّد من `app/demo/` ولا يُحفظ
app/demo-dist/
